# 🔒 Исправление утечки секретов

## ⚠️ КРИТИЧНО: Пароль базы данных был обнаружен в репозитории

GitGuardian обнаружил PostgreSQL URI с паролем в репозитории GitHub.

## Шаг 1: СМЕНИТЕ ПАРОЛЬ В БАЗЕ ДАННЫХ (ОБЯЗАТЕЛЬНО!)

### В Яндекс.Облаке:

1. Перейдите в **Managed Service for PostgreSQL**
2. Откройте ваш кластер
3. Перейдите в **Пользователи** или **Users**
4. Найдите пользователя `zumzam_admin`
5. Нажмите **Изменить пароль** или **Reset Password**
6. Сгенерируйте новый надежный пароль (минимум 20 символов)
7. Сохраните новый пароль в безопасном месте

## Шаг 2: Обновите переменные окружения

### В Vercel:

1. Перейдите в **Settings** → **Environment Variables**
2. Найдите `DATABASE_URL`
3. Обновите пароль в строке подключения:
   ```
   postgresql://zumzam_admin:НОВЫЙ_ПАРОЛЬ@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
   ```
4. Сохраните

### Локально:

Обновите `.env.local`:
```env
DATABASE_URL=postgresql://zumzam_admin:НОВЫЙ_ПАРОЛЬ@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
```

## Шаг 3: Удаление секретов из истории Git

Секреты уже удалены из текущих файлов, но остались в истории Git.

### Вариант 1: Использовать git-filter-repo (рекомендуется)

```bash
# Установите git-filter-repo
pip install git-filter-repo

# Удалите секреты из истории
git filter-repo --invert-paths --path prisma.config.ts --path scripts/test-insert-one.mjs --path scripts/test-all-migrations.mjs --path scripts/import-to-managed-pg.mjs --path scripts/import-to-managed-pg-v2.mjs --path scripts/import-remaining-tables.mjs --path scripts/import-rest-data.mjs --path scripts/import-profiles-only.mjs --path scripts/apply-migration.mjs --path docs/AI_CHAT_RESTORATION.md --path MIGRATION_COMPLETE.md

# Или удалите все вхождения пароля
git filter-repo --replace-text <(echo "SCNK88tank33==>ENV:DB_PASSWORD")
```

### Вариант 2: Force push после очистки (менее безопасно)

```bash
# Удалите проблемные файлы из истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch prisma.config.ts scripts/*.mjs docs/AI_CHAT_RESTORATION.md MIGRATION_COMPLETE.md" \
  --prune-empty --tag-name-filter cat -- --all

# Force push (ОСТОРОЖНО - это перезапишет историю!)
git push origin --force --all
```

## Шаг 4: Проверка

После удаления из истории:

1. Проверьте, что пароль больше не в файлах:
   ```bash
   git grep "SCNK88tank33"
   ```

2. Если ничего не найдено - хорошо!

3. Проверьте GitGuardian - утечка должна исчезнуть через несколько часов

## Шаг 5: Предотвращение в будущем

1. **Всегда используйте переменные окружения** для секретов
2. **Никогда не коммитьте** `.env*` файлы
3. **Используйте `.env.example`** для примеров (без реальных паролей)
4. **Проверяйте файлы перед коммитом**: `git diff` перед `git add`

## Файлы, которые были исправлены:

- ✅ `prisma.config.ts` - теперь использует `process.env.DATABASE_URL`
- ⚠️ Скрипты в `scripts/` - нужно обновить вручную (см. ниже)

## Обновление скриптов

Все скрипты в `scripts/` должны использовать переменные окружения:

```javascript
// ❌ ПЛОХО
password: 'SCNK88tank33'

// ✅ ХОРОШО
password: process.env.DB_PASSWORD || process.env.DATABASE_URL?.match(/:(.*)@/)?.[1] || ''
```

## Важно

После смены пароля:
1. ✅ Обновите `DATABASE_URL` в Vercel
2. ✅ Обновите `.env.local` локально
3. ✅ Перезапустите локальный dev сервер
4. ✅ Проверьте, что сайт на Vercel работает

