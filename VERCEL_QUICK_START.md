# 🚀 Быстрый деплой на Vercel

## Пошаговая инструкция

### 1. Подготовка

```bash
# Убедитесь, что проект собирается
npm run build
```

### 2. Создание проекта на Vercel

1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите **"Add New..."** → **"Project"**
4. Импортируйте репозиторий ZumZam
5. Vercel автоматически определит Next.js

### 3. Настройки сборки

**Install Command:**
```
npm install --legacy-peer-deps
```

**Build Command:**
```
npm run build
```

(Остальное по умолчанию)

### 4. Переменные окружения

В **Settings** → **Environment Variables** добавьте:

#### Обязательные:

```
DATABASE_URL=postgresql://zumzam_admin:ВАШ_ПАРОЛЬ@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
```

```
GOOGLE_GEMINI_API_KEY=ваш_gemini_api_key
```

```
JWT_SECRET=ваш_случайный_ключ_минимум_32_символа
```

#### Опциональные:

```
NEXT_PUBLIC_SITE_URL=https://ваш-проект.vercel.app
```

**Важно:** Для каждой переменной выберите окружения:
- ✅ Production
- ✅ Preview  
- ✅ Development

### 5. Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (2-5 минут)
3. Проверьте логи, если есть ошибки

### 6. Применение миграций БД

После первого деплоя:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Скачайте переменные окружения
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

### 7. Готово! 🎉

Сайт доступен по адресу:
`https://ваш-проект.vercel.app`

---

## 📋 Чек-лист перед деплоем

- [ ] Проект собирается локально (`npm run build`)
- [ ] Все изменения закоммичены в Git
- [ ] Репозиторий подключен к Vercel
- [ ] `DATABASE_URL` настроен в Vercel
- [ ] `GOOGLE_GEMINI_API_KEY` настроен в Vercel
- [ ] `JWT_SECRET` настроен в Vercel
- [ ] Миграции применены к БД

## 🔧 Частые проблемы

**Ошибка сборки Prisma:**
- Убедитесь, что в `package.json` есть `"postinstall": "prisma generate"`

**Ошибка подключения к БД:**
- Проверьте `DATABASE_URL` в Vercel
- Убедитесь, что БД доступна из интернета (firewall в Яндекс.Облаке)

**Ошибка JWT:**
- Убедитесь, что `JWT_SECRET` установлен и достаточно длинный (32+ символа)

## 📚 Подробная документация

См. `docs/VERCEL_DEPLOYMENT.md` для детальной инструкции.

