# Настройки Cursor для проекта ZumZam

## Автоматизация работы

Агенты могут автоматически:
- ✅ Создавать файлы без подтверждения
- ✅ Редактировать существующие файлы без подтверждения
- ✅ Запускать терминальные команды без подтверждения
- ✅ Устанавливать зависимости без подтверждения
- ✅ Запускать скрипты сборки и тестирования

## ❌ Что НЕ создавать

**ЗАПРЕЩЕНО создавать:**
- ❌ MD-файлы с документацией (CHANGELOG, README, инструкции, гайды)
- ❌ Файлы с пояснениями (SUMMARY, INSTRUCTIONS, TODO lists в файлах)
- ❌ Хелперы и вспомогательные скрипты "для удобства"
- ❌ Тестовые файлы без явной просьбы
- ❌ Комментарии-романы в коде

**Работай сухо и по делу:**
- ✅ Только рабочий код
- ✅ Минимум комментариев (только для сложной логики)
- ✅ Если нужно объяснить - напиши в ответе, не в файле

## Стиль кода

- Используй TypeScript для типизации
- Следуй принципам Clean Code
- Код должен быть понятен Junior разработчику
- Избегай наследования, рефлексии и сложных техников
- Используй функциональный подход где возможно

## Правила работы

1. Всегда проверяй линтер после изменений
2. Используй существующие компоненты и утилиты
3. Следуй структуре проекта
4. Добавляй комментарии для сложной логики
5. Используй русский язык для комментариев и сообщений пользователю

## Структура проекта

- `app/` - Next.js страницы и API routes
- `components/` - React компоненты
- `lib/` - Утилиты и хелперы
- `types/` - TypeScript типы
- `supabase/` - Миграции базы данных
- `docs/` - Документация

## Особенности

- Профили имеют динамическую структуру в зависимости от категории
- Используется векторный поиск через Supabase pgvector
- Визуализация деревьев профилей важна для понимания структуры

## 🔧 ЖЕЛЕЗОБЕТОННОЕ ПРАВИЛО: Шестеренки настроек в блоках профиля

**ОБЯЗАТЕЛЬНО для всех блоков с вариантами отображения:**

1. **Передача `isOwner` пропса:**
   - Все компоненты блоков ДОЛЖНЫ получать пропс `isOwner={canEdit}` из страницы профиля
   - `canEdit` определяется как: владелец профиля ИЛИ глобальный админ
   
2. **Логика отображения шестеренки:**
   ```tsx
   const { isClient } = useAuth()
   const showOwnerControls = Boolean(isOwner) && !isClient
   ```
   - Шестеренка показывается только для владельца/админа
   - Клиенты НИКОГДА не видят шестеренки

3. **Единый стиль шестеренки:**
   ```tsx
   <button 
     className="p-2 text-slate-600 hover:text-slate-900 hover:bg-slate-50 rounded-full transition-all"
     type="button"
     aria-label="Настройки дизайна"
     disabled={isUpdating}
   >
     <Settings className="w-5 h-5" />
   </button>
   ```

4. **Dropdown меню с описаниями:**
   ```tsx
   <DropdownMenuItem
     onClick={() => updateTemplate('section', 'templateId')}
     className={`cursor-pointer ${
       viewMode === 'templateId'
         ? 'bg-orange-50 text-orange-700 font-medium' 
         : 'text-slate-700'
     }`}
   >
     <div>
       <div className="font-medium">Вариант 1</div>
       <div className="text-xs text-slate-500">Краткое описание варианта</div>
     </div>
   </DropdownMenuItem>
   ```

5. **Обязательные блоки с шестеренками:**
   - О нас (AboutSectionClient)
   - Фото и видео (PhotoGalleryClient)
   - Услуги/Программы (ServicesBlock)
   - Доп. услуги (если есть)
   - Персонажи и программы (AnimatorCharactersSection)
   - Праздник под ключ (TurnkeyPackagesBlock)
   - Пакетные предложения (PackageTiersDisplay)
   - Часто задаваемые вопросы (ProfileFAQ)
   - Как нас найти / Адреса (LocationsTabs для venue)
   - Контакты с картой (ContactsBlock, только если !hideMap)

6. **Особенности блока Контакты:**
   - Это ОДИН компонент `ContactsBlock` для всех категорий
   - Для аниматора: `hideMap=true`, title="Контакты", НЕТ шестеренки (варианты бессмысленны без карты)
   - Для venue/других: `hideMap=false`, title="Как нас найти", ЕСТЬ шестеренка с 3 вариантами:
     * Вариант 1: Контакты сверху, карта снизу
     * Вариант 2: Карта сверху, контакты снизу
     * Вариант 3: Слева контакты, справа карта

## Дизайн-система (стиль Яндекса)

### 🔥 ЖЕЛЕЗОБЕТОННЫЕ ПРАВИЛА ОКРУГЛЕНИЙ (ОБЯЗАТЕЛЬНО!)

**ВСЕ округления в проекте ДОЛЖНЫ соответствовать этой системе:**

1. **`rounded-[32px]`** - Крупные блоки и кнопки:
   - Основные блоки секций (О нас, Услуги, Персонажи, Контакты, Отзывы)
   - Крупные action-кнопки (CTA)
   - Модальные окна (внешний контейнер)

2. **`rounded-[28px]`** - Внутренние блоки второго уровня:
   - Карточки внутри секций
   - Видео плееры и медиа контейнеры
   - Компактные списки (Accordion, вложенные контейнеры)

3. **`rounded-[24px]`** - Фото и элементы сетки:
   - Все изображения в галереях
   - Элементы сетки (masonry, grid)
   - Превью изображений

4. **`rounded-[18px]`** - Мелкие элементы:
   - Иконки и аватары
   - Теги и badges
   - Миниатюры
   - Инпуты и селекты
   - Мелкие кнопки

5. **`rounded-full`** - Только для:
   - Круглых аватаров
   - Pill-кнопок (капсульных)
   - Индикаторов и точек

**❌ ЗАПРЕЩЕНО использовать:** `rounded`, `rounded-sm`, `rounded-md`, `rounded-lg`, `rounded-xl`, `rounded-2xl`, `rounded-3xl`

### 🎨 ЦВЕТОВАЯ ПАЛИТРА (ОБЯЗАТЕЛЬНО!)

**Основные цвета:**
- **Акцент/Primary**: `orange-500` (#F97316) - кнопки, активные элементы
- **Акцент hover**: `orange-600` (#EA580C)
- **Акцент фон**: `orange-50` (#FFF7ED) - выделенные элементы
- **Акцент текст**: `orange-700` (#C2410C) - текст на светлом фоне

**Нейтральные цвета:**
- **Текст основной**: `slate-900` (#0F172A) или `gray-900` (#111827)
- **Текст вторичный**: `slate-600` (#475569) или `gray-600` (#4B5563)
- **Текст hint**: `slate-500` (#64748B)
- **Фон основной**: `white` (#FFFFFF)
- **Фон вторичный**: `slate-50` (#F8FAFC) или `gray-50` (#F9FAFB)
- **Фон hover**: `slate-100` (#F1F5F9)
- **Границы**: `slate-200` (#E2E8F0) или `gray-200` (#E5E7EB)

**Статусные цвета:**
- **Success**: `green-500` (#22C55E) / `emerald-500` (#10B981)
- **Warning**: `yellow-500` (#EAB308) / `amber-500` (#F59E0B)
- **Error**: `red-500` (#EF4444)
- **Info**: `blue-500` (#3B82F6)

**❌ ЗАПРЕЩЕНО использовать:** `purple-*`, `pink-*`, `indigo-*` (кроме специальных случаев)

### 📐 ТИПОГРАФИКА (Font Sizes)

**Заголовки:**
- **H1** (Главный заголовок страницы): `text-2xl sm:text-3xl font-bold text-slate-900`
- **H2** (Заголовок блока/секции): `text-xl sm:text-2xl font-bold text-slate-900 leading-tight`
- **H3** (Подзаголовок внутри блока): `text-lg sm:text-xl font-bold text-slate-900`
- **H4** (Заголовок карточки): `text-base sm:text-lg font-semibold text-slate-900`
- **H5** (Мелкий заголовок): `text-sm font-semibold text-slate-900`

**Текст:**
- **Body Large**: `text-base text-slate-700` (основной текст, описания)
- **Body**: `text-sm text-slate-700` (стандартный текст)
- **Caption**: `text-xs text-slate-500` (подписи, хинты)
- **Uppercase Label**: `text-[11px] font-semibold text-slate-400 uppercase tracking-wider` (метки секций)

**Адаптивность:**
- Всегда используй `sm:` брейкпоинт для увеличения шрифта на больших экранах
- Пример: `text-xl sm:text-2xl`

### 🔘 КНОПКИ (Buttons)

**Размеры:**
- **Large** (основные CTA): `h-11 px-8 text-base font-semibold`
- **Medium** (стандартные): `h-10 px-6 text-sm font-semibold`
- **Small** (компактные): `h-9 px-4 text-sm font-semibold`
- **Icon** (только иконка): `w-10 h-10 p-2`
- **Settings** (шестеренка): `p-2` + иконка `w-5 h-5`

**Стили:**
- **Primary**: `bg-orange-500 hover:bg-orange-600 text-white rounded-full shadow-sm hover:shadow-md transition-all`
- **Secondary**: `bg-slate-100 hover:bg-slate-200 text-slate-900 rounded-full transition-all`
- **Ghost**: `hover:bg-slate-50 text-slate-700 rounded-full transition-all`
- **Pill** (капсула): Всегда `rounded-full`

**Иконки в кнопках:**
- Large button: иконка `w-5 h-5`, gap `gap-2`
- Medium/Small button: иконка `w-4 h-4` или `w-3.5 h-3.5`, gap `gap-1.5`

### 📦 SPACING (Отступы и промежутки)

**Padding секций:**
- **Основные блоки**: `p-1` (внешний контейнер), внутренние `px-6 py-6 pb-4`
- **Карточки внутри**: `p-4 sm:p-6`
- **Компактные элементы**: `p-3 sm:p-4`
- **Модальные окна**: `p-6 sm:p-8`

**Margin/Gap:**
- **Между блоками**: `space-y-6` или `gap-6`
- **Внутри блока**: `space-y-4` или `gap-4`
- **Мелкие элементы**: `gap-2` или `space-y-2`
- **Теги/Badges**: `gap-1.5` или `gap-2`

**Grid системы:**
- Desktop: `grid-cols-2 gap-4` или `grid-cols-3 gap-6`
- Адаптив: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`

### 📱 МОБИЛЬНАЯ РАЗВЁРТКА (ШИРИНА/ОТСТУПЫ) — ОБЯЗАТЕЛЬНО

**Принцип:** на мобильных экранах страницы кабинета должны быть **максимально широкими**, но с лёгким “воздухом” по краям.

- **Правильный мобильный отступ**: `px-2` (почти edge-to-edge)
- **На `sm+`**: возвращаем “нормальные” отступы/контейнер (`sm:px-4` / `sm:px-6`, `sm:container sm:mx-auto`)
- **Запрещено** для основных страниц: `px-0` (слишком “в край”) и `px-6` на мобиле (слишком узко)
- **Если в layout используется `container mx-auto px-6`**, то для “широких” страниц (например `/notifications`, `/my-requests`) делаем исключение в `app/(dashboard)/layout.tsx`:
  - mobile: `w-full px-2`
  - `sm+`: `sm:container sm:mx-auto sm:px-6`

### ✅ ЧЕК-ЛИСТ АУДИТА UI ПО ДИЗАЙН-КОДУ (прогоняем по страницам)

**Ширина/мобилка**
- На мобильных: контент **почти во всю ширину** (`px-2`), без лишних `max-w-*` и без двойных паддингов (`px-4` внутри страницы + `px-6` в layout).
- Для длинных страниц с нижними табами: `pb-24` (чтобы ничего не перекрывалось).
- Формы: на мобиле `grid-cols-1 sm:grid-cols-2`, не пихаем 2 колонки в 320px.

**Скругления**
- Только: `rounded-[32px]/[28px]/[24px]/[18px]/full`.  
- Важно: верхние sticky-панели делаем с **видимыми округлениями** через обёртку: `sticky ... bg-[#F7F8FA] pt-2` + внутренний `rounded-[24px] mx-2`.

**Цвета**
- Акцент: `orange-500/600/50/700`.
- Не используем `purple-*`, `indigo-*` без спец. причины.

**Кнопки**
- Без “чёрных” primary на клиентских страницах: основное действие — `bg-orange-500`.
- На мобиле не делаем один супер-CTA, если рядом есть другие действия: выравниваем по иерархии (например 2 кнопки одинаковой высоты).

### 🧩 ПАТТЕРНЫ “Яндекс.Лавка” (типографика/зонирование) — ФИКСИРУЕМ

**Контраст типографики (обязательно)**
- Заголовок сущности: `text-2xl sm:text-3xl font-bold text-gray-900`
- Цена/ключевая цифра (если есть): `text-[18px] font-bold text-gray-900` или `text-xl font-bold`
- Подписи/лейблы: `text-[11px] font-semibold text-gray-400 uppercase tracking-wider`
- Вторичный текст: `text-sm text-gray-500/600`

**Зонирование**
- Для “карточки с параметрами” вместо россыпи мелких блоков используем **список-таблицу**:
  - контейнер: `rounded-[24px] border border-gray-100 overflow-hidden divide-y divide-gray-100`
  - строка: `p-4 bg-white flex gap-3`
  - слева маленькая “иконка-капсула”: `w-10 h-10 rounded-full bg-orange-50`
  - сверху лейбл (`text-[11px]...`), ниже значение (`text-[15px] font-semibold`)

**Раскрывашки (accordion)**
- Для длинных “деталей/условий/FAQ” используем `components/ui/accordion.tsx`
- Заголовок accordion: `text-base font-semibold text-gray-900`
- Контент: `text-sm text-gray-700 leading-relaxed`

### 🎭 ТЕНИ (Shadows)

**Системные тени:**
- **Блоки**: `shadow-sm` - мягкая тень для карточек
- **Hover**: `hover:shadow-md` - увеличение тени при наведении
- **Модалки**: `shadow-2xl` - глубокая тень
- **Custom**: `shadow-[0_2px_8px_rgba(0,0,0,0.08)]` - для особых случаев

### 🔄 ПЕРЕХОДЫ (Transitions)

**Стандартные:**
- **Универсальный**: `transition-all` - для большинства интерактивных элементов
- **Цвет**: `transition-colors` - только изменение цвета
- **С настройкой**: `transition-all duration-300 ease-out` - плавный переход

### 📱 АДАПТИВНОСТЬ (Breakpoints)

**Tailwind breakpoints:**
- `sm:` - от 640px (планшеты)
- `md:` - от 768px
- `lg:` - от 1024px (десктоп)
- `xl:` - от 1280px

**Паттерны:**
- Заголовки: всегда с `sm:` для увеличения (`text-xl sm:text-2xl`)
- Grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Padding: `p-4 sm:p-6` или `px-4 sm:px-6`
- Скрытие: `hidden md:block` или `block md:hidden`

### 🎨 ОБЩИЕ ПРИНЦИПЫ

Весь UI должен быть выполнен в стиле Яндекс.Услуг:
- **Скругления**: ТОЛЬКО из системы выше (32/28/24/18/full)
- **Тени**: мягкие `shadow-sm` или `shadow-md` при hover
- **Типографика**: четкая иерархия, всегда с адаптивностью
- **Анимации**: плавные `transition-all` на всех интерактивных элементах
- **Spacing**: щедрые отступы, "воздух" между элементами

### Компоненты UI

При создании интерактивных элементов **ВСЕГДА используй существующие компоненты из `components/ui/`**:

#### Dropdown меню
```tsx
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'

// Использование:
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <button>Открыть меню</button>
  </DropdownMenuTrigger>
  <DropdownMenuContent align="end" className="w-64">
    <DropdownMenuItem>Пункт 1</DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem>Пункт 2</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

#### Select
```tsx
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
```

#### Dialog/Modal
```tsx
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from '@/components/ui/dialog'
```

#### Кнопки
```tsx
import { Button } from '@/components/ui/button'

// Варианты: default (оранжевая), outline, ghost, destructive
<Button variant="default">Основная</Button>
<Button variant="outline">Вторичная</Button>
```

#### Карточки
```tsx
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

<Card className="rounded-[24px] border-none shadow-sm">
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
  <CardContent>Контент</CardContent>
</Card>
```

### Правила стилизации

1. **НЕ создавай самописные dropdown'ы** - используй `DropdownMenu` из `@/components/ui/dropdown-menu`
2. **НЕ используй маленькие скругления** (`rounded-md`, `rounded-lg`) - используй `rounded-[16px]` и больше
3. **НЕ используй стандартные тени** (`shadow-sm`, `shadow-md`) - используй кастомные `shadow-[0_8px_40px_-12px_rgba(0,0,0,0.25)]`
4. **Всегда добавляй плавные переходы** на интерактивные элементы: `transition-all duration-300`
5. **Hover состояния** должны быть мягкими: `hover:bg-gray-50` вместо `hover:bg-gray-100`
6. **Активные элементы** - оранжевые: `bg-orange-50 text-orange-700` или `bg-orange-500 text-white`

### Иконки

Используй `lucide-react` для всех иконок:
```tsx
import { Settings, Check, ChevronRight, X } from 'lucide-react'

<Settings className="w-5 h-5 text-slate-600" />
```

### Цветовая палитра

- **Акцент**: `orange-500` (#F97316)
- **Текст основной**: `gray-900` или `slate-900`
- **Текст вторичный**: `slate-600` или `gray-600`
- **Текст hint**: `slate-500`
- **Фон hover**: `gray-50` или `slate-50`
- **Фон активный**: `orange-50` с текстом `orange-700`
- **Границы**: `border-gray-200` или `border-slate-200`

### Адаптивность

- Используй `md:` и `lg:` брейкпоинты
- Мобильная версия - приоритет (mobile-first)
- На мобильных устройствах используй компактные варианты компонентов
- Скрывай второстепенные элементы на мобильных: `hidden md:block`

### Примеры ХОРОШИХ компонентов в проекте

Изучи эти файлы перед созданием нового UI:
- `components/shared/user-menu.tsx` - идеальный dropdown в стиле Яндекса
- `components/ui/dropdown-menu.tsx` - базовый компонент dropdown
- `components/ui/select.tsx` - селект в стиле Яндекса
- `components/ui/card.tsx` - карточки с правильными скруглениями
- `components/features/profile/templates/about-section-client.tsx` - dropdown для настроек профиля
- `components/features/profile/mobile-profile-nav.tsx` - анимированное меню со sliding indicator

### Анимации меню

Для создания плавных меню с индикаторами используй **Sliding Indicator Pattern**:

```tsx
const [activeItem, setActiveItem] = useState('item1')
const [indicatorStyle, setIndicatorStyle] = useState({ left: 0, width: 0 })
const buttonRefs = useRef<Record<string, HTMLButtonElement | null>>({})

useEffect(() => {
  const activeButton = buttonRefs.current[activeItem]
  if (activeButton) {
    const rect = activeButton.getBoundingClientRect()
    setIndicatorStyle({ left: rect.left, width: rect.width })
  }
}, [activeItem])

return (
  <div className="relative">
    <div 
      className="absolute bg-orange-500 rounded-full transition-all duration-300 ease-out"
      style={{ left: indicatorStyle.left, width: indicatorStyle.width }}
    />
    {/* Кнопки меню */}
  </div>
)
```

**Для сглаживания скролла** используй debounce с `setTimeout`:

```tsx
useEffect(() => {
  let timeoutRef: NodeJS.Timeout | null = null
  
  const handleScroll = () => {
    if (timeoutRef) clearTimeout(timeoutRef)
    timeoutRef = setTimeout(() => {
      updateActiveSection()
    }, 100) // 100ms задержка
  }
  
  window.addEventListener('scroll', handleScroll, { passive: true })
  return () => {
    window.removeEventListener('scroll', handleScroll)
    if (timeoutRef) clearTimeout(timeoutRef)
  }
}, [])
```

**Easing функция для анимаций:**
```tsx
// Cubic ease-out (рекомендуется)
const easeOut = 1 - Math.pow(1 - progress, 3)
```

### ❌ ПЛОХО (не делай так)

```tsx
// Самописный dropdown
<div className="absolute bg-white rounded-xl shadow-lg">
  <button className="hover:bg-gray-100">Item</button>
</div>

// Маленькие скругления
<Card className="rounded-lg">

// Стандартные тени
<div className="shadow-md">

// Резкие переходы
<button className="hover:bg-blue-500">
```

### ✅ ХОРОШО (делай так)

```tsx
// Правильный dropdown из UI библиотеки
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <button className="rounded-full">Menu</button>
  </DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuItem>Item</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>

// Большие скругления
<Card className="rounded-[24px]">

// Мягкие тени
<div className="shadow-[0_8px_40px_-12px_rgba(0,0,0,0.25)]">

// Плавные переходы
<button className="transition-all duration-300 hover:bg-orange-500">
```

