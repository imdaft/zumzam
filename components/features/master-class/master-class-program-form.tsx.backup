'use client'

import { useEffect, useMemo, useRef, useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'
import { Checkbox } from '@/components/ui/checkbox'
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Loader2, Camera, X, CheckCircle2, ArrowLeft, Plus, Trash } from 'lucide-react'
import Image from 'next/image'
import { AIFieldAssistantWrapper } from '@/components/features/ai/ai-field-assistant'
import { ImageCropper } from '@/components/shared/image-cropper'
import { getCroppedImg } from '@/lib/canvasUtils'
import { toast } from 'sonner'
import {
  MASTER_CLASS_CATEGORIES,
  MASTER_CLASS_DURATIONS,
  MASTER_CLASS_AGE_RANGES,
  type MasterClassCategory,
  type MasterClassAgeRange,
} from '@/lib/constants/master-class-constants'

const formSchema = z.object({
  name: z.string().min(2, 'Минимум 2 символа').max(255),
  description: z.string().optional(),
  photos: z.array(z.string()).default([]),
  video_url: z.string().url('Неверный формат URL').optional().or(z.literal('')),
  categories: z.array(z.string()).min(1, 'Выберите хотя бы одну категорию'),
  duration: z.number().min(1, 'Укажите длительность').nullable(),
  price: z.number().min(0, 'Цена не может быть отрицательной').nullable(),
  age_ranges: z.array(z.string()).default([]),
  min_participants: z.number().min(1, 'Минимум 1 участник').nullable(),
  max_participants: z.number().min(1, 'Минимум 1 участник').nullable(),
  materials_included: z.boolean().default(false),
  materials_list: z.array(z.string()).default([]),
  take_home: z.boolean().default(false),
  certificate: z.boolean().default(false),
})

type FormValues = z.infer<typeof formSchema>

interface MasterClassProgramFormProps {
  profileId: string
  initialData?: any
  isEditMode?: boolean
  onSuccess?: () => void
  onCancel?: () => void
}

export function MasterClassProgramForm({
  profileId,
  initialData,
  isEditMode = false,
  onSuccess,
  onCancel,
}: MasterClassProgramFormProps) {
  const [isLoading, setIsLoading] = useState(false)
  const [uploading, setUploading] = useState(false)
  const [isPhotoCropperOpen, setIsPhotoCropperOpen] = useState(false)
  const [tempPhotoSrc, setTempPhotoSrc] = useState<string | null>(null)
  const tempPhotoObjectUrlRef = useRef<string | null>(null)
  const [pendingFiles, setPendingFiles] = useState<File[]>([])
  const activeFileRef = useRef<File | null>(null)
  const [profileMeta, setProfileMeta] = useState<{ display_name: string; category: string } | null>(null)
  const [newMaterial, setNewMaterial] = useState('')
  useEffect(() => {
    return () => {
      if (tempPhotoObjectUrlRef.current) {
        URL.revokeObjectURL(tempPhotoObjectUrlRef.current)
        tempPhotoObjectUrlRef.current = null
      }
    }
  }, [])

  useEffect(() => {
    let isMounted = true
    const loadProfileMeta = async () => {
      try {
        const response = await fetch(`/api/profiles/${profileId}`)
        if (!response.ok) throw new Error('Failed to load profile')
        
        const data = await response.json()
        if (isMounted && data) {
          setProfileMeta({
            display_name: data.display_name || '',
            category: data.category || 'master_class',
          })
        }
      } catch (e) {
        console.warn('[MasterClassForm] Failed to load profile meta:', e)
      }
    }
    loadProfileMeta()
    return () => {
      isMounted = false
    }
  }, [profileId]) // eslint-disable-line react-hooks/exhaustive-deps

  const defaultValues = useMemo<FormValues>(() => {
    // #region agent log
    fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:122',message:'Computing defaultValues',data:{hasInitialData:!!initialData,initialDataSample:initialData?{name:initialData.name,photo:initialData.photo,duration_minutes:initialData.duration_minutes,age_min:initialData.age_min,age_max:initialData.age_max}:null},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'A'})}).catch(()=>{});
    // #endregion
    
    if (!initialData) {
      return {
        name: '',
        description: '',
        photos: [],
        video_url: '',
        categories: [],
        duration: null,
        price: null,
        age_ranges: [],
        min_participants: null,
        max_participants: null,
        materials_included: false,
        materials_list: [],
        take_home: false,
        certificate: false,
      }
    }

    // Transform database schema to form schema
    // DB has: photo (singular), duration_minutes, age_min, age_max
    // Form expects: photos[], duration, age_ranges[]
    
    const photos = initialData.photo ? [initialData.photo] : []
    const duration = initialData.duration_minutes || initialData.duration || null
    
    // Reconstruct age_ranges from age_min/age_max
    const age_ranges: string[] = []
    if (initialData.age_min !== undefined && initialData.age_max !== undefined) {
      const min = initialData.age_min
      const max = initialData.age_max
      
      // Map age ranges back to form options
      if (max >= 99 || max >= 18) age_ranges.push('adults')
      if (min <= 16 && max >= 12) age_ranges.push('12-16')
      if (min <= 12 && max >= 8) age_ranges.push('8-12')
      if (min <= 8 && max >= 5) age_ranges.push('5-8')
    }

    const result = {
      name: initialData.name || '',
      description: initialData.description || '',
      photos: photos,
      video_url: initialData.video_url || '',
      categories: Array.isArray(initialData.categories) ? initialData.categories : [],
      duration: duration,
      price: initialData.price ? Number(initialData.price) : null,
      age_ranges: age_ranges.length > 0 ? age_ranges : (Array.isArray(initialData.age_ranges) ? initialData.age_ranges : []),
      min_participants: initialData.min_participants || null,
      max_participants: initialData.max_participants || null,
      materials_included: initialData.materials_included || false,
      materials_list: Array.isArray(initialData.materials_list) ? initialData.materials_list : [],
      take_home: initialData.take_home || false,
      certificate: initialData.certificate || false,
    }
    
    // #region agent log
    fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:175',message:'defaultValues computed',data:{photos:result.photos,duration:result.duration,age_ranges:result.age_ranges},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'A'})}).catch(()=>{});
    // #endregion
    
    return result
  }, [initialData])

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues,
  })

  // Reset form when initialData changes (important for edit mode)
  useEffect(() => {
    form.reset(defaultValues)
  }, [defaultValues, form])

  const onSubmit = async (values: FormValues) => {
    setIsLoading(true)
    // #region agent log
    fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:195',message:'onSubmit ENTRY',data:{isEditMode,valuesSample:{name:values.name,photos:values.photos,duration:values.duration,age_ranges:values.age_ranges}},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'B'})}).catch(()=>{});
    // #endregion
    try {
      const url = isEditMode
        ? `/api/master-class-programs/${initialData.id}`
        : '/api/master-class-programs'
      const method = isEditMode ? 'PATCH' : 'POST'

      // Transform form data to match database schema
      // Database has: duration_minutes, age_min, age_max, photo (singular), active
      // Form has: duration, age_ranges, photos (plural), categories, etc.
      
      // Extract age range bounds (use first and last from selected ranges)
      const ageRanges = values.age_ranges || []
      let age_min: number | null = null
      let age_max: number | null = null
      
      if (ageRanges.length > 0) {
        const allAges: number[] = []
        ageRanges.forEach(range => {
          if (range === 'adults') {
            allAges.push(18, 99)
          } else {
            const [min, max] = range.split('-').map(Number)
            if (min) allAges.push(min)
            if (max) allAges.push(max)
          }
        })
        if (allAges.length > 0) {
          age_min = Math.min(...allAges)
          age_max = Math.max(...allAges)
        }
      }

      const body = {
        profile_id: profileId,
        name: values.name,
        description: values.description || null,
        duration_minutes: values.duration || null,
        price: values.price || null,
        age_min,
        age_max,
        photo: values.photos?.[0] || null,
        video_url: values.video_url || null,
        categories: values.categories || [],
        min_participants: values.min_participants || null,
        max_participants: values.max_participants || null,
        materials_included: values.materials_included || false,
        materials_list: values.materials_list || [],
        take_home: values.take_home || false,
        certificate: values.certificate || false,
      }

      // #region agent log
      fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:243',message:'BEFORE fetch',data:{url,method,body},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'C'})}).catch(()=>{});
      // #endregion

      const res = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      })

      // #region agent log
      fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:255',message:'AFTER fetch',data:{responseOk:res.ok,responseStatus:res.status},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'C,D'})}).catch(()=>{});
      // #endregion

      if (!res.ok) {
        const errorData = await res.json()
        // #region agent log
        fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:262',message:'Response not OK',data:{errorData},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'D'})}).catch(()=>{});
        // #endregion
        throw new Error(errorData.error || 'Failed to save master class')
      }

      toast.success(isEditMode ? 'Мастер-класс обновлен' : 'Мастер-класс добавлен')
      if (onSuccess) {
        onSuccess()
      }
    } catch (error) {
      // #region agent log
      fetch('http://127.0.0.1:7242/ingest/2f44af11-d8bf-40e4-ab90-d93dad5b63df',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({location:'master-class-program-form.tsx:274',message:'CATCH block',data:{errorMessage:(error as any)?.message,errorName:(error as any)?.name},timestamp:Date.now(),sessionId:'debug-session',runId:'run1',hypothesisId:'E'})}).catch(()=>{});
      // #endregion
      console.error('Error saving master class:', error)
      toast.error(`Ошибка: ${error instanceof Error ? error.message : 'Не удалось сохранить мастер-класс'}`)
    } finally {
      setIsLoading(false)
    }
  }

  const startNextCrop = (filesQueue: File[]) => {
    const next = filesQueue[0]
    if (!next) {
      activeFileRef.current = null
      setTempPhotoSrc(null)
      setIsPhotoCropperOpen(false)
      setUploading(false)
      return
    }

    activeFileRef.current = next
    if (tempPhotoObjectUrlRef.current) {
      URL.revokeObjectURL(tempPhotoObjectUrlRef.current)
      tempPhotoObjectUrlRef.current = null
    }
    const url = URL.createObjectURL(next)
    tempPhotoObjectUrlRef.current = url
    setTempPhotoSrc(url)
    setIsPhotoCropperOpen(true)
  }

  const handlePhotoUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const files = e.target.files
    if (!files || files.length === 0) return

    const selected = Array.from(files)
    const okTypes = new Set(['image/jpeg', 'image/png', 'image/webp'])
    const filtered: File[] = []
    for (const f of selected) {
      if (f.size > 10 * 1024 * 1024) {
        toast.error(`Файл слишком большой: ${f.name} (макс. 10 МБ)`)
        continue
      }
      if (f.type && !okTypes.has(f.type)) {
        toast.error(`Неподдерживаемый формат: ${f.name}`)
        continue
      }
      filtered.push(f)
    }
    if (filtered.length === 0) {
      e.target.value = ''
      return
    }

    setUploading(true)
    setPendingFiles(filtered)
    startNextCrop(filtered)
    e.target.value = ''
  }

  const handleCancelCrop = () => {
    setIsPhotoCropperOpen(false)
    setPendingFiles((prev) => {
      const rest = prev.slice(1)
      queueMicrotask(() => startNextCrop(rest))
      return rest
    })
  }

  const handlePhotoCropComplete = async (
    cropParams: any,
    _unused: any,
    _originalBlob: Blob,
    _isNewUpload: boolean
  ) => {
    const file = activeFileRef.current
    if (!file || !tempPhotoSrc) {
      handleCancelCrop()
      return
    }

    try {
      const blob = await getCroppedImg(tempPhotoSrc, cropParams?.croppedAreaPixels, 0, undefined, 1)
      if (!blob) {
        throw new Error('Не удалось обрезать изображение')
      }

      const croppedFile = new File([blob], `mc-${Date.now()}.jpg`, { type: 'image/jpeg' })

      const formData = new FormData()
      formData.append('file', croppedFile)
      formData.append('bucket', 'portfolio')

      const response = await fetch('/api/upload', {
        method: 'POST',
        body: formData,
      })

      if (!response.ok) {
        const errorData = await response.json().catch(() => ({}))
        throw new Error(errorData.error || 'Ошибка загрузки файла')
      }

      const { url } = await response.json()
      const currentPhotos = form.getValues('photos') || []
      form.setValue('photos', [...currentPhotos, url], { shouldDirty: true })

      setPendingFiles((prev) => {
        const rest = prev.slice(1)
        queueMicrotask(() => startNextCrop(rest))
        return rest
      })
    } catch (error) {
      console.warn('Upload/crop error:', error)
      toast.error(`Не удалось загрузить фото: ${error instanceof Error ? error.message : 'Неизвестная ошибка'}`)
      setPendingFiles((prev) => {
        const rest = prev.slice(1)
        queueMicrotask(() => startNextCrop(rest))
        return rest
      })
    }
  }

  const removePhoto = (url: string) => {
    const currentPhotos = form.getValues('photos') || []
    form.setValue(
      'photos',
      currentPhotos.filter((p) => p !== url)
    )
  }

  const addMaterial = () => {
    if (!newMaterial.trim()) return
    const current = form.getValues('materials_list') || []
    form.setValue('materials_list', [...current, newMaterial.trim()])
    setNewMaterial('')
  }

  const removeMaterial = (index: number) => {
    const current = form.getValues('materials_list') || []
    form.setValue('materials_list', current.filter((_, i) => i !== index))
  }

  const mcName = form.watch('name') || ''

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-8 max-w-3xl mx-auto pb-20">
        <div className="flex items-center gap-4 mb-6">
          <Button type="button" variant="ghost" onClick={onCancel} className="rounded-full">
            <ArrowLeft className="h-4 w-4 mr-2" />
            Назад
          </Button>
          <h1 className="text-2xl font-bold text-slate-900">
            {isEditMode ? 'Редактировать мастер-класс' : 'Добавить мастер-класс'}
          </h1>
        </div>

        <Card className="border-none shadow-sm rounded-[32px]">
          <CardHeader>
            <CardTitle className="text-xl font-bold">Основная информация</CardTitle>
          </CardHeader>
          <CardContent className="space-y-6">
            <FormField
              control={form.control}
              name="name"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-base font-semibold">Название мастер-класса *</FormLabel>
                  <FormControl>
                    <Input
                      placeholder="Например: Роспись пряников, Лепка из полимерной глины"
                      className="h-12 rounded-xl"
                      {...field}
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            <FormField
              control={form.control}
              name="description"
              render={({ field }) => (
                <FormItem>
                  <AIFieldAssistantWrapper
                    type="long"
                    currentText={field.value || ''}
                    profileName={(profileMeta?.display_name || mcName || 'Мастер-класс').trim()}
                    category={profileMeta?.category || 'master_class'}
                    subtype={undefined}
                    existingData={{
                      longDescription: initialData?.description || '',
                    }}
                    onGenerated={(text) => field.onChange(text)}
                  >
                    <FormLabel className="text-base font-semibold">Описание процесса</FormLabel>
                    <FormControl>
                      <Textarea
                        placeholder="Расскажите, что будут делать участники, чему научатся..."
                        className="min-h-[120px] rounded-[18px]"
                        {...field}
                      />
                    </FormControl>
                    <FormDescription>
                      Опишите, что будет происходить во время мастер-класса и чему научатся участники
                    </FormDescription>
                    <FormMessage />
                  </AIFieldAssistantWrapper>
                </FormItem>
              )}
            />
          </CardContent>
        </Card>

        <Card className="border-none shadow-sm rounded-[32px]">
          <CardHeader>
            <CardTitle className="text-xl font-bold">Фотографии готовых работ</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <input
              type="file"
              id="mc-photo-upload"
              accept="image/png,image/jpeg,image/webp"
              multiple
              onChange={handlePhotoUpload}
              disabled={uploading}
              className="hidden"
            />
            <Button
              type="button"
              variant="outline"
              onClick={() => document.getElementById('mc-photo-upload')?.click()}
              disabled={uploading}
              className="rounded-xl"
            >
              {uploading ? (
                <><Loader2 className="mr-2 h-4 w-4 animate-spin" /> Загрузка...</>
              ) : (
                <><Camera className="mr-2 h-4 w-4" /> Загрузить фото</>
              )}
            </Button>

            {form.watch('photos')?.length > 0 && (
              <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
                {form.watch('photos').map((url, index) => (
                  <div key={url} className="relative group aspect-square rounded-[24px] overflow-hidden border bg-slate-100">
                    <Image src={url} alt={`Photo ${index + 1}`} fill className="object-cover" />
                    <button
                      type="button"
                      onClick={() => removePhoto(url)}
                      className="absolute top-2 right-2 rounded-full bg-red-500 p-1 text-white opacity-0 group-hover:opacity-100 transition-opacity"
                    >
                      <X className="h-4 w-4" />
                    </button>
                  </div>
                ))}
              </div>
            )}
          </CardContent>
        </Card>

        <ImageCropper
          imageSrc={tempPhotoSrc}
          isOpen={isPhotoCropperOpen}
          onClose={handleCancelCrop}
          onCropComplete={handlePhotoCropComplete}
          title="Обрезка фото"
          singleCropMode="mobile"
          isNewUpload
        />

        <Card className="border-none shadow-sm rounded-[32px]">
          <CardHeader>
            <CardTitle className="text-xl font-bold">Видео (опционально)</CardTitle>
          </CardHeader>
          <CardContent>
            <FormField
              control={form.control}
              name="video_url"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Ссылка на видео</FormLabel>
                  <FormControl>
                    <Input
                      placeholder="https://youtube.com/watch?v=..."
                      className="h-12 rounded-xl"
                      {...field}
                    />
                  </FormControl>
                  <FormDescription>
                    Добавьте видео с процесса или готовых работ
                  </FormDescription>
                  <FormMessage />
                </FormItem>
              )}
            />
          </CardContent>
        </Card>

        <Card className="border-none shadow-sm rounded-[32px]">
          <CardHeader>
            <CardTitle className="text-xl font-bold">Параметры мастер-класса</CardTitle>
          </CardHeader>
          <CardContent className="space-y-6">
            <FormField
              control={form.control}
              name="categories"
              render={() => (
                <FormItem>
                  <FormLabel className="text-base font-semibold">Категория * (можно выбрать несколько)</FormLabel>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mt-3">
                    {Object.entries(MASTER_CLASS_CATEGORIES).map(([key, label]) => (
                      <FormField
                        key={key}
                        control={form.control}
                        name="categories"
                        render={({ field }) => (
                          <FormItem className="flex flex-row items-center space-x-3 space-y-0 rounded-xl border border-slate-200 p-4 bg-slate-50/50">
                            <FormControl>
                              <Checkbox
                                checked={field.value?.includes(key)}
                                onCheckedChange={(checked) => {
                                  const current = field.value || []
                                  const updated = checked
                                    ? [...current, key]
                                    : current.filter((val) => val !== key)
                                  field.onChange(updated)
                                }}
                              />
                            </FormControl>
                            <FormLabel className="text-sm font-semibold cursor-pointer">
                              {label}
                            </FormLabel>
                          </FormItem>
                        )}
                      />
                    ))}
                  </div>
                  <FormMessage />
                </FormItem>
              )}
            />

            <FormField
              control={form.control}
              name="duration"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-base font-semibold">Длительность *</FormLabel>
                  <Select
                    onValueChange={(val) => field.onChange(parseInt(val))}
                    value={field.value?.toString()}
                  >
                    <FormControl>
                      <SelectTrigger className="h-12 rounded-[18px]">
                        <SelectValue placeholder="Выберите длительность" />
                      </SelectTrigger>
                    </FormControl>
                    <SelectContent>
                      {MASTER_CLASS_DURATIONS.map((d) => (
                        <SelectItem key={d.value} value={d.value.toString()}>
                          {d.label}
                        </SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                  <FormMessage />
                </FormItem>
              )}
            />

            <FormField
              control={form.control}
              name="price"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-base font-semibold">Цена за человека ₽</FormLabel>
                  <FormControl>
                    <Input
                      type="number"
                      placeholder="1500"
                      className="h-12 rounded-[18px]"
                      {...field}
                      value={field.value ?? ''}
                      onChange={(e) => field.onChange(e.target.value ? Number(e.target.value) : null)}
                    />
                  </FormControl>
                  <FormDescription className="text-xs text-slate-500">
                    Укажите стоимость мастер-класса на 1 человека
                  </FormDescription>
                  <FormMessage />
                </FormItem>
              )}
            />

            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              <FormField
                control={form.control}
                name="min_participants"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="text-base font-semibold">Минимум участников *</FormLabel>
                    <FormControl>
                      <Input
                        type="number"
                        min={1}
                        placeholder="1"
                        className="h-12 rounded-xl"
                        value={field.value || ''}
                        onChange={(e) => field.onChange(e.target.value ? parseInt(e.target.value) : null)}
                      />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <FormField
                control={form.control}
                name="max_participants"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="text-base font-semibold">Максимум участников *</FormLabel>
                    <FormControl>
                      <Input
                        type="number"
                        min={1}
                        placeholder="30"
                        className="h-12 rounded-xl"
                        value={field.value || ''}
                        onChange={(e) => field.onChange(e.target.value ? parseInt(e.target.value) : null)}
                      />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />
            </div>

            <FormField
              control={form.control}
              name="age_ranges"
              render={() => (
                <FormItem>
                  <FormLabel className="text-base font-semibold">Возрастная категория (можно выбрать несколько)</FormLabel>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mt-3">
                    {Object.entries(MASTER_CLASS_AGE_RANGES).map(([key, label]) => (
                      <FormField
                        key={key}
                        control={form.control}
                        name="age_ranges"
                        render={({ field }) => (
                          <FormItem className="flex flex-row items-center space-x-3 space-y-0 rounded-xl border border-slate-200 p-4 bg-slate-50/50">
                            <FormControl>
                              <Checkbox
                                checked={field.value?.includes(key)}
                                onCheckedChange={(checked) => {
                                  const current = field.value || []
                                  const updated = checked
                                    ? [...current, key]
                                    : current.filter((val) => val !== key)
                                  field.onChange(updated)
                                }}
                              />
                            </FormControl>
                            <FormLabel className="text-sm font-semibold cursor-pointer">
                              {label}
                            </FormLabel>
                          </FormItem>
                        )}
                      />
                    ))}
                  </div>
                  <FormMessage />
                </FormItem>
              )}
            />
          </CardContent>
        </Card>

        <Card className="border-none shadow-sm rounded-[32px]">
          <CardHeader>
            <CardTitle className="text-xl font-bold">Что включено</CardTitle>
          </CardHeader>
          <CardContent className="space-y-6">
            <FormField
              control={form.control}
              name="materials_included"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center space-x-3 space-y-0 rounded-xl border border-slate-200 p-4 bg-slate-50/50">
                  <FormControl>
                    <Checkbox
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                  <FormLabel className="text-sm font-semibold cursor-pointer">
                    Материалы включены
                  </FormLabel>
                </FormItem>
              )}
            />

            <div>
              <FormLabel className="text-base font-semibold">Список материалов</FormLabel>
              <div className="flex gap-2 mt-2">
                <Input
                  value={newMaterial}
                  onChange={(e) => setNewMaterial(e.target.value)}
                  placeholder="Например: акриловые краски"
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') {
                      e.preventDefault()
                      addMaterial()
                    }
                  }}
                  className="h-12 rounded-xl"
                />
                <Button type="button" onClick={addMaterial} variant="outline" className="rounded-xl">
                  <Plus className="w-4 h-4" />
                </Button>
              </div>
              {form.watch('materials_list')?.length > 0 && (
                <div className="mt-3 space-y-2">
                  {form.watch('materials_list').map((material, index) => (
                    <div key={index} className="flex items-center justify-between p-3 rounded-xl bg-slate-50 border">
                      <span className="text-sm">{material}</span>
                      <Button
                        type="button"
                        variant="ghost"
                        size="sm"
                        onClick={() => removeMaterial(index)}
                        className="text-red-600"
                      >
                        <Trash className="w-4 h-4" />
                      </Button>
                    </div>
                  ))}
                </div>
              )}
            </div>

            <FormField
              control={form.control}
              name="take_home"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center space-x-3 space-y-0 rounded-xl border border-slate-200 p-4 bg-slate-50/50">
                  <FormControl>
                    <Checkbox
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                  <FormLabel className="text-sm font-semibold cursor-pointer">
                    Готовая поделка с собой
                  </FormLabel>
                </FormItem>
              )}
            />

            <FormField
              control={form.control}
              name="certificate"
              render={({ field }) => (
                <FormItem className="flex flex-row items-center space-x-3 space-y-0 rounded-xl border border-slate-200 p-4 bg-slate-50/50">
                  <FormControl>
                    <Checkbox
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                  </FormControl>
                  <FormLabel className="text-sm font-semibold cursor-pointer">
                    Сертификат участника
                  </FormLabel>
                </FormItem>
              )}
            />
          </CardContent>
        </Card>

        <div className="flex items-center justify-end gap-4 pt-6 border-t">
          <Button type="button" variant="outline" onClick={onCancel} disabled={isLoading} className="rounded-xl">
            Отмена
          </Button>
          <Button
            type="submit"
            disabled={isLoading}
            className="bg-primary hover:bg-primary/90 text-white rounded-full shadow-md shadow-orange-200"
          >
            {isLoading ? (
              <><Loader2 className="mr-2 h-4 w-4 animate-spin" /> Сохранение...</>
            ) : (
              <><CheckCircle2 className="mr-2 h-4 w-4" /> {isEditMode ? 'Сохранить изменения' : 'Добавить мастер-класс'}</>
            )}
          </Button>
        </div>
      </form>
    </Form>
  )
}









import { useEffect, useMemo, useRef, useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'
import { Checkbox } from '@/components/ui/checkbox'
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Loader2, Camera, X, CheckCircle2, ArrowLeft, Plus, Trash } from 'lucide-react'
import Image from 'next/image'
import { AIFieldAssistantWrapper } from '@/components/features/ai/ai-field-assistant'
import { ImageCropper } from '@/components/shared/image-cropper'
import { getCroppedImg } from '@/lib/canvasUtils'
import { toast } from 'sonner'
import {
  MASTER_CLASS_CATEGORIES,
  MASTER_CLASS_DURATIONS,
  MASTER_CLASS_AGE_RANGES,
  type MasterClassCategory,
  type MasterClassAgeRange,
} from '@/lib/constants/master-class-constants'

const formSchema = z.object({
  name: z.string().min(2, 'Минимум 2 символа').max(255),
  description: z.string().optional(),
  photos: z.array(z.string()).default([]),
  video_url: z.string().url('Неверный формат URL').optional().or(z.literal('')),
  categories: z.array(z.string()).min(1, 'Выберите хотя бы одну категорию'),
  duration: z.number().min(1, 'Укажите длительность').nullable(),
  price: z.number().min(0, 'Цена не может быть отрицательной').nullable(),
  age_ranges: z.array(z.string()).default([]),
  min_participants: z.number().min(1, 'Минимум 1 участник').nullable(),
  max_participants: z.number().min(1, 'Минимум 1 участник').nullable(),
  materials_included: z.boolean().default(false),
  materials_list: z.array(z.string()).default([]),
  take_home: z.boolean().default(false),
  certificate: z.boolean().default(false),
})

