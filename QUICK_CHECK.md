# ⚡ БЫСТРАЯ ПРОВЕРКА ПОСЛЕ МИГРАЦИИ

## 1. Запустите проверку системы

```bash
npm run check-system
```

**Ожидаемый результат:**
```
🔍 Проверка системы после миграции на Prisma

══════════════════════════════════════════════════

✅ Подключение к БД
   PostgreSQL через Prisma

✅ Основные таблицы
   - users: 45 записей
   - profiles: 142 записей
   - services: 89 записей
   - reviews: 234 записей
   - profile_activities: 156 записей
   - profile_services: 203 записей

✅ Каталоги
   - activity_catalog: 15 записей
   - service_catalog: 12 записей
   - animator_services_catalog: 8 записей
   - show_types_catalog: 6 записей

✅ Целостность данных
   Все связи в порядке

✅ Производительность
   Среднее время: 45ms
   - profiles.findMany(10): 23ms
   - profiles + relations: 67ms
   - profiles.count(): 12ms

══════════════════════════════════════════════════

✅ Все проверки пройдены успешно!

💡 Система работает корректно после миграции на Prisma
```

---

## 2. Проверьте Health Check

```bash
curl http://localhost:4000/api/health
```

**Ожидаемый результат:**
```json
{
  "status": "healthy",
  "checks": {
    "database": { "status": "ok" },
    "data": { "status": "ok" }
  }
}
```

---

## 3. Откройте Dashboard

**URL:** http://localhost:4000/monitoring

(Нужны права администратора)

**Что проверить:**
- ✅ Health Status - зелёный
- ✅ Нет критических ошибок
- ✅ Производительность в норме (<1000ms для БД, <2000ms для API)

---

## 4. Проверьте основные страницы

Откройте в браузере и убедитесь что загружаются:

- ✅ http://localhost:4000/ - главная
- ✅ http://localhost:4000/profiles - список профилей
- ✅ http://localhost:4000/profile/[любой-slug] - профиль
- ✅ http://localhost:4000/settings - настройки (авторизованный)
- ✅ http://localhost:4000/favorites - избранное (авторизованный)

**Если страница не загружается:**
1. Откройте консоль браузера (F12)
2. Посмотрите ошибки
3. Откройте `/monitoring` - там будет детальная информация

---

## 5. Проверьте логи в консоли

Запустите dev сервер:
```bash
npm run dev
```

**В консоли должны быть:**
```
ℹ️ [MonitoringProvider] ✅ Система мониторинга инициализирована
ℹ️ [ErrorTracker] ✅ Глобальные обработчики ошибок установлены
```

**При запросах к БД:**
```
🔍 [Prisma] profiles.findMany (23ms)
🔍 [Prisma] users.findUnique (12ms)
```

**При медленных запросах:**
```
⚠️ [Prisma] 🐌 Медленный запрос: services.findMany (1247ms)
```

**При ошибках:**
```
❌ [Prisma] Ошибка в users.update
❌ [API] 500 ошибка: POST /api/profiles
```

---

## ❌ ЧТО ДЕЛАТЬ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте `.env` файл
2. Убедитесь что `DATABASE_URL` правильный
3. Проверьте доступность Supabase

### Проблема: "Table does not exist"

**Решение:**
1. Запустите миграции:
   ```bash
   npx prisma migrate deploy
   ```
2. Или сгенерируйте Prisma Client:
   ```bash
   npx prisma generate
   ```

### Проблема: "Empty tables"

**Решение:**
1. Импортируйте данные:
   ```bash
   node scripts/import-rest-data.mjs
   ```
2. Проверьте снова:
   ```bash
   npm run check-system
   ```

### Проблема: Страницы не загружаются

**Решение:**
1. Откройте `/monitoring`
2. Посмотрите "Последние ошибки"
3. Проверьте консоль браузера (F12)
4. Запустите полную диагностику:
   ```bash
   curl http://localhost:4000/api/monitoring/diagnostics?type=full
   ```

---

## ✅ ЕСЛИ ВСЁ ЗЕЛЁНОЕ

**Поздравляю! Миграция завершена успешно! 🎉**

Теперь:
- ✅ Все запросы идут через Prisma
- ✅ Supabase используется только для Storage и Auth
- ✅ Система полностью мониторится
- ✅ Все ошибки отслеживаются

**Можно работать дальше!** 🚀

---

**Полная документация:** `MONITORING_GUIDE.md`

