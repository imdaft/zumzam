# Настройка Realtime и Push-уведомлений

## ✅ Что уже готово:

- ✅ База данных обновлена через `prisma db push`
- ✅ Добавлены таблицы: `notifications`, `push_subscriptions`
- ✅ Добавлено поле `sender_role` в таблицу `messages`
- ✅ Prisma Client перегенерирован
- ✅ Сервер запущен и работает

---

## 🔑 ОСТАЛОСЬ: Добавить VAPID ключи

### 1. Откройте или создайте `.env.local` в корне проекта

### 2. Добавьте эти переменные:

```env
# VAPID keys для Web Push (сгенерированы ранее)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="BOF54wqe8E6uFYBe7qqLWySnuHhhJRMVm0FpJ_BOm4hHjcIonGoRvUJsJt9NWQ2qIFuxR_iNjI_xNDmnfdp1ybs"
VAPID_PRIVATE_KEY="XhebY4nTiA6ziS68P4Iojuz_9nlhPtdzLweD3wfNgrw"
VAPID_EMAIL="admin@zumzam.ru"
```

### 3. Перезапустите сервер:

```bash
# Остановите сервер (Ctrl+C)
# Затем:
npm run dev
```

---

## 🧪 Как проверить, что всё работает:

### 1. **Откройте сайт**
```
http://localhost:4000
```

### 2. **Войдите в систему**

### 3. **Проверьте промпт Push-уведомлений**
- Через 5 секунд после входа должен появиться промпт: "Включить уведомления?"
- Нажмите "Включить"
- Браузер запросит разрешение

### 4. **Проверьте консоль браузера (F12)**
Должны быть логи:
```
[Push] Subscription saved: {...}
[Realtime Notifications] SSE connection opened.
```

### 5. **Проверьте Network (DevTools → Network)**
- Фильтр: `eventsource`
- Должно быть активное соединение к `/api/realtime/notifications`
- Status: `200` (pending)
- Type: `eventsource`

---

## 🎯 Что теперь работает:

✅ **Realtime обновления** - счётчики уведомлений обновляются мгновенно через SSE  
✅ **Push-уведомления** - браузерные уведомления даже когда сайт закрыт  
✅ **Таблица notifications** - хранение всех уведомлений пользователей  
✅ **Таблица push_subscriptions** - управление подписками на Push  
✅ **Поле sender_role** - правильная идентификация отправителей в чатах  

---

## 📋 API Endpoints:

- **GET** `/api/realtime/notifications` - SSE поток для realtime обновлений
- **POST** `/api/push/subscribe` - Подписка на Push-уведомления
- **POST** `/api/push/send` - Отправка Push-уведомления
- **GET** `/api/notifications` - Получение списка уведомлений
- **POST** `/api/notifications/[id]/read` - Отметить уведомление прочитанным
- **DELETE** `/api/notifications/[id]` - Удалить уведомление

---

## 🔄 Как работает система:

1. **Создание уведомления:**
   - При создании заказа/сообщения создаётся запись в `notifications`
   - SSE сервис отправляет событие всем подключённым клиентам
   - Если у пользователя есть активная Push-подписка - отправляется браузерное уведомление

2. **Realtime обновления:**
   - Клиент подключается к `/api/realtime/notifications` через EventSource
   - Сервер держит соединение открытым и отправляет события в реальном времени
   - React hook `useRealtimeNotifications` обрабатывает события и обновляет UI

3. **Push-уведомления:**
   - Пользователь даёт разрешение в браузере
   - Браузер генерирует подписку (endpoint, p256dh, auth)
   - Подписка сохраняется в `push_subscriptions`
   - При отправке уведомления используется Web Push API

---

## 🐛 Troubleshooting:

### Промпт не появляется?
- Проверьте, что VAPID ключи добавлены в `.env.local`
- Перезапустите сервер
- Очистите cookies/localStorage

### SSE не подключается?
- Проверьте Network → вкладка `eventsource`
- Проверьте, что `/api/realtime/notifications` возвращает 200
- Проверьте консоль на ошибки

### Push не работает?
- Проверьте, что разрешение дано в браузере (иконка замка в адресной строке)
- Проверьте, что подписка сохранена (есть запись в `push_subscriptions`)
- Push работает только на HTTPS или localhost

---

**Готово! Система уведомлений полностью настроена 🚀**

