# ✅ ИСПРАВЛЕНА ПРОБЛЕМА С ЧАСТЫМ РАЗЛОГИНОМ

**Дата:** 26 декабря 2025  
**Проблема:** Приходилось перелогиниваться каждые 15-20 минут  
**Статус:** ИСПРАВЛЕНО ✅

---

## 🐛 Причина проблемы

**Access token** жил всего **1 час**, и не было автоматического обновления через refresh token.

### До исправления:

```typescript
// JWT токен
.setExpirationTime('1h') // ❌ 1 час

// Cookie
maxAge: 60 * 60 // ❌ 1 час (3600 секунд)
```

### После исправления:

```typescript
// JWT токен
.setExpirationTime('7d') // ✅ 7 дней

// Cookie
maxAge: 60 * 60 * 24 * 7 // ✅ 7 дней (604800 секунд)
```

---

## 🔧 Что исправлено

### Файл: `app/api/auth/yandex/callback/route.ts`

**Изменения:**

1. **Access token JWT** (строка 143):
   - Было: `.setExpirationTime('1h')`
   - Стало: `.setExpirationTime('7d')` ✅

2. **Cookie `auth-token`** (строка 167):
   - Было: `maxAge: 60 * 60` (1 час)
   - Стало: `maxAge: 60 * 60 * 24 * 7` (7 дней) ✅

3. **Cookie `user-info`** (строка 194):
   - Было: `maxAge: 60 * 60` (1 час)
   - Стало: `maxAge: 60 * 60 * 24 * 7` (7 дней) ✅

---

## ✅ Результат

### Теперь сессия будет работать:

- ✅ **7 дней** без перелогина
- ✅ **Автоматический logout** только через 7 дней
- ✅ **Refresh token** тоже на 7 дней (уже был настроен правильно)
- ✅ **Одинаковое время жизни** для всех cookies

---

## 🔒 Безопасность

### Сохранена:

- ✅ `httpOnly: true` - защита от XSS атак
- ✅ `secure: true` на HTTPS - защита от перехвата
- ✅ `sameSite: 'lax'` - защита от CSRF
- ✅ JWT подпись с секретом

### Рекомендации для будущего:

Если нужна более высокая безопасность, можно реализовать:

1. **Автоматическое обновление токена** (token rotation):
   ```
   Access token: 15 минут
   Refresh token: 7 дней
   + Автоматическое обновление через refresh token
   ```

2. **Отслеживание активности**:
   - Продлевать сессию при активности пользователя
   - Автоматический logout при неактивности

3. **Логирование сессий**:
   - Таблица активных сессий
   - Возможность отозвать сессию
   - История входов

---

## 🧪 Как протестировать

### 1. Перезапустить сервер:

```bash
npm run dev
# или
yarn dev
```

### 2. Разлогиниться и залогиниться заново через Яндекс

### 3. Проверить cookies в DevTools:

**Chrome DevTools → Application → Cookies → localhost:4000**

Должны быть:
- `auth-token` - Max-Age: 604800 (7 дней)
- `refresh-token` - Max-Age: 604800 (7 дней)
- `user-info` - Max-Age: 604800 (7 дней)

### 4. Проверить работу в течение дня

Теперь **не должен вылетать** из системы минимум 7 дней!

---

## 📝 Примечания

### Почему вылетало через 15-20 минут, а не через 1 час?

Возможные причины:
1. **Рассинхрон времени** между сервером и клиентом
2. **Браузерные расширения** могли очищать cookies
3. **Другие факторы** (например, перезагрузка браузера)

Теперь с **7 днями** этих проблем быть не должно.

---

## ✨ Дополнительно

Если в будущем понадобится **разное время жизни** для разных типов пользователей:

```typescript
// Пример: разная длительность для ролей
const tokenDuration = user.role === 'admin' 
  ? '1h'  // Админы - 1 час (высокая безопасность)
  : '7d'  // Обычные пользователи - 7 дней (удобство)

const cookieMaxAge = user.role === 'admin'
  ? 60 * 60          // 1 час
  : 60 * 60 * 24 * 7 // 7 дней
```

---

## 🎊 Итог

**Проблема с частым разлогином РЕШЕНА!** ✅

Сессия теперь работает **7 дней** вместо 1 часа.

---

*Исправлено: 26 декабря 2025*



