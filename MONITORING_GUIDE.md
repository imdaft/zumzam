# 🔍 Руководство по мониторингу системы

После завершения миграции с Supabase на Prisma, в систему встроены инструменты для мониторинга и диагностики.

## Быстрая проверка системы

### CLI Скрипт

Запустите из терминала для быстрой проверки:

```bash
node scripts/check-system.mjs
```

**Проверяет:**
- ✅ Подключение к БД через Prisma
- ✅ Наличие основных таблиц и данных
- ✅ Целостность данных (отсутствие "осиротевших" записей)
- ✅ Производительность типичных запросов
- ✅ Наполненность каталогов

**Результат:**
- Зеленые галочки ✅ - всё в порядке
- Желтые предупреждения ⚠️ - есть некритичные проблемы
- Красные крестики ❌ - критические ошибки

---

## Web-интерфейс мониторинга

### 1. Health Check

**Endpoint:** `GET /api/health`

Доступен публично. Возвращает статус системы:

```bash
curl http://localhost:3000/api/health
```

```json
{
  "status": "healthy",
  "timestamp": "2025-12-26T10:00:00.000Z",
  "duration": 45,
  "version": "1.0.0",
  "environment": "development",
  "checks": {
    "database": {
      "status": "ok",
      "message": "Connected to PostgreSQL via Prisma",
      "duration": 23
    },
    "data": {
      "status": "ok",
      "message": "142 profiles in database"
    }
  },
  "memory": {
    "heapUsed": "120 MB",
    "heapTotal": "180 MB",
    "rss": "250 MB"
  }
}
```

### 2. Dashboard мониторинга

**URL:** `/monitoring` (только для администраторов)

**Возможности:**
- 📊 Общий статус системы
- 📈 Статистика Prisma запросов
- 🌐 Статистика API запросов
- 🐌 Медленные запросы (>1000ms)
- ❌ Последние ошибки
- 🔄 Автообновление каждые 5 секунд

**Метрики:**
- Общее количество запросов
- Количество медленных запросов
- Количество ошибок
- Средняя производительность
- Топ медленных моделей/endpoints
- Топ используемых endpoints

### 3. Stats API

**Endpoint:** `GET /api/monitoring/stats` (только для администраторов)

Параметры:
- `type=all` - вся статистика (по умолчанию)
- `type=prisma` - только Prisma
- `type=api` - только API

```bash
curl http://localhost:3000/api/monitoring/stats?type=all \
  -H "Cookie: auth-token=..."
```

**Очистка статистики:**

```bash
curl -X DELETE http://localhost:3000/api/monitoring/stats?type=all \
  -H "Cookie: auth-token=..."
```

### 4. Diagnostics API

**Endpoint:** `GET /api/monitoring/diagnostics` (только для администраторов)

Параметры:
- `type=quick` - быстрая проверка (по умолчанию)
- `type=full` - полная диагностика

**Быстрая проверка:**

```bash
curl http://localhost:3000/api/monitoring/diagnostics?type=quick
```

**Полная диагностика:**

```bash
curl http://localhost:3000/api/monitoring/diagnostics?type=full
```

Полная диагностика включает:
1. ✅ Database Connection
2. ✅ Main Tables
3. ✅ Database Indexes
4. ✅ Data Integrity
5. ✅ Query Performance

---

## Автоматическое логирование

### Prisma Middleware

Все запросы к БД автоматически логируются:

- ✅ Время выполнения каждого запроса
- 🐌 Предупреждение о медленных запросах (>1000ms)
- ❌ Ошибки с полным контекстом
- 📊 Статистика по моделям и операциям

**Пример лога:**

```
[10:23:45] 🔍 [Prisma] profiles.findMany (23ms)
[10:23:46] ⚠️ [Prisma] 🐌 Медленный запрос: services.findMany (1247ms)
[10:23:47] ❌ [Prisma] Ошибка в users.update
```

### API Monitoring Wrapper

Обёртка `withApiMonitoring()` для API routes:

```typescript
import { withApiMonitoring } from '@/lib/monitoring/api-monitor'

export const GET = withApiMonitoring(async (request) => {
  // ваш код
  return NextResponse.json({ data })
}, '/api/profiles')
```

**Логирует:**
- ⏱️ Время ответа API
- 🔢 HTTP статус коды
- 🐌 Медленные запросы (>2000ms)
- ❌ Ошибки 4xx и 5xx

### Error Tracking

Глобальный перехватчик ошибок на клиенте:

```typescript
import { setupGlobalErrorHandlers } from '@/lib/monitoring/error-tracker'

// В корневом layout
useEffect(() => {
  setupGlobalErrorHandlers()
}, [])
```

**Перехватывает:**
- Необработанные promise rejections
- Глобальные JavaScript ошибки
- React компонентные ошибки

---

## Пороговые значения

### Prisma
- ⚠️ **Медленный запрос:** >1000ms
- ❌ **Критично:** >5000ms

### API
- ⚠️ **Медленный запрос:** >2000ms
- ❌ **Критично:** >10000ms

### Memory
- ⚠️ **Предупреждение:** Heap >500MB
- ❌ **Критично:** Heap >1GB

---

## Интеграция с логгером

Используйте централизованный логгер:

```typescript
import { logger } from '@/lib/logger'

// Debug (только в development)
logger.debug('[Component] Loading data', { userId })

// Info
logger.info('[API] Request successful')

// Warning
logger.warn('[Auth] Token expiring soon')

// Error
logger.error('[DB] Query failed', error)
```

**Уровни логирования:**
- `debug` - отладочная информация (только dev)
- `info` - важные события
- `warn` - потенциальные проблемы
- `error` - критические ошибки

---

## Частые проблемы и решения

### 1. Медленные запросы

**Проблема:** Запросы к БД выполняются дольше 1 секунды

**Решение:**
1. Откройте `/monitoring`
2. Проверьте "Самые медленные модели"
3. Добавьте индексы для частых запросов
4. Оптимизируйте relations (используйте `select` вместо `include`)

### 2. Много ошибок 404

**Проблема:** Большое количество 404 ошибок в API

**Решение:**
1. Откройте `/monitoring`
2. Проверьте "Последние ошибки"
3. Проверьте правильность путей в API запросах

### 3. Проблемы с БД подключением

**Проблема:** `DATABASE_URL` неверный или БД недоступна

**Решение:**
1. Запустите `node scripts/check-system.mjs`
2. Проверьте `.env` файл
3. Проверьте доступность Supabase

### 4. Пустые таблицы после миграции

**Проблема:** Таблицы существуют, но данных нет

**Решение:**
1. Запустите импорт данных:
   ```bash
   node scripts/import-rest-data.mjs
   ```
2. Проверьте снова: `node scripts/check-system.mjs`

---

## Рекомендации

### Development

✅ **Всегда включайте:**
- Логирование всех Prisma запросов
- Логирование всех API запросов
- Error tracking

✅ **Регулярно проверяйте:**
- Dashboard `/monitoring`
- Статистику медленных запросов
- Логи ошибок

### Production

✅ **Настройте:**
- Отправку критических ошибок на email/Telegram
- Алерты при падении health check
- Регулярный мониторинг производительности

✅ **Отключите:**
- Debug логирование (автоматически)
- Детальное логирование каждого запроса

---

## Пример workflow

### Утренняя проверка

1. Откройте `/monitoring`
2. Проверьте Health Status (всё зелёное?)
3. Проверьте количество ошибок за ночь
4. Если есть проблемы - запустите полную диагностику

### Перед деплоем

1. Запустите `node scripts/check-system.mjs`
2. Убедитесь, что все проверки прошли ✅
3. Проверьте производительность
4. Очистите статистику для чистого старта

### После деплоя

1. Сразу проверьте `GET /api/health`
2. Откройте `/monitoring` и следите 5-10 минут
3. Проверьте логи на наличие ошибок
4. Запустите `GET /api/monitoring/diagnostics?type=full`

---

## Контакты поддержки

При возникновении проблем:

1. 📊 Проверьте `/monitoring`
2. 🔍 Запустите `node scripts/check-system.mjs`
3. 📋 Соберите логи и статистику
4. 📧 Отправьте отчет разработчикам

---

**Система мониторинга готова! Все проблемы будут видны сразу.** 🚀

