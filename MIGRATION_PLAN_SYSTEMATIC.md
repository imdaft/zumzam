# 🚀 СИСТЕМАТИЧЕСКИЙ ПЛАН ПОЛНОЙ МИГРАЦИИ

**Цель:** Перенести ВСЁ с Supabase (США) на Managed PostgreSQL (Yandex)  
**Подход:** "Делай → Проверяй → Документируй"  
**Время:** 4-6 часов

---

## 📋 ЭТАП 1: ИМПОРТ ВСЕХ ДАННЫХ (30-60 мин)

### 1.1 Экспорт из старого Supabase
- [ ] Запустить скрипт экспорта `scripts/export-from-old-supabase.mjs`
- [ ] Проверить `old_supabase_data.json` (должно быть ~20+ таблиц)
- [ ] Подсчитать записи в каждой таблице

**Проверка:**
```bash
node scripts/export-from-old-supabase.mjs
# Ожидаем: profiles > 40, reviews > 15, users > 2, и т.д.
```

### 1.2 Импорт в Managed PostgreSQL
- [ ] Запустить скрипт импорта для каждой таблицы
- [ ] Проверить FK constraints
- [ ] Подсчитать записи после импорта

**Проверка:**
```sql
-- Для КАЖДОЙ таблицы
SELECT COUNT(*) FROM table_name;
-- Сравнить с экспортом
```

### 1.3 Таблицы для импорта (по приоритету)

**Критичные (основные данные):**
1. ✅ `users` (уже есть: 2)
2. ✅ `profiles` (уже есть: 43)
3. ✅ `reviews` (уже есть: 20)
4. ✅ `profile_locations` (уже есть: 12)
5. ✅ `category_images` (уже есть: 7)
6. ✅ `profile_services` (уже есть: 7)

**Важные (функционал профилей):**
7. ⏳ `master_class_programs` (есть: 1, нужно больше?)
8. ⏳ `show_programs` (пусто: 0)
9. ⏳ `quest_programs` (пусто: 0)
10. ⏳ `animator_characters` (пусто: 0)
11. ⏳ `agency_partners` (пусто: 0)
12. ⏳ `agency_cases` (пусто: 0)
13. ⏳ `profile_activities` (пусто: 0)

**Системные (активность, заявки):**
14. ⏳ `order_requests` (пусто: 0)
15. ⏳ `user_activity` (пусто: 0)

**Реклама:**
16. ✅ `advertisements` (есть: 1)
17. ✅ `ad_campaigns` (есть: 1)
18. ✅ `ad_slots` (есть: 3)
19. ⏳ `ad_bookings` (пусто: 0)
20. ✅ `ad_impressions` (есть: 5)

---

## 📋 ЭТАП 2: МИГРАЦИЯ API (2-3 часа)

### Приоритет 1: КРИТИЧНЫЕ (работа сайта)

#### 2.1 Профили (CRUD)
- [ ] `GET /api/profiles` - список профилей для владельца
- [ ] `POST /api/profiles` - создание профиля
- [ ] `PATCH /api/profiles/[id]` - обновление профиля
- [ ] `DELETE /api/profiles/[id]` - удаление профиля
- [ ] `GET /api/profiles/me` - мой профиль

**Проверка:** Создать → Редактировать → Удалить тестовый профиль

#### 2.2 Отзывы
- [x] `GET /api/reviews` - получение отзывов (уже мигрирован)
- [ ] `POST /api/reviews` - добавление отзыва
- [ ] `PATCH /api/reviews/[id]` - редактирование
- [ ] `DELETE /api/reviews/[id]` - удаление

**Проверка:** Добавить → Редактировать → Удалить тестовый отзыв

#### 2.3 Услуги
- [x] `GET /api/services` - получение услуг (уже мигрирован)
- [ ] `POST /api/services` - добавление услуги
- [ ] `PATCH /api/services/[id]` - редактирование
- [ ] `DELETE /api/services/[id]` - удаление
- [ ] `POST /api/services/reorder` - пересортировка

**Проверка:** Добавить → Редактировать → Удалить → Пересортировать

#### 2.4 Загрузка файлов
- [ ] `POST /api/upload` - загрузка файлов (фото, видео)

**Проверка:** Загрузить фото профиля, фото услуги, видео

#### 2.5 Заявки
- [ ] `GET /api/requests` - список заявок
- [ ] `POST /api/requests` - создание заявки
- [ ] `GET /api/requests/[id]` - детали заявки
- [ ] `POST /api/requests/[id]/respond` - ответ на заявку

**Проверка:** Создать → Посмотреть → Ответить на заявку

#### 2.6 Программы (для разных типов профилей)
- [x] `GET /api/master-class-programs` (уже мигрирован)
- [x] `POST /api/master-class-programs` (уже мигрирован)
- [x] `PATCH /api/master-class-programs/[id]` (уже мигрирован)
- [ ] `GET /api/show-programs`
- [ ] `POST /api/show-programs`
- [ ] `PATCH /api/show-programs/[id]`
- [ ] `GET /api/quest-programs`
- [ ] `POST /api/quest-programs`
- [ ] `PATCH /api/quest-programs/[id]`
- [ ] `GET /api/animator-characters`
- [ ] `POST /api/animator-characters`
- [ ] `PATCH /api/animator-characters/[id]`

**Проверка:** Для КАЖДОГО типа - создать → редактировать → удалить программу

#### 2.7 Партнёры агентств
- [x] `GET /api/agency-partners` (уже мигрирован)
- [ ] `POST /api/agency-partners`
- [ ] `PATCH /api/agency-partners/[id]`
- [ ] `DELETE /api/agency-partners/[id]`

**Проверка:** Создать → Редактировать → Удалить партнёра

#### 2.8 Локации
- [ ] `GET /api/profile-locations`
- [ ] `POST /api/profile-locations`
- [ ] `PATCH /api/profile-locations/[id]`
- [ ] `DELETE /api/profile-locations/[id]`

**Проверка:** Добавить → Редактировать → Удалить локацию

### Приоритет 2: ВАЖНЫЕ (функционал)

#### 2.9 Аналитика
- [ ] `POST /api/analytics/track` - tracking событий
- [ ] `POST /api/analytics/source` - источники трафика
- [ ] `POST /api/analytics/interest` - интересы пользователей

**Проверка:** Отправить тестовые события

#### 2.10 Уведомления
- [ ] `GET /api/notifications` - список уведомлений
- [ ] `POST /api/notifications/[id]/read` - отметить как прочитанное

**Проверка:** Создать уведомление → Прочитать

#### 2.11 Чаты
- [ ] `GET /api/conversations` - список чатов
- [ ] `GET /api/conversations/[id]/messages` - сообщения
- [ ] `POST /api/conversations/[id]/messages` - отправка сообщения

**Проверка:** Создать чат → Отправить сообщение → Прочитать

### Приоритет 3: ДОПОЛНИТЕЛЬНЫЕ (админка, интеграции)

- [ ] `/api/admin/*` - админка (можно позже)
- [ ] `/api/ai/*` - AI функции (можно позже)
- [ ] `/api/payments/*` - платежи (можно позже)
- [ ] `/api/telegram/*` - Telegram (можно позже)

---

## 📋 ЭТАП 3: STORAGE (1-2 часа)

### 3.1 Анализ текущего Storage
- [ ] Подсчитать файлы в Supabase Storage
- [ ] Определить bucket'ы (portfolio, profiles, etc.)
- [ ] Оценить размер данных

### 3.2 Миграция на Yandex Object Storage
- [ ] Создать bucket в Yandex Object Storage
- [ ] Скопировать файлы из Supabase
- [ ] Обновить URL в БД
- [ ] Мигрировать `/api/upload` на новый storage

**Проверка:** Загрузить файл → Открыть URL → Удалить файл

---

## 📋 ЭТАП 4: ФИНАЛЬНАЯ ПРОВЕРКА (30-60 мин)

### 4.1 Функциональное тестирование

**Сценарий 1: Владелец площадки**
- [ ] Регистрация через Yandex
- [ ] Создание профиля "Площадка"
- [ ] Добавление услуг
- [ ] Загрузка фото
- [ ] Добавление локации
- [ ] Публикация профиля
- [ ] Просмотр на главной

**Сценарий 2: Владелец мастер-класса**
- [ ] Создание профиля "Мастер-класс"
- [ ] Добавление программы
- [ ] Загрузка фото программы
- [ ] Публикация
- [ ] Просмотр на главной

**Сценарий 3: Клиент**
- [ ] Поиск профилей на главной
- [ ] Открытие профиля
- [ ] Отправка заявки
- [ ] Получение уведомления

**Сценарий 4: Реклама**
- [ ] Создание кампании
- [ ] Создание объявления
- [ ] Показ на главной
- [ ] Tracking impressions/clicks

### 4.2 Проверка данных
- [ ] Все профили на месте (43)
- [ ] Все отзывы на месте (20)
- [ ] Все локации на месте (12)
- [ ] Все изображения загружаются

### 4.3 Проверка производительности
- [ ] Главная загружается < 2 сек
- [ ] Профиль загружается < 1 сек
- [ ] API отвечает < 500ms

---

## 📋 ЭТАП 5: CLEANUP (30 мин)

### 5.1 Удаление legacy кода
- [ ] Удалить dummy Supabase переменные из `.env.local`
- [ ] Удалить `lib/supabase/client.ts` и `server.ts` (если не используются)
- [ ] Удалить старые backup файлы

### 5.2 Документация
- [ ] Обновить README.md
- [ ] Создать MIGRATION_COMPLETE.md
- [ ] Архивировать все migration scripts

### 5.3 Деплой на production
- [ ] Обновить `.env.local` на сервере
- [ ] Деплой нового кода
- [ ] Проверка на production

---

## ✅ КРИТЕРИИ УСПЕХА

Миграция считается ЗАВЕРШЁННОЙ, если:

1. ✅ Все таблицы заполнены данными (0 пустых критичных таблиц)
2. ✅ Все критичные API мигрированы на Prisma
3. ✅ Storage работает на Yandex Object Storage
4. ✅ Все 4 тестовых сценария проходят без ошибок
5. ✅ Нет dummy Supabase переменных
6. ✅ Производительность в норме

---

## 🔥 НАЧИНАЕМ!

**Время старта:** {{ NOW }}  
**Ожидаемое время завершения:** {{ NOW + 4-6 часов }}

**Этап 1: ИМПОРТ ДАННЫХ → СТАРТ**


**Цель:** Перенести ВСЁ с Supabase (США) на Managed PostgreSQL (Yandex)  
**Подход:** "Делай → Проверяй → Документируй"  
**Время:** 4-6 часов

---

## 📋 ЭТАП 1: ИМПОРТ ВСЕХ ДАННЫХ (30-60 мин)

### 1.1 Экспорт из старого Supabase
- [ ] Запустить скрипт экспорта `scripts/export-from-old-supabase.mjs`
- [ ] Проверить `old_supabase_data.json` (должно быть ~20+ таблиц)
- [ ] Подсчитать записи в каждой таблице

**Проверка:**
```bash
node scripts/export-from-old-supabase.mjs
# Ожидаем: profiles > 40, reviews > 15, users > 2, и т.д.
```

### 1.2 Импорт в Managed PostgreSQL
- [ ] Запустить скрипт импорта для каждой таблицы
- [ ] Проверить FK constraints
- [ ] Подсчитать записи после импорта

**Проверка:**
```sql
-- Для КАЖДОЙ таблицы
SELECT COUNT(*) FROM table_name;
-- Сравнить с экспортом
```

### 1.3 Таблицы для импорта (по приоритету)

**Критичные (основные данные):**
1. ✅ `users` (уже есть: 2)
2. ✅ `profiles` (уже есть: 43)
3. ✅ `reviews` (уже есть: 20)
4. ✅ `profile_locations` (уже есть: 12)
5. ✅ `category_images` (уже есть: 7)
6. ✅ `profile_services` (уже есть: 7)

**Важные (функционал профилей):**
7. ⏳ `master_class_programs` (есть: 1, нужно больше?)
8. ⏳ `show_programs` (пусто: 0)
9. ⏳ `quest_programs` (пусто: 0)
10. ⏳ `animator_characters` (пусто: 0)
11. ⏳ `agency_partners` (пусто: 0)
12. ⏳ `agency_cases` (пусто: 0)
13. ⏳ `profile_activities` (пусто: 0)

**Системные (активность, заявки):**
14. ⏳ `order_requests` (пусто: 0)
15. ⏳ `user_activity` (пусто: 0)

**Реклама:**
16. ✅ `advertisements` (есть: 1)
17. ✅ `ad_campaigns` (есть: 1)
18. ✅ `ad_slots` (есть: 3)
19. ⏳ `ad_bookings` (пусто: 0)
20. ✅ `ad_impressions` (есть: 5)

---

## 📋 ЭТАП 2: МИГРАЦИЯ API (2-3 часа)

### Приоритет 1: КРИТИЧНЫЕ (работа сайта)

#### 2.1 Профили (CRUD)
- [ ] `GET /api/profiles` - список профилей для владельца
- [ ] `POST /api/profiles` - создание профиля
- [ ] `PATCH /api/profiles/[id]` - обновление профиля
- [ ] `DELETE /api/profiles/[id]` - удаление профиля
- [ ] `GET /api/profiles/me` - мой профиль

**Проверка:** Создать → Редактировать → Удалить тестовый профиль

#### 2.2 Отзывы
- [x] `GET /api/reviews` - получение отзывов (уже мигрирован)
- [ ] `POST /api/reviews` - добавление отзыва
- [ ] `PATCH /api/reviews/[id]` - редактирование
- [ ] `DELETE /api/reviews/[id]` - удаление

**Проверка:** Добавить → Редактировать → Удалить тестовый отзыв

#### 2.3 Услуги
- [x] `GET /api/services` - получение услуг (уже мигрирован)
- [ ] `POST /api/services` - добавление услуги
- [ ] `PATCH /api/services/[id]` - редактирование
- [ ] `DELETE /api/services/[id]` - удаление
- [ ] `POST /api/services/reorder` - пересортировка

**Проверка:** Добавить → Редактировать → Удалить → Пересортировать

#### 2.4 Загрузка файлов
- [ ] `POST /api/upload` - загрузка файлов (фото, видео)

**Проверка:** Загрузить фото профиля, фото услуги, видео

#### 2.5 Заявки
- [ ] `GET /api/requests` - список заявок
- [ ] `POST /api/requests` - создание заявки
- [ ] `GET /api/requests/[id]` - детали заявки
- [ ] `POST /api/requests/[id]/respond` - ответ на заявку

**Проверка:** Создать → Посмотреть → Ответить на заявку

#### 2.6 Программы (для разных типов профилей)
- [x] `GET /api/master-class-programs` (уже мигрирован)
- [x] `POST /api/master-class-programs` (уже мигрирован)
- [x] `PATCH /api/master-class-programs/[id]` (уже мигрирован)
- [ ] `GET /api/show-programs`
- [ ] `POST /api/show-programs`
- [ ] `PATCH /api/show-programs/[id]`
- [ ] `GET /api/quest-programs`
- [ ] `POST /api/quest-programs`
- [ ] `PATCH /api/quest-programs/[id]`
- [ ] `GET /api/animator-characters`
- [ ] `POST /api/animator-characters`
- [ ] `PATCH /api/animator-characters/[id]`

**Проверка:** Для КАЖДОГО типа - создать → редактировать → удалить программу

#### 2.7 Партнёры агентств
- [x] `GET /api/agency-partners` (уже мигрирован)
- [ ] `POST /api/agency-partners`
- [ ] `PATCH /api/agency-partners/[id]`
- [ ] `DELETE /api/agency-partners/[id]`

**Проверка:** Создать → Редактировать → Удалить партнёра

#### 2.8 Локации
- [ ] `GET /api/profile-locations`
- [ ] `POST /api/profile-locations`
- [ ] `PATCH /api/profile-locations/[id]`
- [ ] `DELETE /api/profile-locations/[id]`

**Проверка:** Добавить → Редактировать → Удалить локацию

### Приоритет 2: ВАЖНЫЕ (функционал)

#### 2.9 Аналитика
- [ ] `POST /api/analytics/track` - tracking событий
- [ ] `POST /api/analytics/source` - источники трафика
- [ ] `POST /api/analytics/interest` - интересы пользователей

**Проверка:** Отправить тестовые события

#### 2.10 Уведомления
- [ ] `GET /api/notifications` - список уведомлений
- [ ] `POST /api/notifications/[id]/read` - отметить как прочитанное

**Проверка:** Создать уведомление → Прочитать

#### 2.11 Чаты
- [ ] `GET /api/conversations` - список чатов
- [ ] `GET /api/conversations/[id]/messages` - сообщения
- [ ] `POST /api/conversations/[id]/messages` - отправка сообщения

**Проверка:** Создать чат → Отправить сообщение → Прочитать

### Приоритет 3: ДОПОЛНИТЕЛЬНЫЕ (админка, интеграции)

- [ ] `/api/admin/*` - админка (можно позже)
- [ ] `/api/ai/*` - AI функции (можно позже)
- [ ] `/api/payments/*` - платежи (можно позже)
- [ ] `/api/telegram/*` - Telegram (можно позже)

---

## 📋 ЭТАП 3: STORAGE (1-2 часа)

### 3.1 Анализ текущего Storage
- [ ] Подсчитать файлы в Supabase Storage
- [ ] Определить bucket'ы (portfolio, profiles, etc.)
- [ ] Оценить размер данных

### 3.2 Миграция на Yandex Object Storage
- [ ] Создать bucket в Yandex Object Storage
- [ ] Скопировать файлы из Supabase
- [ ] Обновить URL в БД
- [ ] Мигрировать `/api/upload` на новый storage

**Проверка:** Загрузить файл → Открыть URL → Удалить файл

---

## 📋 ЭТАП 4: ФИНАЛЬНАЯ ПРОВЕРКА (30-60 мин)

### 4.1 Функциональное тестирование

**Сценарий 1: Владелец площадки**
- [ ] Регистрация через Yandex
- [ ] Создание профиля "Площадка"
- [ ] Добавление услуг
- [ ] Загрузка фото
- [ ] Добавление локации
- [ ] Публикация профиля
- [ ] Просмотр на главной

**Сценарий 2: Владелец мастер-класса**
- [ ] Создание профиля "Мастер-класс"
- [ ] Добавление программы
- [ ] Загрузка фото программы
- [ ] Публикация
- [ ] Просмотр на главной

**Сценарий 3: Клиент**
- [ ] Поиск профилей на главной
- [ ] Открытие профиля
- [ ] Отправка заявки
- [ ] Получение уведомления

**Сценарий 4: Реклама**
- [ ] Создание кампании
- [ ] Создание объявления
- [ ] Показ на главной
- [ ] Tracking impressions/clicks

### 4.2 Проверка данных
- [ ] Все профили на месте (43)
- [ ] Все отзывы на месте (20)
- [ ] Все локации на месте (12)
- [ ] Все изображения загружаются

### 4.3 Проверка производительности
- [ ] Главная загружается < 2 сек
- [ ] Профиль загружается < 1 сек
- [ ] API отвечает < 500ms

---

## 📋 ЭТАП 5: CLEANUP (30 мин)

### 5.1 Удаление legacy кода
- [ ] Удалить dummy Supabase переменные из `.env.local`
- [ ] Удалить `lib/supabase/client.ts` и `server.ts` (если не используются)
- [ ] Удалить старые backup файлы

### 5.2 Документация
- [ ] Обновить README.md
- [ ] Создать MIGRATION_COMPLETE.md
- [ ] Архивировать все migration scripts

### 5.3 Деплой на production
- [ ] Обновить `.env.local` на сервере
- [ ] Деплой нового кода
- [ ] Проверка на production

---

## ✅ КРИТЕРИИ УСПЕХА

Миграция считается ЗАВЕРШЁННОЙ, если:

1. ✅ Все таблицы заполнены данными (0 пустых критичных таблиц)
2. ✅ Все критичные API мигрированы на Prisma
3. ✅ Storage работает на Yandex Object Storage
4. ✅ Все 4 тестовых сценария проходят без ошибок
5. ✅ Нет dummy Supabase переменных
6. ✅ Производительность в норме

---

## 🔥 НАЧИНАЕМ!

**Время старта:** {{ NOW }}  
**Ожидаемое время завершения:** {{ NOW + 4-6 часов }}

**Этап 1: ИМПОРТ ДАННЫХ → СТАРТ**




