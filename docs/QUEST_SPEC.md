# Спецификация профиля "Квест" (quest)

## 🎮 Структура блоков

### **Уникальные блоки:**

#### 1. **"Наши квесты"** 
Аналог "Персонажей" у аниматора. Основной блок с квестами.

**Характеристики квеста:**
- Название квеста (обязательно)
- Тематика (мультивыбор):
  - `adventure` - Приключения
  - `detective` - Детектив
  - `fantasy` - Фантастика
  - `horror` - Ужасы (легкие)
  - `sci_fi` - Научная фантастика
  - `historical` - Исторический
  - `minecraft` - Майнкрафт
  - `fairy_tale` - Сказка
  - `spy` - Шпионский
  - `pirates` - Пираты
  - `superhero` - Супергерои
  - `other` - Другое
- Сложность:
  - `easy` - Легкая (для детей 5-8 лет)
  - `medium` - Средняя (для детей 8-12 лет)
  - `hard` - Сложная (для подростков и взрослых)
- Длительность (минуты): 60, 90, 120, 150
- Количество участников: от-до (например, 5-15 человек)
- Возрастная категория (мультивыбор): 5-8, 8-12, 12-16, взрослые
- Описание сюжета (с AI-генератором)
- Фото/Видео

**Игровые характеристики:**
- Тип активности:
  - `active` - Активный (с физическими заданиями)
  - `logic` - Логический (головоломки)
  - `mixed` - Смешанный
- Реквизит включен (boolean)
- Актер в игре (boolean)
- Требования к площадке (текст)

#### 2. **"Характеристики организатора"**
Блок с характеристиками квестовой команды.

**Поля:**
- Опыт работы (лет): 1-3, 3-5, 5+
- Формат работы: `mobile` (только выездной)
- Радиус выезда (км): 10, 20, 50, 100, без ограничений
- География работы (районы/города)
- Количество проведенных квестов (число)
- Собственный реквизит (boolean)

---

### **Стандартные блоки:**

1. **О нас** - с AI-генератором
2. **Фото и видео** - фото квестов, реквизита (с 3 вариантами отображения)
3. **Услуги** - квесты (основные)
4. **Доп. услуги** - актеры, фотограф, аниматор после квеста
5. **Отзывы**
6. **FAQ**
7. **Контакты** - география работы (БЕЗ карты)

---

### **Блоки которых НЕТ:**
- ❌ Адреса/Локации (это выездная услуга)
- ❌ Персонажи (заменены на "Квесты")

---

## 🗄️ База данных

### Таблица: `quest_programs`

```sql
CREATE TABLE public.quest_programs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profile_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  
  -- Основная информация
  name TEXT NOT NULL,
  description TEXT, -- сюжет квеста
  
  -- Медиа
  photos TEXT[] DEFAULT '{}',
  video_url TEXT,
  
  -- Характеристики
  themes TEXT[] DEFAULT '{}', -- тематики
  difficulty TEXT, -- easy, medium, hard
  duration INTEGER, -- минуты
  min_players INTEGER, -- минимум участников
  max_players INTEGER, -- максимум участников
  age_ranges TEXT[] DEFAULT '{}', -- 5-8, 8-12, 12-16, adult
  
  -- Игровые характеристики
  activity_type TEXT, -- active, logic, mixed
  includes_props BOOLEAN DEFAULT true, -- реквизит включен
  has_actor BOOLEAN DEFAULT false, -- актер в игре
  venue_requirements TEXT, -- требования к площадке
  
  -- Метаданные
  is_active BOOLEAN DEFAULT true,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

### Обновление: `profile_details` для quest

```json
{
  "experience_years": "3-5",
  "work_format": "mobile",
  "coverage_radius": 50,
  "total_quests_conducted": 250,
  "has_own_props": true
}
```

---

## 🎨 Компоненты

```
components/features/quest/
├── quest-programs-section.tsx      # Публичный блок квестов
├── quest-characteristics.tsx       # Характеристики организатора
├── quest-programs-manager.tsx      # Управление квестами
└── quest-program-form.tsx          # Форма создания/редактирования
```

---

## 🎯 Интеграция в профиль

### Меню для quest:
1. О нас
2. **Наши квесты** ⭐
3. Фото и видео
4. Услуги
5. Доп. услуги
6. Отзывы
7. FAQ
8. Контакты

### Корзина:
- ✅ Есть корзина
- Кнопка "Заказать" на карточках квестов





