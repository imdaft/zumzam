# Спецификация профиля "Шоу-программа" (show)

## 🎭 Структура блоков

### **Уникальные блоки:**

#### 1. **"Номера и программы"** 
Аналог "Персонажей" у аниматора. Основной блок с шоу-номерами.

**Характеристики номера:**
- Название номера (обязательно)
- Жанр шоу (мультивыбор):
  - `bubble_show` - Шоу мыльных пузырей
  - `magic_show` - Фокусы и иллюзии
  - `fire_show` - Огненное шоу
  - `light_show` - Световое шоу
  - `paper_disco` - Бумажная дискотека
  - `chemical_show` - Химическое шоу
  - `sand_animation` - Песочная анимация
  - `acrobatics` - Акробатика
  - `mime` - Пантомима
  - `dance` - Танцевальное шоу
  - `music` - Музыкальное выступление
  - `other` - Другое
- Длительность (минуты): 15, 30, 45, 60, 90
- Количество артистов: 1-10
- Возрастная аудитория (мультивыбор): 3-5, 5-10, 10-14, взрослые, универсальное
- Описание номера (с AI-генератором)
- Фото/Видео

**Технический райдер (опционально):**
- Требуется звуковое оборудование (boolean)
- Требуется сцена/площадка (текст - размер в метрах)
- Световое оборудование (boolean)
- Дополнительные требования (текст)

#### 2. **"Характеристики исполнителя"**
Блок с характеристиками артиста/коллектива.

**Поля:**
- Опыт работы (лет): 1-3, 3-5, 5-10, 10+
- Формат работы:
  - `mobile` - Только выездной
  - `studio` - Только на площадке
  - `both` - Выездной и на площадке
- Радиус выезда (км): 10, 20, 50, 100, без ограничений
- География работы (районы/города)
- Награды/Достижения (массив текстов)
- Собственное оборудование (boolean)

---

### **Стандартные блоки:**

1. **О нас** - с AI-генератором
2. **Фото и видео** - портфолио выступлений (с 3 вариантами отображения)
3. **Услуги** - программы (основные)
4. **Доп. услуги** - дополнительные опции (аквагрим, реквизит и т.д.)
5. **Отзывы** - отзывы клиентов
6. **FAQ** - частые вопросы
7. **Контакты** - география работы (БЕЗ карты, только районы)

---

### **Блоки которых НЕТ:**
- ❌ Адреса/Локации (это выездная услуга)
- ❌ Персонажи (заменены на "Номера")
- ❌ Пакетные предложения tier (используются обычные услуги)

---

## 🗄️ База данных

### Таблица: `show_programs`

```sql
CREATE TABLE public.show_programs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profile_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  
  -- Основная информация
  name TEXT NOT NULL,
  description TEXT,
  
  -- Медиа
  photos TEXT[] DEFAULT '{}',
  video_url TEXT,
  
  -- Характеристики
  genres TEXT[] DEFAULT '{}', -- ['bubble_show', 'magic_show']
  duration INTEGER, -- минуты
  performers_count INTEGER, -- количество артистов
  age_ranges TEXT[] DEFAULT '{}', -- ['3-5', '5-10', 'universal']
  
  -- Технический райдер
  requires_sound BOOLEAN DEFAULT false,
  stage_requirements TEXT, -- размеры площадки
  requires_light BOOLEAN DEFAULT false,
  additional_requirements TEXT,
  
  -- Метаданные
  is_active BOOLEAN DEFAULT true,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_show_programs_profile ON public.show_programs(profile_id);
CREATE INDEX idx_show_programs_active ON public.show_programs(profile_id, is_active);
```

### Обновление: `profile_details` для show

Добавить в `profile_details.jsonb` для category='show':

```json
{
  "experience_years": "5-10",
  "work_format": "both",
  "coverage_radius": 50,
  "awards": ["Лауреат конкурса 2023", "Приз зрительских симпатий"],
  "has_own_equipment": true
}
```

---

## 🎨 Компоненты для создания

### 1. Блоки для публичного профиля:
```
components/features/show/
├── show-programs-section.tsx       # Главный блок "Номера и программы"
├── show-characteristics.tsx         # Блок характеристик исполнителя
└── program-card.tsx                 # Карточка номера
```

### 2. Управление для владельца:
```
components/features/show/
├── programs-manager.tsx             # Управление номерами
├── program-form.tsx                 # Форма создания/редактирования номера
└── show-details-form.tsx            # Форма характеристик исполнителя
```

### 3. API Routes:
```
app/api/
└── show-programs/
    ├── route.ts                     # POST (создать), GET (список)
    └── [id]/
        └── route.ts                 # PATCH (обновить), DELETE (удалить)
```

---

## 🎯 Интеграция в профиль

### Меню для show:
1. О нас
2. **Номера и программы** ⭐
3. Фото и видео
4. Услуги
5. Доп. услуги
6. Отзывы
7. FAQ
8. Контакты

### Корзина:
- ✅ Есть корзина
- Кнопка "Заказать" на карточках номеров
- Добавление в корзину: номер + доп. услуги

---

## 📝 Wizard (шаги создания профиля)

Для category='show' добавить шаг:

**"Характеристики исполнителя"** (после базовой информации):
- Опыт работы
- Формат работы (выездной/площадка)
- Радиус выезда
- Собственное оборудование

---

## ✅ Чеклист реализации

- [ ] Создать миграцию для таблицы `show_programs`
- [ ] Создать API routes для управления программами
- [ ] Создать компонент `ShowProgramsSection` (публичный вид)
- [ ] Создать компонент `ProgramsManager` (управление)
- [ ] Создать компонент `ProgramForm` (форма создания/редактирования)
- [ ] Создать компонент `ShowCharacteristics` (блок характеристик)
- [ ] Добавить шаг в wizard для show
- [ ] Обновить страницу профиля для отображения блоков show
- [ ] Добавить иконки и константы для жанров
- [ ] Тестирование всех компонентов





