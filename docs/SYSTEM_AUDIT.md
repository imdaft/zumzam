# 🧹 АУДИТ СИСТЕМЫ: Legacy код и мертвые страницы

**Дата:** 18 декабря 2025

---

## 🔍 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 1. **ДУБЛИРОВАНИЕ СИСТЕМ ПРОФИЛЕЙ**

#### ✅ АКТУАЛЬНАЯ СИСТЕМА (используется):
**Директория:** `app/(dashboard)/profiles/`

**Маршруты:**
- `/profiles` → `app/(dashboard)/profiles/page.tsx` - список профилей
- `/profiles/[slug]/edit` → `app/(dashboard)/profiles/[slug]/edit/` - редактирование

**Логика:**
- ✅ Кнопка "Создать" вызывает `/api/profiles/quick-create` (строка 152)
- ✅ Создается пустой профиль и сразу редирект на `/profiles/{slug}/edit`
- ✅ **НЕТ** отдельной страницы создания - все в одном месте
- ✅ Использует `CreateProfileForm` в режиме `mode="edit"`

#### ❌ LEGACY СИСТЕМА (частично используется):
**Директория:** `app/dashboard/profiles/`

**Маршруты:**
- `/dashboard/profiles/[id]/show-programs/page` - управление шоу-программами
- `/dashboard/profiles/[id]/show-programs/new/page` - создание шоу-программы
- `/dashboard/profiles/[id]/quest-programs/page` - управление квест-программами
- `/dashboard/profiles/[id]/quest-programs/new/page` - создание квест-программы
- `/dashboard/profiles/[id]/master-class-programs/new/page` - создание мастер-класса
- `/dashboard/profiles/[id]/photography-styles/new/page` - создание стиля фотографии

**Проблема:**
- ⚠️ Использует **старый путь** `/dashboard/profiles/[id]/` вместо нового `/(dashboard)/profiles/[slug]/`
- ⚠️ Использует **ID** вместо **slug**
- ⚠️ Эти страницы **изолированы** и не интегрированы в новую систему

---

### 2. **КОМПОНЕНТ CreateProfileForm**

**Файл:** `components/features/profile/create-profile-form.tsx`

**Анализ:**
- ✅ Используется в `app/(dashboard)/profiles/[slug]/edit/client.tsx` (строка 281)
- ✅ Поддерживает **два режима**: `mode="create"` и `mode="edit"`
- ✅ Режим `edit` активен когда `initialData` передан
- ❌ Режим `create` **НЕ используется** нигде в текущей системе

**Логика создания профиля (ТЕКУЩАЯ):**
1. Пользователь нажимает "Создать" на `/profiles`
2. Вызывается `handleCreateProfile()` → `/api/profiles/quick-create` (POST)
3. API создает минимальный профиль в БД
4. Редирект на `/profiles/{slug}/edit`
5. Открывается `CreateProfileForm` в режиме `mode="edit"` с `initialData={profile}`

**Вывод:**
- ✅ **НЕТ отдельной страницы создания** - правильно!
- ✅ Создание = редактирование пустого профиля
- ❌ Код для `mode="create"` в компоненте **не используется** (можно удалить)

---

### 3. **ДУБЛИРУЮЩИЕСЯ ROUTE GROUPS**

#### В `app/`:
- `(admin)` - админка
- `(auth)` - авторизация
- `(dashboard)` - **НОВАЯ** панель управления ✅
- `(public)` - публичные страницы

#### Вне route groups:
- `admin/` - старая админка? (нужно проверить)
- `auth/` - старая авторизация? (нужно проверить)
- `dashboard/` - **СТАРАЯ** панель управления ❌

---

### 4. **СТАРАЯ СИСТЕМА КЛАССИФИКАЦИИ**

**Файл:** `components/features/profile/create-profile-form.tsx`

**Проблема:**
- Строки 909-923: `CategoryVisualSelector` - старая система
- Строки 927-948: `VenueTypeGroupedSelector` - старая система типов площадок
- **НЕТ:** Нового визарда классификации

**Где используется:**
- ✅ `app/(dashboard)/profiles/[slug]/edit/client.tsx` (строка 281) - **АКТИВНО**
- Это **единственная** точка входа для создания/редактирования

---

### 5. **API ENDPOINTS**

Нужно проверить:
- `/api/profiles/quick-create` - создание пустого профиля
- `/api/profiles/check` - проверка существующих профилей
- `/api/profiles/[id]` - CRUD операции
- `/api/profiles/me` - текущий профиль

---

## 📋 ПЛАН ОЧИСТКИ

### 🔴 **КРИТИЧНО: Удалить/Переместить**

#### 1. Старая система dashboard
**Удалить:** `app/dashboard/profiles/[id]/`

**НО:** Проверить, используются ли эти страницы:
- `/dashboard/profiles/[id]/show-programs/`
- `/dashboard/profiles/[id]/quest-programs/`
- `/dashboard/profiles/[id]/master-class-programs/`
- `/dashboard/profiles/[id]/photography-styles/`

**Если используются:**
- Переместить в `app/(dashboard)/profiles/[slug]/edit/` как табы/модалы
- Или в раздел "Услуги" внутри редактирования профиля

#### 2. Неиспользуемый код в CreateProfileForm
**Удалить:**
- Логику для `mode="create"` (строки 138, 359-364, 812-815)
- `step === 'category'` (строка 868) - если не нужен визард выбора
- Весь код для `CategorySelectionStep`

#### 3. Старые компоненты классификации
**Заменить:**
- `CategoryVisualSelector` → `ClassificationWizard` (шаг 1-2)
- `VenueTypeGroupedSelector` → `ClassificationWizard` (шаг 3-5)

---

### 🟡 **СРЕДНИЙ ПРИОРИТЕТ: Проверить**

#### 1. Проверить старые директории
- `app/admin/` vs `app/(admin)/` - что используется?
- `app/auth/` vs `app/(auth)/` - что используется?
- `app/dashboard/` vs `app/(dashboard)/` - уже нашли

#### 2. Проверить API endpoints
- Какие используют **ID**, какие **slug**?
- Нужно мигрировать все на **slug**

#### 3. Проверить компоненты
- `ProfileDraftsList` - используется для localStorage drafts
- Нужен ли он еще после `/api/profiles/quick-create`?

---

### 🟢 **НИЗКИЙ ПРИОРИТЕТ: Оптимизация**

#### 1. Убрать дублирование
- `profile-card.tsx` - 2 копии (Windows paths)
- `create-profile-form.tsx` - 2 копии

#### 2. Обновить типы
- `DbProfile` интерфейс на главной странице
- Добавить `primary_venue_type`, `activities`, `business_models`

---

## 🎯 ВЫВОДЫ

### ✅ Текущая система ПРАВИЛЬНАЯ:
1. ✅ **Единая форма** создания/редактирования
2. ✅ Quick-create через API → сразу в edit
3. ✅ Route groups `(dashboard)` для панели управления
4. ✅ Slug-based маршруты `/profiles/[slug]/edit`

### ❌ Что нужно ИСПРАВИТЬ:
1. ❌ Удалить/переместить `app/dashboard/profiles/`
2. ❌ Интегрировать новый визард в `CreateProfileForm`
3. ❌ Убрать старую классификацию
4. ❌ Мигрировать ID → slug в API

---

## 📊 СЛЕДУЮЩИЕ ШАГИ

1. **Проверить использование старых маршрутов** (show-programs, quest-programs...)
2. **Интегрировать ClassificationWizard** вместо старой системы
3. **Удалить неиспользуемый код** в CreateProfileForm
4. **Очистить legacy папки** (после подтверждения)

---

**Готово к началу интеграции! 🚀**





