# Адаптивность страницы сообщений (Чат)

> ℹ️ **Примечание:** Эта страница использует индивидуальный breakpoint. См. общую стратегию: [`RESPONSIVE_STRATEGY.md`](./RESPONSIVE_STRATEGY.md)

---

## 📱 Индивидуальная настройка для /messages

Страница сообщений (`/messages`) использует **кастомный breakpoint 1200px** из-за сложности интерфейса.

### Breakpoint: **1200px (chat:)**

- **≥ 1200px** (Десктопная версия)
  - 3 панели (список диалогов + чат + панель информации о заказе/заявке)
  - Или 2 панели (список + чат) если нет заказа
  
- **< 1200px** (Мобильная версия)
  - Полноэкранный режим
  - Навигация между списком и чатом через кнопку "Назад"
  - Панель информации о заказе выдвигается свайпом сверху

## 🎯 Почему именно 1200px?

### Проблема на 1024px:
На разрешении 1024px при открытии панели сделки (3 панели) всё "сплющивается":
- Список диалогов: 280px
- Чат: 400px
- Панель сделки: 320px
- **Итого: ~1000px** - слишком мало, текст нечитаемый! ❌

### Решение - 1200px:
Минимальная ширина для **комфортной работы с 3 панелями**:
- Список диалогов: 300px
- Чат: 500px
- Панель информации о заказе: 350px
- **Итого: ~1150-1200px** ✅

### Преимущества 1200px:
- ✅ **Full HD вертикально (1080px)** = мобильная версия (удобная!)
- ✅ **iPad Pro вертикально (1024px)** = мобильная версия (оптимально для планшета)
- ✅ **MacBook Air 13" (1440px)** = десктопная версия с 3 панелями (комфортно)
- ✅ **Full HD горизонтально (1920px)** = десктопная версия (отлично)
- ✅ **3 панели не "сплющены"** - всё читаемо и удобно

## 💻 Реализация

### 1. Кастомный breakpoint в Tailwind

**Файл:** `tailwind.config.ts`

```typescript
screens: {
  'xs': '375px',
  'chat': '1200px',  // ⭐ Кастомный для страницы messages
  'touch': { 'raw': '(hover: none)' },
  'mouse': { 'raw': '(hover: hover)' },
}
```

### 2. JavaScript логика

**Файл:** `app/(dashboard)/messages/page.tsx` (строка ~215)

```typescript
const isDesktop = useMediaQuery('(min-width: 1200px)')
const isMobile = !isDesktop
```

### 3. Tailwind классы с префиксом `chat:`

```tsx
// Пример использования кастомного breakpoint
<div className="hidden chat:flex">
  {/* Показывается только на ≥1200px */}
</div>

<div className="flex chat:hidden">
  {/* Показывается только на <1200px */}
</div>
```

## 🔧 Как изменить порог

### JavaScript логику:
```typescript
// app/(dashboard)/messages/page.tsx
const isDesktop = useMediaQuery('(min-width: 1200px)') // Измените здесь
```

### Tailwind config:
```typescript
// tailwind.config.ts
screens: {
  'chat': '1200px',  // Измените здесь
}
```

⚠️ **Важно:** После изменения `tailwind.config.ts` нужно перезапустить dev-сервер!

## 📊 Сравнение breakpoints

| Breakpoint | Проблемы | Преимущества |
|------------|----------|--------------|
| **1024px** | ❌ 3 панели "сплющены"<br>❌ Текст нечитаемый | ✅ Вертикальный Full HD = десктоп |
| **1200px** ⭐ | - | ✅ 3 панели комфортно<br>✅ Всё читаемо<br>✅ Вертикальный Full HD = мобильная (удобно) |
| **1280px** | ⚠️ Слишком высоко | ✅ Максимальный комфорт |

## ⚠️ Важно

Это изменение **только для страницы `/messages`**. Остальные страницы используют стандартные breakpoints:
- Базовый `useIsMobile()`: **768px (md)**
- Большинство компонентов: **md:** (768px) и **lg:** (1024px)

## 🧪 Как протестировать

1. Откройте `/messages` в браузере
2. Откройте DevTools (F12) → Режим адаптивности (Ctrl+Shift+M)
3. Проверьте разные разрешения:
   - **1199px и меньше** → мобильная версия
   - **1200px и больше** → десктопная версия с 3 панелями
4. Откройте панель сделки (кнопка справа) и убедитесь, что всё читаемо

---

**Дата последнего изменения:** 06.12.2024  
**Текущий breakpoint:** 1200px (chat:)  
**Причина:** Оптимальный баланс - 3 панели помещаются комфортно, не "сплющиваются"
