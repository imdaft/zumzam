# ⚠️ ВАЖНО: Перезапустите сервер!

## Изменения в `next.config.ts` требуют перезапуска

После изменений в `next.config.ts` **обязательно** нужно:

### 1. Остановить текущий сервер
```bash
Ctrl + C   # в терминале где запущен npm run dev
```

### 2. Очистить кеш Next.js
```bash
rm -rf .next
# или на Windows:
rmdir /s /q .next
```

### 3. Запустить сервер заново
```bash
npm run dev
```

---

## Что исправлено:

### ✅ Permissions Policy
- `microphone=(self)` - доступ к микрофону разрешен

### ✅ Webpack ошибки
- `serverComponentsExternalPackages` - пакеты помечены как серверные
- `config.resolve.alias = false` - модули игнорируются на клиенте

---

## После перезапуска:

✅ Никаких ошибок `Module parse failed`  
✅ Микрофон работает в AI чате  
✅ Whisper транскрипция работает на сервере  

---

## Если ошибки остались:

1. Убедитесь что сервер **полностью остановлен** (закройте терминал)
2. Удалите папку `.next` **вручную**
3. Запустите `npm run dev` в **новом терминале**

---

# 🚀 Перезапустите сервер СЕЙЧАС!















