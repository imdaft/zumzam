# Адаптивность: Индивидуальный подход

## 📱 Текущая стратегия

После экспериментов с глобальным изменением breakpoints, мы вернулись к **стандартным Tailwind breakpoints** с возможностью индивидуальной настройки для сложных страниц.

## 🎯 Стандартные breakpoints (по умолчанию)

### JavaScript (хук)
```typescript
// hooks/use-media-query.ts
export function useIsMobile(): boolean {
  return !useMediaQuery('(min-width: 768px)') // md breakpoint
}
```

### Tailwind CSS
- `sm:` = **640px** - маленькие планшеты
- `md:` = **768px** - планшеты (основной порог mobile/desktop)
- `lg:` = **1024px** - десктопы, ноутбуки
- `xl:` = **1280px** - широкие экраны
- `2xl:` = **1536px** - очень широкие экраны

## 🔧 Индивидуальные настройки для сложных страниц

### Пример: Страница сообщений (`/messages`)

Для страницы сообщений используется увеличенный breakpoint **1280px (xl)**, т.к. интерфейс с 3 панелями требует больше места.

**Файл:** `app/(dashboard)/messages/page.tsx`

```typescript
// Индивидуальный breakpoint для чата
const isDesktop = useMediaQuery('(min-width: 1280px)')
const isMobile = !isDesktop

// И соответствующие Tailwind классы: xl: вместо md:/lg:
```

### Когда использовать индивидуальные breakpoints?

✅ **Используйте увеличенные breakpoints (1024px - 1280px) для:**
- Страниц с 3+ колонками/панелями
- Сложных дашбордов с множеством данных
- Интерфейсов с таблицами, где нужно видеть много столбцов
- Редакторов, конструкторов, админ-панелей

❌ **НЕ используйте для:**
- Обычных страниц контента
- Карточек товаров/услуг
- Форм
- Списков
- Простых UI-компонентов

## 🚫 Почему НЕ 1280px везде?

### Проблема с вертикальными мониторами
- **Full HD вертикально** = 1080px ширины
- При breakpoint 1280px → **мобильная версия на вертикальном мониторе!** ❌
- Это неудобно для пользователей с вертикальными экранами

### Оптимальная стратегия
- **768px (md)** - для большинства страниц
- **1024px (lg)** - для страниц средней сложности
- **1280px (xl)** - только для очень сложных интерфейсов

## 📋 Как добавить индивидуальную настройку

### Шаг 1: Определите breakpoint
```typescript
// В начале компонента страницы
const isDesktop = useMediaQuery('(min-width: 1280px)') // или 1024px
const isMobile = !isDesktop
```

### Шаг 2: Используйте в JSX логике
```typescript
{!isMobile ? (
  <DesktopLayout />
) : (
  <MobileLayout />
)}
```

### Шаг 3: Обновите Tailwind классы
Замените `md:` или `lg:` на `xl:` (если breakpoint 1280px):

```tsx
// Было:
<div className="md:flex lg:grid-cols-3">

// Стало (для 1280px):
<div className="xl:flex xl:grid-cols-3">
```

### Шаг 4: Документируйте
Добавьте комментарий в начало файла:

```typescript
/**
 * Страница использует индивидуальный breakpoint 1280px (xl)
 * Причина: сложный интерфейс с 3 панелями
 */
```

## 📊 Текущие индивидуальные настройки

| Страница | Breakpoint | Причина |
|----------|-----------|---------|
| `/messages` | **1200px (chat:)** | 3 панели: список + чат + инфо о заказе. На 1024px всё "сплющивается" |

## 🛠️ Массовое изменение breakpoints (если нужно)

Если в будущем нужно будет массово изменить breakpoints, можно создать Node.js скрипт:

```javascript
// scripts/change-breakpoints.js
// Заменяет md: на xl: во всех файлах, кроме исключений

const excludeFiles = [
  'app/(dashboard)/messages/page.tsx', // уже настроен индивидуально
];

// ... логика замены
```

Но **лучше избегать** массовых изменений без веской причины.

## 💡 Рекомендации

1. **Начинайте со стандартных breakpoints** - они работают для 90% случаев
2. **Тестируйте на реальных устройствах** - эмулятор может обманывать
3. **Документируйте индивидуальные настройки** - чтобы не забыть почему так сделано
4. **Избегайте промежуточных версий** - только mobile и desktop, без "планшетной"

---

**Дата создания:** 06.12.2024  
**Статус:** ✅ Активная стратегия

