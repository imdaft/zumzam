# Настройка уведомлений ZumZam

## 📧 Email уведомления через российский SMTP

### Вариант 1: Yandex Mail (Рекомендуется)

**Преимущества:**
- Бесплатный российский провайдер
- Стабильная доставка
- Поддержка SMTP
- Подходит для бизнеса

**Настройка:**

1. **Создайте почтовый ящик** на [yandex.ru](https://mail.yandex.ru)
2. **Включите пароль для приложений:**
   - Откройте [https://id.yandex.ru/security](https://id.yandex.ru/security)
   - Найдите "Пароли приложений"
   - Создайте новый пароль для приложения "Почта"
   - Скопируйте сгенерированный пароль

3. **Добавьте в `.env.local`:**
```bash
# Yandex SMTP
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=ваш-email@yandex.ru
SMTP_PASS=пароль-приложения
SMTP_FROM="ZumZam <ваш-email@yandex.ru>"
```

### Вариант 2: Mail.ru

**Настройка:**

1. **Создайте почту** на [mail.ru](https://mail.ru)
2. **Включите доступ по SMTP:**
   - Настройки → Почта → Все настройки
   - Прокрутите до "Сервер исходящей почты (SMTP)"
   - Включите "Разрешить доступ по SMTP"

3. **Добавьте в `.env.local`:**
```bash
# Mail.ru SMTP
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=ваш-email@mail.ru
SMTP_PASS=ваш-пароль
SMTP_FROM="ZumZam <ваш-email@mail.ru>"
```

### Вариант 3: Собственный домен через Yandex 360

**Для профессионального использования:**

1. Зарегистрируйте домен (например, `zumzam.ru`)
2. Подключите домен к [Yandex 360 для бизнеса](https://360.yandex.ru)
3. Настройте DNS записи (MX, SPF, DKIM)
4. Используйте настройки как для обычного Yandex, но с вашим доменом:

```bash
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=noreply@zumzam.ru
SMTP_PASS=пароль-приложения
SMTP_FROM="ZumZam <noreply@zumzam.ru>"
```

---

## 📱 Telegram уведомления

### Существующий бот: @ZumZamRu_bot

У вас уже есть настроенный Telegram бот с полной системой привязки аккаунтов!

**Текущая реализация:**
- ✅ Таблица `telegram_users` для хранения подключений
- ✅ Таблица `telegram_connect_codes` для кодов привязки
- ✅ API `/api/telegram/connect` для привязки аккаунта
- ✅ Webhook `/api/telegram/webhook` для обработки команд
- ✅ Отправка уведомлений через `telegram_id`

**Как работает:**

1. Пользователь нажимает "Подключить" в настройках уведомлений
2. Открывается ссылка `https://t.me/ZumZamRu_bot?start=connect`
3. Бот генерирует уникальный код и сохраняет в `telegram_connect_codes`
4. Пользователь вводит код на сайте
5. Система создает запись в `telegram_users` со связью `telegram_id` ↔ `user_id`
6. Уведомления отправляются напрямую в Telegram через Bot API

**Структура таблицы telegram_users:**
```sql
telegram_users:
  - telegram_id (уникальный ID пользователя в Telegram)
  - telegram_username (@username)
  - user_id (ID пользователя в ZumZam)
  - notify_new_requests (настройка уведомлений о новых заявках)
  - notify_messages (настройка уведомлений о сообщениях)
  - notify_order_status (настройка уведомлений о статусе заказа)
  - filters (JSON фильтры для подписок)
  - is_active (активность подключения)
```

**Интеграция с notification_settings:**
- API `/api/settings/notifications` проверяет `telegram_users.is_active`
- Возвращает `telegram_verified` и `telegram_username`
- При отключении обновляет `is_active = false`

### Отправка уведомлений

**Пример функции:**

```typescript
async function sendTelegramNotification(userId: string, title: string, message: string, link?: string) {
  const supabase = await createClient()
  
  // Получаем telegram_id из существующей таблицы
  const { data: telegramUser } = await supabase
    .from('telegram_users')
    .select('telegram_id')
    .eq('user_id', userId)
    .eq('is_active', true)
    .single()
  
  if (!telegramUser?.telegram_id) return
  
  // Проверяем, включены ли Telegram уведомления
  const { data: settings } = await supabase
    .from('notification_settings')
    .select('enabled_telegram')
    .eq('user_id', userId)
    .single()
  
  if (!settings?.enabled_telegram) return
  
  // Отправляем уведомление
  await fetch(`https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendMessage`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      chat_id: telegramUser.telegram_id,
      text: `<b>${title}</b>\n\n${message}${link ? `\n\n<a href="${link}">Открыть</a>` : ''}`,
      parse_mode: 'HTML'
    })
  })
}
```

---

## 🔔 Типы уведомлений

Система поддерживает следующие типы:

### Заказы:
- `notify_new_order` - Новая заявка (для исполнителей)
- `notify_order_accepted` - Заявка принята
- `notify_order_rejected` - Заявка отклонена
- `notify_order_completed` - Заявка завершена
- `notify_order_cancelled` - Заявка отменена

### Отклики:
- `notify_new_response` - Новый отклик (для клиентов)
- `notify_response_accepted` - Отклик принят
- `notify_response_rejected` - Отклик отклонен

### Общие:
- `notify_new_message` - Новое сообщение
- `notify_new_review` - Новый отзыв
- `notify_payment_success` - Успешная оплата
- `notify_payment_failed` - Ошибка оплаты

### Подписки:
- `notify_subscription_expiring` - Подписка заканчивается
- `notify_subscription_expired` - Подписка истекла
- `notify_subscription_renewed` - Подписка продлена

### Профиль:
- `notify_profile_verified` - Профиль верифицирован
- `notify_profile_rejected` - Профиль отклонен

### Доска объявлений:
- `notify_board_new_match` - Новое объявление по подписке

---

## 🚀 Быстрый старт

1. **Настройте SMTP** (выберите один из вариантов выше)
2. **Добавьте переменные окружения** в `.env.local`
3. **Перезапустите сервер:**
   ```bash
   npm run dev
   ```
4. **Протестируйте:**
   - Перейдите в `/settings/notifications`
   - Подтвердите email (код придет на почту)
   - Подключите Telegram бота

---

## 📊 Таблицы базы данных

### notification_settings
Основная таблица с настройками уведомлений:
- Каналы: `enabled_site`, `enabled_email`, `enabled_telegram`, `enabled_push`
- Типы: 15+ булевых полей для разных уведомлений
- Метаданные: `email_verified`, `created_at`, `updated_at`

### telegram_users (существующая)
Привязки Telegram аккаунтов:
- Связь `telegram_id` ↔ `user_id`
- Настройки уведомлений
- Фильтры для заявок
- Статус активности

### email_verification_codes
Коды подтверждения email:
- 6-значный код
- Срок действия 15 минут
- Одноразовое использование

### telegram_connect_codes (существующая)
Коды подключения Telegram:
- Генерируются ботом
- Срок действия 10 минут
- Используются для привязки аккаунта

---

## 🔒 Безопасность

1. **SMTP пароли:**
   - Храните в `.env.local` (добавлен в `.gitignore`)
   - Используйте пароли приложений (не основной пароль)
   - Регулярно обновляйте

2. **Telegram Bot Token:**
   - Храните в переменных окружения
   - Используйте webhook с секретным ключом
   - Не логируйте токен в консоль

3. **Коды подтверждения:**
   - Автоматическое истечение через 10-15 минут
   - Одноразовое использование
   - Хешируйте при хранении (опционально)

---

## 🧪 Тестирование

### Email:
```bash
# Проверка подключения к SMTP
curl -X POST http://localhost:4000/api/settings/notifications/email-verify
```

### Telegram:
1. Откройте [https://t.me/ZumZamRu_bot?start=connect](https://t.me/ZumZamRu_bot?start=connect)
2. Следуйте инструкциям бота
3. Введите код на сайте

---

## 📝 Чек-лист перед production

- [ ] Настроен SMTP через российский провайдер
- [ ] Все переменные окружения добавлены на сервере
- [ ] Email отправка протестирована
- [ ] Telegram бот запущен и отвечает
- [ ] Webhook настроен для Telegram
- [ ] RLS политики проверены
- [ ] Логирование настроено
- [ ] Мониторинг ошибок подключен

---

**Дата обновления:** 6 декабря 2024
