# Спецификация профиля "Агентство" (agency)

## 🏢 Структура блоков

### **Уникальные блоки:**

#### 1. **"Наши специалисты"** 
Блок с партнерами и специалистами агентства.

**Два типа специалистов:**

**A. Интеграция с профилями ZumZam:**
- Выбор профилей из базы ZumZam
- Автоматическое подтягивание: аватар, имя, категория, рейтинг
- Ссылка на профиль специалиста

**B. Ручное добавление (внешние партнеры):**
- Имя специалиста
- Специализация (аниматор, фотограф, ведущий и т.д.)
- Фото
- Краткое описание
- Контакты (опционально)

**Отображение:**
- Карточки в сетке 2-3 колонки
- С возможностью клика на профиль (если это профиль ZumZam)

#### 2. **"Кейсы"** (Портфолио праздников)
Блок с примерами организованных мероприятий.

**Характеристики кейса:**
- Название праздника (например, "День рождения в стиле Гарри Поттера")
- Описание концепции (с AI-генератором)
- Галерея фото (10-30 фото с праздника)
- Тематика: День рождения, Свадьба, Корпоратив, Выпускной, Юбилей, Детский праздник, Другое
- Количество гостей (число)
- Бюджет мероприятия:
  - `budget` - Эконом (до 50,000₽)
  - `standard` - Стандарт (50,000-150,000₽)
  - `premium` - Премиум (150,000-500,000₽)
  - `luxury` - Люкс (500,000₽+)
- Что было включено (массив):
  - Площадка
  - Декор
  - Аниматоры
  - Фотограф/Видеограф
  - Кейтеринг
  - Торт
  - Шоу-программы
  - Ведущий
  - DJ/Музыка
  - Другое

**Отображение:**
- Каждый кейс - это альбом
- Превью: главное фото + название + теги
- При клике - открывается полная галерея кейса

#### 3. **"Услуги агентства"**
Что организуем (без конкретных цен, так как все под заказ).

**Типы праздников:**
- День рождения (детский/взрослый)
- Свадьба
- Корпоратив
- Выпускной
- Юбилей
- Тематические вечеринки
- Другое

---

### **Стандартные блоки:**

1. **О нас** - с AI-генератором (о команде, опыт, философия)
2. **Фото и видео** - общее портфолио (или скрыт, если есть Кейсы)
3. **Услуги** - что организуем (описательные, БЕЗ цен или "от X₽")
4. **Отзывы**
5. **FAQ**
6. **Контакты** - география работы, офис (может быть карта)

---

### **Блоки которых НЕТ:**
- ❌ Корзина (заменена на кнопку "Связаться с агентством")
- ❌ Цены на услуги (все индивидуально)
- ❌ Персонажи/Программы

---

## 🗄️ База данных

### Таблица: `agency_partners`

```sql
CREATE TABLE public.agency_partners (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agency_profile_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  
  -- Если это профиль с ZumZam
  partner_profile_id UUID REFERENCES public.profiles(id) ON DELETE SET NULL,
  
  -- Или ручное добавление
  custom_name TEXT,
  custom_specialization TEXT,
  custom_photo TEXT,
  custom_description TEXT,
  custom_contacts TEXT,
  
  -- Метаданные
  is_active BOOLEAN DEFAULT true,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### Таблица: `agency_cases`

```sql
CREATE TABLE public.agency_cases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profile_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  
  -- Основная информация
  title TEXT NOT NULL, -- название праздника
  description TEXT, -- концепция
  
  -- Галерея
  photos TEXT[] DEFAULT '{}',
  
  -- Характеристики
  event_type TEXT, -- birthday, wedding, corporate, etc.
  guest_count INTEGER,
  budget_tier TEXT, -- budget, standard, premium, luxury
  included_services TEXT[] DEFAULT '{}', -- что было включено
  
  -- Метаданные
  is_active BOOLEAN DEFAULT true,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

### Обновление: `profile_details` для agency

```json
{
  "experience_years": "5-10",
  "team_size": 15,
  "events_organized": 500,
  "has_office": true,
  "office_address": "ул. Пример, 10"
}
```

---

## 🎨 Компоненты

```
components/features/agency/
├── agency-partners-section.tsx         # Публичный блок специалистов
├── agency-partners-manager.tsx         # Управление специалистами
├── agency-cases-section.tsx            # Публичный блок кейсов
├── agency-cases-manager.tsx            # Управление кейсами
├── agency-case-gallery.tsx             # Галерея кейса (лайтбокс)
├── partner-form.tsx                    # Форма добавления специалиста
└── case-form.tsx                       # Форма создания кейса
```

---

## 🎯 Интеграция

### Меню для agency:
1. О нас
2. **Наши специалисты** ⭐
3. **Кейсы** ⭐
4. Услуги (что организуем)
5. Отзывы
6. FAQ
7. Контакты

### Вместо корзины:
- ❌ НЕТ корзины
- ✅ Кнопка "Связаться с агентством" → переход в чат
- Логика:
  ```tsx
  onClick={() => router.push(`/dashboard/messages?profile=${profileId}`)}
  ```

---

## 💬 Интеграция с чатом

**Кнопка "Связаться с агентством":**
- Фиксированная кнопка (аналог корзины)
- При клике:
  1. Проверить авторизацию
  2. Создать/открыть чат с агентством
  3. Перейти на страницу чата
  4. Опционально: предзаполнить сообщение ("Здравствуйте! Хочу организовать...")

---

## 🎨 Дизайн особенности

**Блок "Кейсы":**
- Masonry grid (как фотогалерея)
- Каждый кейс - большая карточка с превью
- Hover эффект с кратким описанием
- При клике - открывается полная галерея кейса

**Блок "Наши специалисты":**
- Карточки специалистов 2-3 колонки
- Фото + имя + специализация
- Если профиль ZumZam - бейдж и ссылка
- Рейтинг (если есть)





