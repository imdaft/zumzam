# Настройка MCP Miro с полученным токеном

## ✅ Токен получен!

Ваш OAuth токен:
```
eyJtaXJvLm9yaWdpbiI6ImV1MDEifQ_Ravk0Fy8qcQ7BswfkxbS9fYFVIo
```

---

## 📝 Настройка Cursor

### Шаг 1: Найдите папку конфигурации Cursor

**Для Windows:**

1. Откройте Проводник (Windows Explorer)
2. В адресной строке введите:
   ```
   %APPDATA%\Cursor\User\globalStorage
   ```
3. Нажмите Enter

**Или путь напрямую:**
```
C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Roaming\Cursor\User\globalStorage
```

### Шаг 2: Создайте файл mcp.json

В папке `globalStorage`:

1. Создайте новый файл с именем `mcp.json`
2. Откройте его в текстовом редакторе (Блокнот, VS Code и т.д.)

### Шаг 3: Вставьте конфигурацию

Скопируйте и вставьте следующее содержимое в файл `mcp.json`:

```json
{
  "mcpServers": {
    "miro": {
      "command": "npx",
      "args": [
        "-y",
        "@k-jarzyna/mcp-miro"
      ],
      "env": {
        "MIRO_ACCESS_TOKEN": "eyJtaXJvLm9yaWdpbiI6ImV1MDEifQ_Ravk0Fy8qcQ7BswfkxbS9fYFVIo"
      }
    }
  }
}
```

**⚠️ Важно:** 
- Убедитесь, что токен вставлен точно так, как показано выше
- Не добавляйте пробелы или переносы строк
- Файл должен быть в формате JSON (расширение `.json`)

### Шаг 4: Сохраните файл

1. Сохраните файл `mcp.json` (Ctrl+S)
2. Закройте редактор

---

## 🔄 Перезапуск Cursor

1. **Полностью закройте Cursor:**
   - Закройте все окна Cursor
   - Убедитесь, что процесс не работает в фоне (через Диспетчер задач)

2. **Откройте Cursor заново**

---

## ✅ Проверка подключения

После перезапуска Cursor:

1. Откройте чат с AI в Cursor
2. Попросите AI:
   - **"Покажи доступные MCP ресурсы"**
   - или
   - **"Создай доску Miro с названием 'Тест подключения'"**

Если AI может:
- ✅ Видеть MCP ресурсы Miro
- ✅ Создавать доски в Miro
- ✅ Добавлять элементы на доски

**Значит всё работает! 🎉**

---

## 🎨 Что дальше?

После успешного подключения мы сможем:

1. **Создать визуальную диаграмму дерева профилей** в Miro
2. **Добавить все категории и подтипы** с детальными блоками
3. **Создать связи между элементами**
4. **Экспортировать для команды**

Можете попросить AI:
```
Создай доску Miro с визуализацией дерева создания профилей:
- Основные категории: Площадка, Аниматор, Шоу, Агентство, Квест, Мастер-класс, Фото/Видео
- Подтипы для каждой категории
- Блоки характеристик для каждого подтипа
- Связи между элементами
```

---

## 🆘 Если что-то не работает

### MCP инструменты не появляются

1. Проверьте путь к файлу `mcp.json`
2. Убедитесь, что JSON валидный (можно проверить на jsonlint.com)
3. Проверьте, что токен скопирован полностью (без пробелов)
4. Полностью перезапустите Cursor
5. Проверьте, что файл сохранен с расширением `.json` (не `.txt`)

### Ошибка доступа к Miro

1. Проверьте, что токен правильный и не истек
2. Убедитесь, что у приложения есть все необходимые разрешения в Miro
3. Проверьте интернет-соединение

### Файл не создается

1. Убедитесь, что у вас есть права на запись в папку
2. Попробуйте создать файл от имени администратора
3. Проверьте, что папка `globalStorage` существует

---

## 📍 Быстрый путь к файлу

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\mcp.json
```

**Путь напрямую (замените USERNAME на ваше имя пользователя):**
```
C:\Users\USERNAME\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

---

Готово! После создания файла и перезапуска Cursor, MCP Miro будет подключен! 🚀

