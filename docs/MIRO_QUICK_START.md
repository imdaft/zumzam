# Быстрый старт: Miro + MCP за 5 минут

## 🎯 Что нужно сделать (кратко)

1. ✅ Нажать **"Create your first app"** на странице Developer Portal
2. ✅ Создать приложение (выбрать REST API)
3. ✅ Включить разрешения
4. ✅ Получить OAuth токен
5. ✅ Добавить токен в конфиг Cursor
6. ✅ Перезапустить Cursor

---

## 📍 Точные места для кликов

### Шаг 1: Создание приложения

**Где кликать:**
- Раздел **"Your apps"**
- Кнопка **"Create your first app"** (большая кнопка в центре)

**Что выбрать:**
- **App type**: `REST API` ⚠️ (важно!)
- **App name**: любое (например, "ZumZam MCP")
- **Team**: ваша команда

### Шаг 2: Разрешения

После создания приложения:

1. В левом меню найдите **"Permissions"** (или "App permissions")
2. Включите галочки:
   - ✅ boards:read
   - ✅ boards:write
   - ✅ identity:read
   - ✅ identity:write
   - ✅ team:read
   - ✅ team:write
3. Нажмите **"Save"**

### Шаг 3: OAuth токен

1. В левом меню найдите **"Install app and get OAuth token"**
2. Выберите команду (team) из выпадающего списка
3. Нажмите **"Generate token"** или **"Get OAuth token"**
4. **Скопируйте токен** (показывается только один раз!)

---

## ⚙️ Настройка Cursor

### Файл конфигурации

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\mcp.json
```

Или через Проводник:
```
C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/mcp.json
```

### Содержимое файла

```json
{
  "mcpServers": {
    "miro": {
      "command": "npx",
      "args": ["-y", "@k-jarzyna/mcp-miro"],
      "env": {
        "MIRO_ACCESS_TOKEN": "ВСТАВЬТЕ_СЮДА_ВАШ_ТОКЕН"
      }
    }
  }
}
```

---

## ✅ Проверка

После перезапуска Cursor спросите AI:
- "Покажи доступные MCP ресурсы"
- "Создай доску Miro с названием 'Тест'"

Если AI может создавать доски - всё работает! 🎉

---

## 🆘 Если что-то не работает

1. **Не вижу кнопку "Create your first app"**
   → Попробуйте: https://developers.miro.com/apps

2. **Токен не работает**
   → Проверьте, что все разрешения включены
   → Убедитесь, что токен скопирован полностью

3. **MCP инструменты не появляются**
   → Проверьте путь к файлу mcp.json
   → Полностью перезапустите Cursor
   → Проверьте, что JSON валидный

---

**Подробная инструкция:** `docs/MIRO_SETUP_STEP_BY_STEP.md`

