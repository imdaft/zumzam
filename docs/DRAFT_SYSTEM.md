# Система черновиков для создания профилей

## 📝 Описание

Реализована система черновиков, которая автоматически сохраняет прогресс создания профиля в `localStorage` и позволяет пользователю:
- Продолжить незавершённое создание профиля
- Создать несколько черновиков параллельно
- Удалить ненужные черновики
- Выбрать между продолжением черновика или созданием нового профиля

## 🗂️ Структура данных

### ProfileDraft (интерфейс)
```typescript
{
  id: string                    // Уникальный ID черновика
  category: ProfileCategory     // Тип профиля (venue, animator, etc.)
  display_name: string          // Название профиля
  currentStep: number           // Текущий шаг в wizard (1-5)
  totalSteps: number            // Общее количество шагов
  data: {                       // Все данные формы
    display_name?: string
    bio?: string
    city?: string
    address?: string
    geo_location?: [number, number] | null
    logo?: string | null
    cover_photo?: string | null
    details?: any
    photos?: string[]
    videos?: string[]
    [key: string]: any
  }
  createdAt: number             // Timestamp создания
  updatedAt: number             // Timestamp последнего обновления
}
```

### localStorage
Ключ: `zumzam_profile_drafts`

Значение: массив объектов `ProfileDraft[]`

## 🔧 Компоненты

### 1. `hooks/use-profile-drafts.ts`
**Хук для работы с черновиками**

Методы:
- `saveDraft(draft)` - сохранить или обновить черновик
- `getDraft(id)` - получить черновик по ID
- `getAllDrafts()` - получить все черновики (сортировка по дате обновления)
- `deleteDraft(id)` - удалить черновик
- `clearAllDrafts()` - удалить все черновики
- `getLatestDraft()` - получить последний обновлённый черновик
- `hasDrafts()` - проверить наличие черновиков

### 2. `components/features/profile/create-profile-wizard.tsx`
**Wizard с автосохранением**

Изменения:
- Добавлен параметр URL `?draft=ID` для загрузки черновика
- Автосохранение при каждом изменении `step` или `profileData`
- Индикатор сохранения в header (✓ Сохранено / Сохранение...)
- Удаление черновика после успешного создания профиля

### 3. `components/features/profile/profile-drafts-list.tsx`
**Список черновиков на странице `/profiles`**

Показывает:
- Название черновика
- Тип профиля (категория)
- Прогресс (Шаг X из Y)
- Время последнего обновления
- Кнопки "Продолжить" и "Удалить"
- Progress bar (визуальный прогресс)

### 4. `components/features/profile/draft-selection-dialog.tsx`
**Диалог выбора при наличии черновиков**

Появляется при клике "Создать профиль", если есть активные черновики.

Опции:
- Продолжить один из черновиков (до 3 штук показывается)
- Создать новый профиль
- Отмена

### 5. `app/(dashboard)/profiles/page.tsx`
**Страница списка профилей**

Изменения:
- Добавлен компонент `<ProfileDraftsList />` в начале страницы
- Кнопка "Создать профиль" проверяет наличие черновиков
- При наличии черновиков показывает диалог выбора

## 🎨 UI/UX

### Визуальные элементы
1. **Черновики** - оранжевый градиент (`from-orange-50 to-orange-100/50`)
2. **Индикатор сохранения** - зелёная галочка или спиннер
3. **Progress bar** - оранжевый заполненный бар
4. **Диалог** - модальное окно с карточками черновиков

### Мобильная адаптация
- Компактные карточки черновиков
- Responsive layout для диалога
- Touch-friendly кнопки

## 🚀 Использование

### Создание нового профиля
1. Пользователь нажимает "Создать профиль"
2. Если есть черновики → показывается диалог выбора
3. Если черновиков нет → переход на `/profiles/create`

### Продолжение черновика
1. Пользователь нажимает "Продолжить" на карточке черновика
2. Переход на `/profiles/create?draft=ID`
3. Wizard загружает данные и шаг из черновика
4. Продолжение с места остановки

### Автосохранение
- Срабатывает при каждом изменении шага или данных
- Задержка 500мс для показа анимации
- Не блокирует UI

### Удаление черновика
1. Кнопка "Удалить" на карточке черновика
2. Диалог подтверждения
3. Удаление из localStorage

### После создания профиля
- Черновик автоматически удаляется
- Редирект на страницу редактирования

## 📋 Логика работы

```
┌─────────────────────────────────────────┐
│  Пользователь на /profiles              │
└─────────────────────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────┐
    │ Есть черновики?            │
    └────────────────────────────┘
         │ Да            │ Нет
         ▼               ▼
┌──────────────────┐  ┌────────────────────┐
│ Показать список  │  │ Кнопка "Создать"   │
│ черновиков       │  │ → /profiles/create │
└──────────────────┘  └────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Клик "Создать профиль"       │
└──────────────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Диалог выбора:               │
│ - Продолжить черновик        │
│ - Создать новый              │
└──────────────────────────────┘
    │              │
    ▼              ▼
┌─────────┐  ┌──────────────┐
│ ?draft= │  │ /create      │
│ ID      │  │ (новый)      │
└─────────┘  └──────────────┘
    │              │
    └──────┬───────┘
           ▼
  ┌──────────────────────┐
  │ Wizard с             │
  │ автосохранением      │
  └──────────────────────┘
           │
           ▼
  ┌──────────────────────┐
  │ Создание профиля     │
  │ (API /profiles)      │
  └──────────────────────┘
           │
           ▼
  ┌──────────────────────┐
  │ Удалить черновик     │
  │ Редирект на /edit    │
  └──────────────────────┘
```

## ⚠️ Ограничения

1. **localStorage** - максимум ~5-10MB (зависит от браузера)
2. **Безопасность** - данные хранятся на клиенте (не критично для черновиков)
3. **Синхронизация** - нет синхронизации между устройствами
4. **Приватный режим** - может не работать в некоторых браузерах

## 🔮 Возможные улучшения

1. **Серверное хранение** черновиков для синхронизации
2. **TTL для черновиков** - автоудаление старых (например, > 30 дней)
3. **Восстановление из crashed wizard** - при обновлении страницы
4. **Diff-показ** изменений между черновиками
5. **Экспорт/импорт** черновиков

## 📚 Связанные файлы

- `hooks/use-profile-drafts.ts`
- `components/features/profile/create-profile-wizard.tsx`
- `components/features/profile/profile-drafts-list.tsx`
- `components/features/profile/draft-selection-dialog.tsx`
- `app/(dashboard)/profiles/page.tsx`
- `types/supabase.ts` (для типов `ProfileCategory`)

## ✅ Тестирование

### Сценарии для проверки:
1. ✅ Создать профиль → закрыть на середине → открыть `/profiles` → увидеть черновик
2. ✅ Продолжить черновик → завершить создание → черновик удалён
3. ✅ Создать 2+ черновика → показывается диалог выбора
4. ✅ Удалить черновик → подтверждение → удаление
5. ✅ Индикатор "Сохранено" появляется после изменений
6. ✅ Мобильная версия работает корректно
7. ✅ Progress bar заполняется по мере прохождения шагов

---

**Дата создания:** 7 декабря 2025  
**Автор:** AI Assistant (Claude Sonnet 4.5)  
**Версия:** 1.0

















