# ✅ Чек-лист развертывания расширенных площадок

## 📋 Предварительная подготовка

- [ ] Проект ZumZam локально склонирован
- [ ] Node.js установлен (v18+)
- [ ] npm установлен
- [ ] Есть доступ к Supabase Dashboard
- [ ] Есть права на выполнение SQL миграций

---

## 🔧 Шаг 1: Проверка файлов

### Новые файлы (должны быть созданы):

- [ ] `types/venue-details.ts`
- [ ] `components/features/profile/subtype-selection-step.tsx`
- [ ] `components/features/profile/wizard-steps/venue-types/base-venue.tsx`
- [ ] `supabase/migrations/20241208_add_venue_types.sql`
- [ ] `supabase/test-data/venue_examples.sql`
- [ ] `scripts/deploy-venue-types.sh`
- [ ] `scripts/deploy-venue-types.ps1`

### Обновленные файлы (должны быть изменены):

- [ ] `lib/constants/index.ts` - VENUE_TYPES
- [ ] `lib/constants/profile-categories.ts` - venue подтипы
- [ ] `lib/ai/generate-rich-embedding-text.ts` - embeddings
- [ ] `components/features/profile/wizard-steps/characteristics.tsx` - роутинг
- [ ] `components/features/profile/wizard-steps/venue-types/index.ts` - экспорт

### Документация (должна быть создана):

- [ ] `docs/QUICKSTART.md`
- [ ] `docs/DEPLOYMENT_GUIDE.md`
- [ ] `docs/IMPLEMENTATION_STATUS.md`
- [ ] `docs/FINAL_REPORT.md`
- [ ] `docs/VENUE_TYPES_README.md`
- [ ] `docs/COMPLETE_MARKET_ANALYSIS.md`
- [ ] `docs/DETAILED_PROFILE_BLOCKS.md`

---

## 🗄️ Шаг 2: SQL миграция

### Применение миграции:

- [ ] Открыт Supabase Dashboard
- [ ] Проект ZumZam выбран
- [ ] SQL Editor открыт
- [ ] Файл `supabase/migrations/20241208_add_venue_types.sql` скопирован
- [ ] Миграция выполнена (Run / Ctrl+Enter)
- [ ] Успешное сообщение получено

### Проверка миграции:

```sql
-- Выполните эти запросы для проверки
```

- [ ] Проверка enum values:
```sql
SELECT unnest(enum_range(NULL::venue_subtype));
-- Должно вернуть 33 значения
```

- [ ] Проверка функции:
```sql
SELECT get_venue_type_label('trampoline_park');
-- Должно вернуть: "Батутный центр"
```

- [ ] Проверка view:
```sql
SELECT * FROM venue_types_stats;
-- Должна вернуться таблица со статистикой
```

- [ ] Проверка индекса:
```sql
SELECT indexname FROM pg_indexes 
WHERE indexname = 'idx_profiles_venue_subtype';
-- Должен найтись индекс
```

---

## 💻 Шаг 3: Локальная разработка

### Установка и сборка:

- [ ] `npm install` выполнен успешно
- [ ] `npm run build` выполнен без ошибок
- [ ] `npm run lint` не показывает критических ошибок

### Запуск dev сервера:

- [ ] `npm run dev` запущен
- [ ] Сервер доступен на http://localhost:3000
- [ ] Нет ошибок в консоли терминала

---

## 🎨 Шаг 4: UI тестирование

### Компонент выбора подтипа:

- [ ] Открыт http://localhost:3000/create-profile
- [ ] Выбрана категория "Площадка"
- [ ] Отображаются 33 подтипа
- [ ] Подтипы разбиты на 9 групп
- [ ] Поиск работает корректно
- [ ] Клик по подтипу переходит к следующему шагу

### Форма характеристик:

- [ ] Выбран любой новый тип (например, "Батутный центр")
- [ ] Форма загружается без ошибок
- [ ] Отображаются все блоки полей
- [ ] Условная логика работает (например, при выборе "Кухня" появляется "Тип кухни")
- [ ] Кнопка "Далее" работает
- [ ] Кнопка "Пропустить" работает

---

## 🧪 Шаг 5: Функциональное тестирование

### Создание профиля:

- [ ] Создан тестовый профиль нового типа
- [ ] Все шаги wizard пройдены успешно
- [ ] Профиль сохранен без ошибок
- [ ] Получено подтверждение создания

### Проверка в БД:

```sql
-- Проверьте последний профиль
SELECT 
  display_name,
  category,
  details->>'subtype' as subtype,
  get_venue_type_label(details->>'subtype') as type_label,
  details
FROM profiles
WHERE category = 'venue'
ORDER BY created_at DESC
LIMIT 1;
```

- [ ] Профиль найден в БД
- [ ] Поле `details->>'subtype'` содержит правильное значение
- [ ] Функция `get_venue_type_label()` возвращает русское название
- [ ] Все заполненные поля сохранены в `details`

### Проверка embeddings:

- [ ] Подождать ~30 секунд после создания профиля
- [ ] Выполнить поиск по характеристикам профиля
- [ ] Профиль должен найтись в результатах

```sql
-- Проверьте что embedding создан
SELECT 
  display_name,
  embedding IS NOT NULL as has_embedding
FROM profiles
WHERE category = 'venue'
ORDER BY created_at DESC
LIMIT 5;
```

- [ ] Поле `embedding` не NULL для нового профиля

---

## 🔍 Шаг 6: Тестирование поиска

### Создать тестовые данные:

```sql
-- Выполните в SQL Editor
\i supabase/test-data/venue_examples.sql
```

- [ ] Тестовые данные загружены
- [ ] Создано 6 тестовых профилей

### Проверить поиск:

- [ ] Поиск "батутный центр" находит батутный парк
- [ ] Поиск "VR игры" находит VR-арену
- [ ] Поиск "аквапарк горки" находит аквапарк
- [ ] Поиск "картинг для детей" находит картинг-центр
- [ ] Фильтр по типу площадки работает
- [ ] Фильтр по городу работает

---

## 📊 Шаг 7: Проверка статистики

### SQL запросы:

```sql
-- Статистика по типам
SELECT * FROM venue_types_stats;
```

- [ ] View возвращает данные
- [ ] Видны все типы площадок
- [ ] Счетчики корректны

```sql
-- Распределение по типам
SELECT 
  get_venue_type_label(details->>'subtype') as type_ru,
  COUNT(*) as count
FROM profiles
WHERE category = 'venue'
GROUP BY details->>'subtype'
ORDER BY count DESC;
```

- [ ] Запрос выполняется успешно
- [ ] Русские названия отображаются корректно

---

## 🚀 Шаг 8: Production готовность

### Код:

- [ ] Нет линтер ошибок
- [ ] Нет TypeScript ошибок
- [ ] Build проходит успешно
- [ ] Нет console.log в production коде

### База данных:

- [ ] SQL миграция задокументирована
- [ ] Индексы созданы
- [ ] Views работают
- [ ] Функции протестированы

### Документация:

- [ ] README обновлен
- [ ] Все docs файлы созданы
- [ ] Инструкции понятны
- [ ] Примеры актуальны

---

## ✅ Финальная проверка

### Базовая функциональность:

- [ ] Создание профиля площадки работает для всех 33 типов
- [ ] Поиск учитывает новые характеристики
- [ ] Старые профили продолжают работать (обратная совместимость)
- [ ] UI отзывчивый и без багов

### Производительность:

- [ ] Страница загружается быстро
- [ ] Нет задержек при выборе подтипа
- [ ] Форма характеристик рендерится мгновенно
- [ ] Поиск работает без лагов

### Качество данных:

- [ ] Все поля правильно типизированы
- [ ] Валидация работает корректно
- [ ] Embeddings генерируются с полной информацией
- [ ] SQL функции возвращают правильные значения

---

## 🎉 Готово!

Если все пункты отмечены ✅, то расширенная структура площадок успешно развернута и готова к использованию!

### Следующие шаги:

1. **Мониторинг:** Отслеживайте создание профилей и качество поиска
2. **Обратная связь:** Собирайте feedback от пользователей
3. **Оптимизация:** Улучшайте на основе реальных данных

### Проблемы?

Если что-то не работает:
1. Проверьте логи Supabase
2. Проверьте консоль браузера (F12)
3. Проверьте терминал dev сервера
4. См. `docs/DEPLOYMENT_GUIDE.md` раздел "Решение проблем"

---

**📅 Дата проверки:** __________  
**👤 Проверил:** __________  
**✅ Статус:** __________

