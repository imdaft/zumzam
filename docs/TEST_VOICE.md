# 🎙️ Тестирование голосового ввода

## ⚠️ ВАЖНО: Сервер должен быть перезапущен!

После изменений в `next.config.ts` **обязательно**:

1. Остановите сервер (**Ctrl+C**)
2. Запустите заново: `npm run dev`

---

## 🧪 Как протестировать:

### 1. Откройте сайт
```
http://localhost:4000
```

### 2. Откройте AI чат
- Нажмите оранжевую кнопку AI в центре нижнего меню

### 3. Нажмите кнопку микрофона 🎤
- Браузер должен запросить доступ → **Разрешите**

### 4. Говорите
- Скажите что-то по-русски (например: "Помоги организовать день рождения")
- Следите за таймером

### 5. Нажмите "Стоп" ⏹
- Подождите 5-10 секунд (модель загружается первый раз)
- Индикатор покажет "Распознаю речь..."

---

## 📋 Что проверить в логах сервера:

### ✅ Успешная работа:
```
[Whisper API] Request received
[Whisper API] Audio file: { name: 'recording.webm', size: 93998, type: 'audio/webm' }
[Whisper API] Loading transformers...
[Whisper API] Transformers loaded!
[Whisper API] Loading Whisper model...
[Whisper API] Model loaded successfully!
[Whisper API] Reading audio buffer...
[Whisper API] Buffer ready, size: 93998
[Whisper API] Starting transcription...
[Whisper API] Transcription successful: помоги организовать день рождения
```

### ❌ Если ошибка:
```
[Whisper API] Import error: ...
[Whisper API] Model loading error: ...
[Whisper API] Transcription error: ...
```

**Скопируй полный лог ошибки и отправь мне!**

---

## 🐛 Возможные проблемы:

### 1. Ошибка импорта transformers
**Причина:** Next.js не может найти модуль  
**Решение:** Убедись что установлен `@xenova/transformers`:
```bash
npm install @xenova/transformers --legacy-peer-deps
```

### 2. Ошибка загрузки модели
**Причина:** Нет интернета или модель не скачивается  
**Решение:** При первом запросе модель скачивается (~75MB). Подожди 30-60 сек.

### 3. Ошибка транскрипции
**Причина:** Неподдерживаемый формат аудио  
**Решение:** Проверь что браузер записывает в `audio/webm`

---

## 📞 Если не работает:

1. **Перезапусти сервер** полностью (закрой терминал, открой новый)
2. **Очисти кеш Next.js**: удали папку `.next`
3. **Проверь логи сервера** - там будет точная ошибка
4. **Отправь мне логи** - я исправлю!

---

🚀 **Удачи с тестированием!**















