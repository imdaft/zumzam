# Спецификация профиля "Мастер-класс" (master_class)

## 🎨 Структура блоков

### **Уникальные блоки:**

#### 1. **"Виды мастер-классов"** 
Аналог "Персонажей" у аниматора. Основной блок с МК.

**Характеристики МК:**
- Название МК (обязательно)
- Категория (мультивыбор):
  - `creative` - Творчество
  - `cooking` - Кулинария
  - `science` - Наука и эксперименты
  - `handmade` - Рукоделие
  - `art` - Рисование и живопись
  - `modeling` - Лепка и моделирование
  - `slime` - Слаймы
  - `soap` - Мыловарение
  - `candles` - Свечи
  - `jewelry` - Украшения
  - `origami` - Оригами
  - `decor` - Декор
  - `cosmetics` - Косметика
  - `other` - Другое
- Длительность (минуты): 30, 45, 60, 90, 120
- Возраст участников (мультивыбор): 3-5, 5-8, 8-12, 12-16, взрослые
- Минимум участников: 1-30
- Максимум участников: 1-50
- Описание процесса (с AI-генератором)
- Фото готовых работ/Видео

**Включено в МК:**
- Материалы включены (boolean)
- Список материалов (массив строк)
- Готовая поделка с собой (boolean)
- Сертификат участника (boolean)

#### 2. **"Характеристики мастера"**
Блок с характеристиками преподавателя.

**Поля:**
- Опыт работы (лет): 1-3, 3-5, 5-10, 10+
- Образование/Квалификация (текст)
- Формат работы:
  - `mobile` - Только выездной
  - `studio` - Только в студии
  - `both` - Выездной и в студии
- Радиус выезда (км)
- География работы
- Количество проведенных МК (число)
- Собственные материалы (boolean)

---

### **Стандартные блоки:**

1. **О нас** - с AI-генератором
2. **Фото и видео** - примеры работ участников (с 3 вариантами)
3. **Услуги** - МК (основные)
4. **Доп. услуги** - материалы премиум, упаковка, доставка готовых работ
5. **Отзывы**
6. **FAQ**
7. **Контакты** - география работы (БЕЗ карты для выездных)

---

## 🗄️ База данных

### Таблица: `master_class_programs`

```sql
CREATE TABLE public.master_class_programs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profile_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  
  -- Основная информация
  name TEXT NOT NULL,
  description TEXT,
  
  -- Медиа
  photos TEXT[] DEFAULT '{}', -- фото готовых работ
  video_url TEXT,
  
  -- Характеристики
  categories TEXT[] DEFAULT '{}',
  duration INTEGER, -- минуты
  age_ranges TEXT[] DEFAULT '{}',
  min_participants INTEGER,
  max_participants INTEGER,
  
  -- Включено в МК
  materials_included BOOLEAN DEFAULT true,
  materials_list TEXT[], -- список материалов
  take_home BOOLEAN DEFAULT true, -- забирают поделку
  certificate BOOLEAN DEFAULT false, -- сертификат
  
  -- Метаданные
  is_active BOOLEAN DEFAULT true,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

### Обновление: `profile_details` для master_class

```json
{
  "experience_years": "5-10",
  "education": "Педагогическое образование, курсы...",
  "work_format": "both",
  "coverage_radius": 30,
  "total_classes_conducted": 500,
  "has_own_materials": true
}
```

---

## 🎨 Компоненты

```
components/features/master-class/
├── master-class-programs-section.tsx   # Публичный блок МК
├── master-class-characteristics.tsx    # Характеристики мастера
├── master-class-programs-manager.tsx   # Управление МК
└── master-class-program-form.tsx       # Форма создания/редактирования
```

---

## 🎯 Интеграция

### Меню для master_class:
1. О нас
2. **Виды мастер-классов** ⭐
3. Фото и видео
4. Услуги
5. Доп. услуги
6. Отзывы
7. FAQ
8. Контакты

### Корзина:
- ✅ Есть корзина
- Кнопка "Заказать" на карточках МК





