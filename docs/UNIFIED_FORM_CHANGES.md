# Объединение форм создания и редактирования профиля

## Проблема

Ранее существовало две независимые системы:
1. **CreateProfileWizard** - пошаговый визард для создания профиля
2. **ProfileManageClient** - полная форма для редактирования

Это приводило к проблемам:
- 🔴 Рассинхронизация данных между созданием и редактированием
- 🔴 Дублирование кода и логики
- 🔴 Сложность поддержки (нужно обновлять оба места)
- 🔴 Некоторые поля из wizard не переносились в edit форму

## Решение

Объединили обе формы в **единую систему ProfileManageClient**, которая работает в двух режимах:
- `isCreating={true}` - режим создания нового профиля
- `isCreating={false}` - режим редактирования существующего

## Изменения в коде

### 1. ProfileManageClient (`app/(dashboard)/profiles/[slug]/edit/client.tsx`)

**Интерфейс:**
```typescript
interface ProfileManageClientProps {
  profile?: any        // Опционально для режима создания
  isCreating?: boolean // Флаг режима создания
}
```

**Логика:**
- В режиме создания показывается только вкладка "Данные"
- Остальные вкладки (Услуги, Фото, Юр.документы) появляются после создания профиля
- Виджет готовности профиля не показывается при создании
- Если `profile` не передан, используется пустой объект

### 2. Страница создания (`app/(dashboard)/profiles/create/page.tsx`)

Теперь просто рендерит `ProfileManageClient` в режиме создания:

```typescript
export default function CreateProfilePage() {
  return <ProfileManageClient isCreating={true} />
}
```

### 3. Backup файлы

Старые wizard компоненты переименованы в `.backup`:
- `create-profile-wizard.tsx.backup`
- `wizard-progress.tsx.backup`
- `draft-continue-dialog.tsx.backup`

## Автосохранение

Черновики продолжают работать через `CreateProfileForm`:
- Сохранение в `sessionStorage` при изменении полей
- Автоматическое восстановление при возврате
- Ключ: `profile-form-create-${userId}`

## Преимущества новой версии

✅ **Единый источник правды** - одна форма для всего  
✅ **Полная синхронизация** - все поля всегда совпадают  
✅ **Проще поддержка** - изменения в одном месте  
✅ **Все функции доступны** - не нужно ждать перехода в edit  
✅ **Быстрее разработка** - меньше кода для поддержки  

## Недостатки

❌ **Нет пошагового wizard** - может быть сложнее для новых пользователей  
❌ **Много полей сразу** - может быть перегружено визуально  
❌ **Потеря мобильной оптимизации** - wizard был лучше адаптирован под мобильные  

## Возможные улучшения

1. **Добавить аккордеоны** - сворачивать/разворачивать секции
2. **Прогресс бар** - показывать заполненность профиля
3. **Подсказки** - направлять пользователя что заполнять
4. **Валидация в реальном времени** - показывать ошибки сразу

## Откат

Если новая версия не подойдёт, см. инструкцию: `docs/ROLLBACK_GUIDE.md`

## Тестирование

Проверить:
- [ ] Создание нового профиля с нуля
- [ ] Автосохранение черновика при создании
- [ ] Восстановление черновика при возврате
- [ ] Редактирование существующего профиля
- [ ] Переход к вкладкам (Услуги, Фото) после создания
- [ ] Все поля корректно сохраняются
- [ ] Удобство использования на мобильных устройствах

