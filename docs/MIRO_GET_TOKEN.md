# Как получить OAuth токен в Miro

## Текущая ситуация

✅ Приложение создано: "ZumZam Profile Wizard"  
✅ Client ID получен: `3458764651454344052`  
✅ Client Secret получен: `690vYcW3tb7g90qbgLC4hsoIfOZYN25C`  
⏳ Нужно получить OAuth токен

---

## Шаг 1: Проверка разрешений

На странице настроек приложения:

1. Найдите раздел **"Permissions"**
2. Убедитесь, что включены (стоят галочки) следующие разрешения:

   - ✅ **boards:read** - Read boards you have access to
   - ✅ **boards:write** - Modify boards you have access to
   - ✅ **identity:read** - Read profile information for current user
   - ✅ **identity:write** - Modify profile information for current user
   - ✅ **team:read** - Read current team information
   - ✅ **team:write** - Modify current team title, invite users, change users' roles

3. Если какие-то разрешения не включены - включите их
4. Нажмите **"Save"** (если была кнопка сохранения)

---

## Шаг 2: Получение OAuth токена

### 2.1. Найдите раздел "Install app and get OAuth token"

На странице настроек приложения прокрутите вниз до раздела:

**"Install app and get OAuth token"**

Этот раздел находится после раздела "Permissions".

### 2.2. Выберите команду (Team)

В разделе "Install app and get OAuth token" вы увидите:

- Выпадающий список или поле для выбора команды (Team)
- Возможно, будет написано: **"Boards Picker"** или **"Select team"**

**👉 Выберите вашу команду Miro** из списка

### 2.3. Нажмите кнопку для получения токена

После выбора команды найдите кнопку:

- **"Get OAuth token"** или
- **"Generate token"** или
- **"Install app"** или
- **"Authorize"**

**👉 Нажмите на эту кнопку**

### 2.4. Скопируйте токен

После нажатия:

1. Откроется окно или появится текст с токеном
2. Токен будет выглядеть примерно так:
   ```
   pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   или
   ```
   xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

3. **⚠️ ВАЖНО: Токен показывается только один раз!**

4. **👉 Скопируйте весь токен** (Ctrl+C / Cmd+C)

5. **Сохраните токен в безопасном месте** (например, в блокнот или файл)

---

## Шаг 3: Настройка MCP в Cursor

### 3.1. Найдите файл конфигурации

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\mcp.json
```

Или через Проводник:
```
C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Roaming\Cursor\User\globalStorage\mcp.json
```

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/mcp.json
```

### 3.2. Создайте или отредактируйте файл

Откройте файл `mcp.json` в текстовом редакторе (или создайте, если его нет).

### 3.3. Добавьте конфигурацию

Вставьте или замените содержимое файла:

```json
{
  "mcpServers": {
    "miro": {
      "command": "npx",
      "args": [
        "-y",
        "@k-jarzyna/mcp-miro"
      ],
      "env": {
        "MIRO_ACCESS_TOKEN": "ВСТАВЬТЕ_СЮДА_ВАШ_ТОКЕН"
      }
    }
  }
}
```

**👉 Замените `ВСТАВЬТЕ_СЮДА_ВАШ_ТОКЕН` на токен, который вы скопировали в шаге 2.4**

### 3.4. Сохраните файл

Сохраните файл `mcp.json` (Ctrl+S / Cmd+S)

---

## Шаг 4: Перезапуск Cursor

1. **Полностью закройте Cursor** (все окна)
2. **Откройте Cursor заново**

---

## Шаг 5: Проверка

После перезапуска Cursor:

1. Откройте чат с AI
2. Попросите: **"Покажи доступные MCP ресурсы"**
3. Или попросите: **"Создай доску Miro с названием 'Тест'"**

Если AI может создавать доски - всё работает! 🎉

---

## Устранение проблем

### Проблема: Не вижу раздел "Install app and get OAuth token"

**Решение:**
- Прокрутите страницу вниз
- Ищите раздел после "Permissions"
- Возможно, он называется "OAuth" или "Authorization"
- Попробуйте обновить страницу (F5)

### Проблема: Не могу выбрать команду

**Решение:**
- Убедитесь, что вы состоите в команде Miro
- Попробуйте создать новую команду в Miro
- Обновите страницу

### Проблема: Токен не появляется после нажатия

**Решение:**
- Проверьте, что все разрешения включены
- Убедитесь, что вы выбрали команду
- Попробуйте обновить страницу и повторить
- Проверьте консоль браузера на ошибки (F12)

### Проблема: Токен не работает в Cursor

**Решение:**
- Убедитесь, что токен скопирован полностью (без пробелов)
- Проверьте, что JSON файл валидный (можно проверить на jsonlint.com)
- Убедитесь, что токен в кавычках в JSON
- Полностью перезапустите Cursor

---

## Альтернативный способ: Использование Client ID и Client Secret

Если получение OAuth токена через интерфейс не работает, можно использовать OAuth 2.0 flow:

1. Используйте Client ID: `3458764651454344052`
2. Используйте Client Secret: `690vYcW3tb7g90qbgLC4hsoIfOZYN25C`
3. Настройте Redirect URI (в разделе "Redirect URI for OAuth2.0")
4. Выполните OAuth 2.0 authorization code flow

Но для MCP проще использовать токен через "Install app and get OAuth token".

---

## Готово! 🎉

После выполнения всех шагов MCP Miro будет подключен, и вы сможете создавать доски и визуализировать дерево профилей!

