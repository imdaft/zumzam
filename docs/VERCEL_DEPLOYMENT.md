# Деплой ZumZam на Vercel

## Шаг 1: Подготовка проекта

### 1.1. Убедитесь, что проект готов к деплою

```bash
# Проверьте, что проект собирается
npm run build

# Если есть ошибки - исправьте их
npm run lint
```

### 1.2. Закоммитьте изменения в Git

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push
```

## Шаг 2: Создание проекта на Vercel

### 2.1. Войдите в Vercel

1. Перейдите на https://vercel.com
2. Войдите через GitHub/GitLab/Bitbucket (или создайте аккаунт)

### 2.2. Создайте новый проект

1. Нажмите **"Add New..."** → **"Project"**
2. Импортируйте ваш репозиторий ZumZam
3. Vercel автоматически определит Next.js

### 2.3. Настройки проекта

**Framework Preset:** Next.js (автоматически)

**Root Directory:** `./` (корень проекта)

**Build Command:** `npm run build` (по умолчанию)

**Output Directory:** `.next` (по умолчанию)

**Install Command:** `npm install --legacy-peer-deps`

## Шаг 3: Настройка переменных окружения

### 3.1. Откройте настройки проекта

В проекте Vercel → **Settings** → **Environment Variables**

### 3.2. Добавьте обязательные переменные

#### База данных (PostgreSQL)

```
DATABASE_URL=postgresql://zumzam_admin:ВАШ_ПАРОЛЬ@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
```

**Важно:** Замените `ВАШ_ПАРОЛЬ` на реальный пароль из Яндекс.Облака

#### AI (Google Gemini)

```
GOOGLE_GEMINI_API_KEY=ваш_gemini_api_key
```

или

```
GEMINI_API_KEY=ваш_gemini_api_key
```

#### JWT Secret (для аутентификации)

```
JWT_SECRET=ваш_случайный_секретный_ключ_минимум_32_символа
```

**Генерация секрета:**
```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
-join ((1..32) | ForEach-Object { Get-Random -Maximum 256 | ForEach-Object { '{0:X2}' -f $_ } })
```

#### Сайт URL (опционально, но рекомендуется)

```
NEXT_PUBLIC_SITE_URL=https://ваш-домен.vercel.app
```

### 3.3. Настройка окружений

Для каждой переменной выберите окружения:
- ✅ **Production** (обязательно)
- ✅ **Preview** (рекомендуется)
- ✅ **Development** (опционально)

## Шаг 4: Настройка Prisma для Vercel

### 4.1. Добавьте postinstall скрипт

В `package.json` добавьте:

```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

### 4.2. Создайте `vercel.json` (опционально)

Если нужны специальные настройки:

```json
{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  }
}
```

## Шаг 5: Деплой

### 5.1. Первый деплой

1. Нажмите **"Deploy"** в Vercel
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. Проверьте логи, если есть ошибки

### 5.2. Применение миграций БД

После первого деплоя нужно применить миграции:

**Вариант 1: Через Vercel CLI (рекомендуется)**

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Примените миграции
vercel env pull .env.local
npx prisma migrate deploy
```

**Вариант 2: Через API Route**

Создайте временный API endpoint для применения миграций (только для первого раза):

```typescript
// app/api/migrate/route.ts (ВРЕМЕННЫЙ ФАЙЛ - удалите после использования!)
import { NextResponse } from 'next/server'
import { exec } from 'child_process'
import { promisify } from 'util'

const execAsync = promisify(exec)

export async function POST(request: Request) {
  // Защита: только с секретным ключом
  const authHeader = request.headers.get('authorization')
  if (authHeader !== `Bearer ${process.env.MIGRATION_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  try {
    const { stdout, stderr } = await execAsync('npx prisma migrate deploy')
    return NextResponse.json({ 
      success: true, 
      output: stdout,
      error: stderr 
    })
  } catch (error: any) {
    return NextResponse.json({ 
      error: error.message,
      output: error.stdout,
      stderr: error.stderr 
    }, { status: 500 })
  }
}
```

Добавьте в Vercel:
```
MIGRATION_SECRET=ваш_секретный_ключ
```

Вызовите:
```bash
curl -X POST https://ваш-домен.vercel.app/api/migrate \
  -H "Authorization: Bearer ваш_секретный_ключ"
```

**Важно:** Удалите этот endpoint после применения миграций!

## Шаг 6: Проверка деплоя

### 6.1. Проверьте сайт

1. Откройте URL, который дал Vercel: `https://ваш-проект.vercel.app`
2. Проверьте основные страницы
3. Проверьте API endpoints

### 6.2. Проверьте подключение к БД

Создайте тестовую страницу или используйте существующую:

```typescript
// app/test-db/page.tsx
import prisma from '@/lib/prisma'

export default async function TestDBPage() {
  try {
    const count = await prisma.profiles.count()
    return <div>✅ БД работает! Профилей: {count}</div>
  } catch (error: any) {
    return <div>❌ Ошибка БД: {error.message}</div>
  }
}
```

## Шаг 7: Настройка домена (опционально)

### 7.1. Добавьте домен

1. В проекте Vercel → **Settings** → **Domains**
2. Добавьте ваш домен (например, `zumzam.ru`)
3. Следуйте инструкциям для настройки DNS

### 7.2. Обновите переменные окружения

```
NEXT_PUBLIC_SITE_URL=https://zumzam.ru
```

## Шаг 8: Настройка автоматических деплоев

### 8.1. Автоматический деплой из Git

По умолчанию Vercel автоматически деплоит:
- **Production:** при push в `main`/`master` ветку
- **Preview:** при push в другие ветки или создании PR

### 8.2. Настройка веток

В **Settings** → **Git** можно настроить:
- Production Branch: `main` (по умолчанию)
- Preview Deployments: включены (по умолчанию)

## Частые проблемы и решения

### Проблема 1: Ошибка сборки Prisma

**Решение:**
```bash
# Добавьте в package.json
"postinstall": "prisma generate"
```

### Проблема 2: Ошибка подключения к БД

**Проверьте:**
1. Правильность `DATABASE_URL` в Vercel
2. Доступность БД из интернета (проверьте firewall в Яндекс.Облаке)
3. SSL режим: `?sslmode=require`

### Проблема 3: Ошибка "Module not found"

**Решение:**
- Убедитесь, что все зависимости в `package.json`
- Проверьте `next.config.ts` на правильную настройку `serverExternalPackages`

### Проблема 4: Ошибка JWT

**Решение:**
- Убедитесь, что `JWT_SECRET` установлен в Vercel
- Используйте длинный случайный ключ (минимум 32 символа)

## Мониторинг и логи

### Просмотр логов

1. В проекте Vercel → **Deployments**
2. Выберите нужный деплой
3. Откройте **"Functions"** или **"Build Logs"**

### Мониторинг производительности

Vercel автоматически предоставляет:
- Analytics (в Pro плане)
- Real User Monitoring (RUM)
- Web Vitals

## Обновление проекта

### После изменений в коде

```bash
git add .
git commit -m "Update code"
git push
```

Vercel автоматически задеплоит изменения.

### После изменений в БД

1. Создайте миграцию локально:
```bash
npx prisma migrate dev --name your_migration_name
```

2. Примените на продакшене:
```bash
# Через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy
```

## Стоимость

### Vercel планы

- **Hobby (Free):** 
  - ✅ Бесплатно для личных проектов
  - ✅ 100 GB bandwidth/месяц
  - ✅ Автоматические SSL сертификаты
  - ✅ Preview deployments

- **Pro ($20/месяц):**
  - ✅ Всё из Hobby
  - ✅ Unlimited bandwidth
  - ✅ Analytics
  - ✅ Team collaboration

### Рекомендация

Для старта используйте **Hobby (Free)** план - его достаточно для тестирования.

## Готово! 🎉

Ваш сайт должен быть доступен по адресу:
`https://ваш-проект.vercel.app`

