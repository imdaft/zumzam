# 🚀 Быстрая настройка Resend

## Шаг 1: Получите API ключ

### 1.1 Регистрация
1. Откройте [resend.com](https://resend.com)
2. Нажмите **"Start Building"** или **"Sign Up"**
3. Войдите через GitHub (проще всего) или email

### 1.2 Создание API ключа
1. После входа попадёте в Dashboard
2. Перейдите в **API Keys** (слева в меню)
3. Нажмите **"Create API Key"**
4. Название: `ZumZam Development` (или любое)
5. Permission: **Full Access** (или Sending access)
6. Нажмите **Create**
7. **ВАЖНО:** Скопируйте ключ сразу! Он начинается с `re_...`

---

## Шаг 2: Настройте .env.local

Создайте или откройте файл `.env.local` в корне проекта:

```bash
# Email Provider
EMAIL_PROVIDER="resend"
RESEND_API_KEY="re_ваш_скопированный_ключ"
EMAIL_FROM="ZumZam <noreply@zumzam.ru>"

# App URL (для ссылок в письмах)
NEXT_PUBLIC_APP_URL="http://localhost:4000"
```

**Замените:**
- `re_ваш_скопированный_ключ` на реальный ключ из Resend
- `noreply@zumzam.ru` можно оставить (будет работать) или изменить

---

## Шаг 3: Проверьте работу

### 3.1 Перезапустите dev сервер
```bash
# Остановите текущий (Ctrl+C)
npm run dev
```

### 3.2 Протестируйте восстановление пароля
1. Откройте http://localhost:4000/login
2. Нажмите **"Забыли пароль?"**
3. Введите существующий email из БД
4. Письмо придёт на реальный email!

### 3.3 Проверьте логи в Resend
1. Откройте [resend.com/emails](https://resend.com/emails)
2. Увидите список отправленных писем
3. Можно кликнуть и посмотреть детали

---

## 📊 Лимиты бесплатного тарифа

✅ **3,000 писем в месяц** - более чем достаточно для разработки  
✅ **100 писем в день**  
✅ Все функции доступны  
✅ Без ограничений по времени  

---

## 🎨 (Опционально) Настройка своего домена

Если хотите отправлять с `noreply@zumzam.ru` (а не `onboarding@resend.dev`):

### 1. Добавьте домен в Resend
1. В Dashboard перейдите в **Domains**
2. Нажмите **Add Domain**
3. Введите `zumzam.ru`
4. Resend покажет DNS записи

### 2. Настройте DNS
Добавьте записи в панели управления доменом (например, в Яндекс.Коннект или регистраторе):

**Обычно это:**
- **SPF** (TXT запись)
- **DKIM** (TXT запись)  
- **DMARC** (TXT запись)

Resend покажет точные значения в интерфейсе.

### 3. Дождитесь верификации
Обычно 5-30 минут. Статус домена станет **Verified**.

### 4. Обновите .env.local
```bash
EMAIL_FROM="ZumZam <noreply@zumzam.ru>"
```

---

## ✅ Готово!

Теперь все письма отправляются через Resend:
- ✅ Восстановление пароля
- ✅ Приветственные письма (когда добавим)
- ✅ Уведомления (когда добавим)

---

## 🐛 Troubleshooting

### Письма не отправляются
1. Проверьте `.env.local` - правильный ли ключ?
2. Перезапустили ли dev сервер после изменения `.env.local`?
3. Проверьте консоль - есть ли ошибки?

### Письма в спаме
Это нормально для `@resend.dev` адреса. После настройки домена пройдёт.

### Ошибка "Invalid API key"
- Ключ скопирован полностью (начинается с `re_`)?
- Нет пробелов в начале/конце?
- Ключ не удалён в Resend Dashboard?

### Хочу протестировать без реальной отправки
Удалите `EMAIL_PROVIDER` из `.env.local` - письма будут логироваться в консоль.

---

## 📚 Полезные ссылки

- [Resend Dashboard](https://resend.com/emails)
- [Resend Documentation](https://resend.com/docs)
- [API Reference](https://resend.com/docs/api-reference/introduction)
- [React Email](https://react.email) - для красивых шаблонов (опционально)



