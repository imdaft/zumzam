# Настройка WireGuard: только Google API через VPN

## 🎯 Что мы делаем

Настраиваем WireGuard так, чтобы **только запросы к Google API** шли через VPN в Нидерландах, а весь остальной трафик (Yandex, Supabase, и т.д.) шёл напрямую из России.

---

## 📋 Шаг 1: Подготовка на VPN сервере (Нидерланды)

### 1.1. Создайте нового клиента для Yandex Cloud

**На вашем VPN сервере выполните:**

```bash
# Сгенерируйте ключи для нового клиента
wg genkey | tee yandex-cloud-private.key | wg pubkey > yandex-cloud-public.key

# Посмотрите приватный ключ (понадобится на Yandex Cloud)
cat yandex-cloud-private.key

# Посмотрите публичный ключ (добавим в конфиг VPN сервера)
cat yandex-cloud-public.key
```

### 1.2. Добавьте peer в конфиг VPN сервера

**Отредактируйте `/etc/wireguard/wg0.conf` на VPN сервере:**

```bash
sudo nano /etc/wireguard/wg0.conf
```

**Добавьте в конец файла:**

```ini
[Peer]
# Публичный ключ клиента (Yandex Cloud сервер)
PublicKey = <публичный_ключ_из_yandex-cloud-public.key>
# IP адрес в VPN сети для этого клиента
AllowedIPs = 10.0.0.X/32  # Замените X на свободный IP в вашей сети
```

**Примените изменения:**

```bash
sudo wg syncconf wg0 <(wg-quick strip wg0)
# Или перезагрузите WireGuard:
sudo systemctl restart wg-quick@wg0
```

---

## 📋 Шаг 2: Настройка на Yandex Cloud сервере

### 2.1. Скопируйте скрипт настройки

```bash
# Подключитесь к Yandex Cloud серверу
ssh user@your-yandex-cloud-server

# Скачайте скрипт (или скопируйте содержимое scripts/setup-wireguard-google-api.sh)
# Сделайте исполняемым
chmod +x setup-wireguard-google-api.sh
```

### 2.2. Запустите скрипт

```bash
sudo ./setup-wireguard-google-api.sh
```

**Скрипт запросит:**
- Приватный ключ клиента (из `yandex-cloud-private.key`)
- IP адрес в VPN сети (тот же, что указали в `AllowedIPs` на VPN сервере)
- Публичный ключ VPN сервера (из `/etc/wireguard/wg0.conf` на VPN сервере, секция `[Interface]`)
- Endpoint (IP или домен вашего VPN сервера:порт, обычно `:51820`)

### 2.3. Или настройте вручную

Если не хотите использовать скрипт:

```bash
# Установите WireGuard
sudo apt update
sudo apt install -y wireguard wireguard-tools

# Узнайте IP адреса Google API
dig +short generativelanguage.googleapis.com

# Создайте конфиг
sudo nano /etc/wireguard/wg0.conf
```

**Вставьте (замените на ваши данные):**

```ini
[Interface]
PrivateKey = <приватный_ключ_из_yandex-cloud-private.key>
Address = 10.0.0.X/24  # IP из VPN сети
DNS = 8.8.8.8

[Peer]
PublicKey = <публичный_ключ_VPN_сервера>
Endpoint = <IP_или_домен_VPN>:51820
# Только Google API через VPN
AllowedIPs = 142.250.0.0/15, 172.217.0.0/16, 216.58.0.0/16
PersistentKeepalive = 25
```

**Запустите:**

```bash
sudo wg-quick up wg0
sudo systemctl enable wg-quick@wg0
```

---

## 📋 Шаг 3: Проверка

### 3.1. Проверьте статус WireGuard

```bash
sudo wg show
```

**Должно быть:**
- `interface: wg0`
- `peer:` с вашим VPN сервером
- `transfer:` с не нулевыми значениями (после первого запроса)

### 3.2. Запустите тест-скрипт

```bash
chmod +x test-google-api-vpn.sh
./test-google-api-vpn.sh
```

Или вручную:

```bash
# Проверка доступа к Google API
curl -I https://generativelanguage.googleapis.com/v1/models

# Должен вернуть 401 (Unauthorized) или 400, но НЕ 403 (Forbidden)
# 403 = блокировка по геолокации (не работает)
# 401 = нужен API ключ (работает!)
```

### 3.3. Проверьте маршруты

```bash
# Проверьте, что трафик к Google API идет через VPN
ip route get $(dig +short generativelanguage.googleapis.com | head -1)

# Должно быть что-то типа:
# 142.250.185.14 via 10.0.0.1 dev wg0 ...
```

### 3.4. Тест с вашим приложением

```bash
# Установите API ключ
export GEMINI_API_KEY=your_key

# Простой тест
node -e "
const { GoogleGenerativeAI } = require('@google/generative-ai');
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
const model = genAI.getGenerativeModel({ model: 'gemini-2.5-flash' });
model.generateContent('Привет').then(r => {
  console.log('✅ Работает! Ответ:', r.response.text());
}).catch(e => {
  console.log('❌ Ошибка:', e.message);
});
"
```

---

## 🔧 Если что-то не работает

### Проблема: WireGuard не подключается

```bash
# Проверьте логи
sudo journalctl -u wg-quick@wg0 -n 50

# Проверьте, что порт 51820/udp открыт на VPN сервере
sudo ufw status
# Или
sudo iptables -L -n | grep 51820
```

### Проблема: Ошибка 403 при запросе к Google API

```bash
# Проверьте, что трафик действительно идет через VPN
# Попробуйте сделать запрос и проверить IP отправителя

# Проверьте маршруты
ip route show | grep wg0

# Если маршрутов нет, возможно неправильно указаны IP в AllowedIPs
# Проверьте текущие IP Google API:
dig +short generativelanguage.googleapis.com
```

### Проблема: Запросы медленные

```bash
# Проверьте задержку до VPN сервера
ping <IP_VPN_сервера>

# Если > 100ms, это нормально для Нидерландов из России
# Но можно оптимизировать, используя только конкретные IP вместо диапазонов
```

### Проблема: Весь трафик идет через VPN

Проверьте `AllowedIPs` в `/etc/wireguard/wg0.conf`:

```bash
sudo cat /etc/wireguard/wg0.conf | grep AllowedIPs
```

**Должно быть** что-то типа:
```
AllowedIPs = 142.250.0.0/15, 172.217.0.0/16, 216.58.0.0/16
```

**НЕ должно быть:**
```
AllowedIPs = 0.0.0.0/0  # Это отправляет весь трафик через VPN!
```

---

## 📊 Мониторинг

### Проверить статистику передачи

```bash
sudo wg show
```

### Проверить логи

```bash
sudo journalctl -u wg-quick@wg0 -f
```

---

## 🔒 Безопасность

1. ✅ **Firewall**: Убедитесь, что на VPN сервере открыт только порт 51820/udp для WireGuard
2. ✅ **Ключи**: Храните приватные ключи в безопасности (`chmod 600`)
3. ✅ **Обновления**: Регулярно обновляйте WireGuard: `sudo apt upgrade wireguard`

---

## ✅ Готово!

После настройки ваш Yandex Cloud сервер будет:
- ✅ Обращаться к Google API через VPN в Нидерландах (работает)
- ✅ Обращаться к Yandex, Supabase и другим сервисам напрямую из России (быстро)
- ✅ Автоматически перезапускать VPN при перезагрузке сервера

**Ваш код приложения менять НЕ нужно** - всё будет работать автоматически! 🚀
















