# 🚀 План оптимизации производительности ZumZam

## 📊 Текущие проблемы

### Диагностика: почему страницы грузятся 5-7 секунд?

**Главная причина**: Supabase на удалённом сервере (~100-200ms на каждый запрос)

При загрузке любой страницы происходит:

```
1. Middleware: auth.getUser() ─────────────────── ~150ms
2. AuthContext: auth.getSession() + loadProfile() ~300ms  
3. FavoritesProvider: загрузка избранного ───────── ~150ms
4. Dashboard Layout: 3 запроса (counts, messages, notifications) ~450ms
5. Сама страница: ещё 2-5 запросов ──────────────── ~400-1000ms
═══════════════════════════════════════════════════════════
ИТОГО: ~1.5 - 2.5 секунды минимум на каждую страницу
```

Если страница делает ещё запросы (профиль = 7 запросов), то суммарно ~5-7 секунд.

---

## ✅ Уже сделано

### Сессия 1 (базовые оптимизации)
- [x] Убран retry 3×500ms в AuthContext (экономия ~1.5с)
- [x] Убран дублирующий loadRole()
- [x] /api/conversations оптимизирован (batch-запросы)
- [x] /api/profiles/public: параллельные запросы + кеш 60с
- [x] FavoritesProvider использует AuthContext вместо дублирующего auth
- [x] Dashboard Layout: Promise.all для параллельных запросов

### Сессия 2 (глубокие оптимизации)
- [x] **Страница профиля `/profiles/[slug]`**: 7 последовательных запросов → 2 группы параллельных
- [x] **API `/api/user/counts`**: объединены все счётчики (уведомления + сообщения)
- [x] **mobile-header-icons**: 2 запроса → 1 запрос
- [x] **Dashboard Layout**: убрано дублирование счётчиков

### Сессия 3 (оптимизация чатов и сообщений)
- [x] **Реакции на сообщения**: N запросов → 1 batch запрос (`/api/messages/reactions/batch`)
- [x] **API `/api/conversations/[id]/messages`**: отметка прочитанными неблокирующая
- [x] **API POST messages**: обновление conversations неблокирующее

### Сессия 4 (главная + карточки профилей)
- [x] **ProfileCard**: исправлен баг с кликом после свайпа (expandProgress > 0.1 блокировал)
- [x] **Главная страница**: удалены 15+ console.log (замедляли рендеринг)
- [x] **Главная страница**: удалён неиспользуемый код (parseEWKB, supabase client)
- [x] **Кеш профилей**: увеличен с 60 секунд до 5 минут
- [x] **loading.tsx**: добавлены skeleton-загрузчики для главной и страницы профиля

### Сессия 5 (SSG + CDN кеширование)
- [x] **SSG для профилей**: `generateStaticParams` — страницы генерируются при билде
- [x] **ISR**: `revalidate = 300` — автообновление каждые 5 минут
- [x] **Prefetch**: карточки профилей предзагружают страницу при появлении в viewport
- [x] **HTTP Cache-Control**: `s-maxage=300, stale-while-revalidate=600` для API

---

## 🎯 План дальнейшей оптимизации

### Фаза 1: Критические оптимизации (высокий приоритет)

#### 1.1 Страница профиля `/profiles/[slug]` — 7 последовательных запросов!

**Проблема**: Server Component делает 7+ запросов к Supabase последовательно:
```typescript
// Текущий код (последовательно):
const profile = await getProfile(slug)           // 1 запрос
await supabase.auth.getUser()                    // 2 запрос
const services = await getProfileServices()      // 3 запрос
const packageTiers = await supabase...           // 4 запрос
const yandexData = await getYandexReviewsData()  // 5 запрос
const internalReviews = await getInternalReviews() // 6 запрос
```

**Решение**: Параллельные запросы + один объединённый запрос профиля
```typescript
// После (параллельно):
const [profile, user] = await Promise.all([
  getProfile(slug),
  supabase.auth.getUser()
])

const [services, packageTiers, yandexData, internalReviews] = await Promise.all([
  getProfileServices(profile.id),
  getPackageTiers(profile.id),
  getYandexReviewsData(profile.id, locationIds),
  getInternalReviewsData(profile.id)
])
```

**Экономия**: ~800ms → ~200ms

---

#### 1.2 API `/api/orders` — оптимизация как /api/conversations

**Проблема**: Возможно делает N+1 запросов для связанных данных

**Файл**: `app/api/orders/route.ts`

---

#### 1.3 Удалить дублирование запросов между Layout и Page

**Проблема**: Dashboard Layout грузит counts, Page снова их грузит

**Решение**: Передавать данные через контекст или props

---

### Фаза 2: Кеширование (средний приоритет)

#### 2.1 Добавить кеширование в ключевые API

| API | Время кеша | Триггер инвалидации |
|-----|------------|---------------------|
| `/api/profiles/public` | 60с | ✅ Уже сделано |
| `/api/services?profileId=X` | 60с | При изменении услуг |
| `/api/board-listing-plans` | 5 мин | Редко меняется |
| `/api/notifications` | 10с | При новом уведомлении |

#### 2.2 SWR/React Query для клиентских запросов

**Проблема**: Каждый переход на страницу = новый fetch

**Решение**: Добавить SWR с staleWhileRevalidate
```typescript
// Вместо:
useEffect(() => {
  fetch('/api/orders').then(...)
}, [])

// Использовать:
const { data: orders } = useSWR('/api/orders', fetcher, {
  revalidateOnFocus: false,
  dedupingInterval: 60000
})
```

---

### Фаза 3: Архитектурные улучшения (низкий приоритет)

#### 3.1 Server Components где возможно

Некоторые страницы можно сделать Server Components для SSR:
- `/help` — статичная страница
- `/terms`, `/privacy` — статичные страницы

#### 3.2 Prefetch для ключевых переходов

```typescript
// В Layout добавить prefetch для частых маршрутов
<Link href="/messages" prefetch={true}>
```

#### 3.3 Ленивая загрузка тяжёлых компонентов

```typescript
const HeavyChart = dynamic(() => import('./HeavyChart'), {
  loading: () => <Skeleton />,
  ssr: false
})
```

---

## 📋 Чек-лист для реализации

### Приоритет 1 (сделать сейчас)
- [ ] Оптимизировать `/profiles/[slug]` — параллельные запросы
- [ ] Проверить и оптимизировать `/api/orders`
- [ ] Добавить кеш в `/api/services`

### Приоритет 2 (сделать на неделе)
- [ ] Внедрить SWR для клиентских запросов
- [ ] Добавить кеш в остальные API
- [ ] Оптимизировать компоненты Header (mobile-header-icons делает 2 запроса)

### Приоритет 3 (при возможности)
- [ ] Prefetch для навигации
- [ ] Ленивая загрузка карт и графиков
- [ ] Оптимизировать изображения (blur placeholder, lazy loading)

---

## 🔧 Быстрые победы (можно сделать за 5 минут)

1. **Увеличить кеш профилей** с 60с до 2 минут
2. **Убрать console.log** из production
3. **Добавить loading.tsx** для мгновенного отклика UI

---

## 📈 Ожидаемые результаты

| Страница | Было | Сейчас | После SSG (prod) |
|----------|------|--------|------------------|
| Главная | 3-4с | 0.5-1с | 0.3-0.5с |
| **Профиль** | **5-7с** | **1-2с** | **~50ms** ⚡ |
| Сообщения | 3-4с | 0.3-0.5с | 0.3-0.5с |
| Dashboard | 4-5с | 1с | 1с |
| Заказы | 3-4с | 0.5-1с | 0.5-1с |

> ⚡ SSG работает в production после `npm run build`. В dev-режиме страницы рендерятся динамически.

---

## ⚠️ Что НЕ нужно делать

1. **НЕ** урезать функциональность
2. **НЕ** убирать визуальные эффекты
3. **НЕ** менять UX
4. **НЕ** переходить на другую БД

Все оптимизации — это улучшение кода без потери функций.
