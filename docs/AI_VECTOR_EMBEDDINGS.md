# Автоматическая система векторных embeddings с отзывами

## Архитектура

### 1. Генерация embeddings
Каждый профиль имеет векторное представление (embedding) в колонке `profiles.embedding` (768 измерений).

Embedding генерируется из:
- Название и описание профиля
- Все активные услуги (название, описание, цена, возраст)
- Пакетные предложения с тарифами
- **Последние 5 отзывов клиентов** (РЕАЛЬНЫЙ ОПЫТ!)

### 2. Почему отзывы важны?

**Отзывы - это золотая жила для AI!**

- 📝 **Реальный опыт**: клиенты описывают то, чего нет в официальных описаниях
- 🎯 **Конкретика**: "дети были в восторге", "аниматоры внимательные", "чисто и уютно"
- 💡 **Контекст**: упоминают детали, которые важны родителям
- ⭐ **Доверие**: AI может цитировать реальных людей

**Пример без отзывов:**
> "KidsPoint - студия для детских праздников с программой рисования"

**Пример с отзывами:**
> "KidsPoint - студия для детских праздников. *Родители отмечают: 'Дети были в восторге от свободного рисования, аниматоры очень внимательные'*. По отзывам, студия чистая и уютная, администраторы помогают с организацией."

### 2. Автоматические триггеры

**При создании профиля** (`POST /api/profiles`):
- Генерируется базовый embedding из названия и описания

**При добавлении услуги** (`POST /api/services`):
- Автоматически регенерируется полный embedding профиля

**При обновлении услуги** (`PATCH /api/services/[id]`):
- Автоматически регенерируется embedding профиля

**При удалении услуги** (`DELETE /api/services/[id]`):
- Автоматически регенерируется embedding профиля

**При добавлении отзыва** (`POST /api/reviews`):
- Автоматически регенерируется embedding профиля

### 3. Векторный поиск + Отзывы

AI-чат использует двухступенчатый подход:

**Шаг 1: Векторный поиск** (быстро находит релевантные профили)
```typescript
const embedding = await generateEmbedding(userMessage)
const { data: profiles } = await supabase.rpc('search_profiles_by_vector', {
  query_embedding: embedding,
  match_threshold: 0.3,
  match_count: 8
})
```

**Шаг 2: Загрузка отзывов** (для персонализированных рекомендаций)
```typescript
const { data: reviews } = await supabase
  .from('reviews')
  .select('rating, comment, created_at')
  .eq('profile_id', profileId)
  .eq('visible', true)
  .eq('moderated', true)
  .order('created_at', { ascending: false })
  .limit(5)
```

**Шаг 3: AI генерирует ответ** с цитатами из отзывов
```
Контекст для AI:
- Профиль: KidsPoint
- Услуга: Свободное рисование (3-10 лет, от 20000₽)
- Отзыв 1: ⭐5/5 "Дети были в восторге..."
- Отзыв 2: ⭐5/5 "Аниматоры очень внимательные..."
↓
AI: "Рекомендую **KidsPoint** - по отзывам родителей: 
     *'Дети были в восторге от рисования'*..."
```

## Стоимость

### Gemini API (text-embedding-004)
- **Генерация embedding**: $0.000025 за 1000 токенов
- **Один профиль** (1000 слов): ~$0.00002 (0.002₽)
- **1000 профилей**: ~$0.02 (2₽)

### Запросы пользователей
- **Embedding запроса**: $0.000001 (0.0001₽)
- **Векторный поиск**: БЕСПЛАТНО (pgvector локально)
- **AI ответ**: $0.0001 (0.01₽)
- **Один запрос**: ~$0.0001 (0.01₽)

### Месячные затраты
```
10,000 запросов/день × 30 дней = 300,000 запросов
300,000 × $0.0001 = $30 (3,000₽/мес)
```

### Сравнение с альтернативами
| Решение | Стоимость/мес |
|---------|---------------|
| **Наша система** (Gemini + Supabase pgvector) | $1-30 |
| OpenAI embeddings + Pinecone | $25-100 |
| OpenAI embeddings + Weaviate | $50-200 |
| Cohere + векторная БД | $100-300 |

## Ручная генерация

Для массовой регенерации всех профилей:

```bash
curl -X POST http://localhost:4000/api/admin/generate-embeddings
```

Обновляет все профили, включая:
- Услуги
- Пакеты
- Отзывы

## Мониторинг

Логи генерации:
```
[generateProfileEmbedding] Generating for "KidsPoint" (6 services, 1 packages, 0 reviews)
[generateProfileEmbedding] ✅ Saved for "KidsPoint"
```

## База данных

### Таблица `profiles`
```sql
-- Векторная колонка (768 измерений для Gemini text-embedding-004)
embedding vector(768)

-- Индекс для быстрого поиска (IVFFlat)
CREATE INDEX ON profiles USING ivfflat (embedding vector_cosine_ops);
```

### RPC функция
```sql
CREATE FUNCTION search_profiles_by_vector(
  query_embedding vector(768),
  match_threshold float,
  match_count int
)
RETURNS TABLE (...) 
AS $$
  SELECT *,
    (1 - (embedding <=> query_embedding)) AS similarity
  FROM profiles
  WHERE embedding IS NOT NULL
    AND (1 - (embedding <=> query_embedding)) > match_threshold
  ORDER BY embedding <=> query_embedding
  LIMIT match_count;
$$;
```

## Оптимизация

### Кеширование embeddings
- Embeddings НЕ перегенерируются при каждом запросе
- Регенерация только при изменении контента (услуги/отзывы)
- Векторный поиск работает напрямую с базой (очень быстро)

### Производительность
- **Генерация embedding**: ~200-500ms
- **Векторный поиск**: ~10-50ms (зависит от размера БД)
- **Общее время запроса AI**: 1-3 секунды

### Масштабирование
- pgvector поддерживает миллионы векторов
- Индексы IVFFlat для ускорения поиска
- Горизонтальное масштабирование через Supabase

## Файлы

- `lib/ai/generate-profile-embedding.ts` - Генерация полного embedding
- `lib/ai/trigger-embedding-regeneration.ts` - Триггер регенерации
- `app/api/webhooks/profile-updated/route.ts` - Webhook для фоновой обработки
- `app/api/admin/generate-embeddings/route.ts` - Массовая генерация
- `app/api/ai/chat/route.ts` - AI-чат с векторным поиском

## Будущие улучшения

1. **Инкрементальная генерация**: обновлять только измененные части
2. **Кеширование запросов**: сохранять популярные запросы пользователей
3. **A/B тестирование**: разные пороги similarity для поиска
4. **Мультиязычность**: embeddings для разных языков
5. **Персонализация**: учитывать историю запросов пользователя

