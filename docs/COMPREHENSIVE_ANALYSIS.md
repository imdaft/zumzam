# 🔍 ПОЛНЫЙ АНАЛИЗ: Что было, что сделано, что осталось

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### ✅ ЧТО УЖЕ СОЗДАНО (База данных):

1. **Новая схема классификации** ✅
   - `primary_venue_type_enum` (7 значений: active_entertainment, quest_escape, creative_studio, event_space, vr_digital, animal_interaction, outdoor_recreation)
   - `business_model_enum` (5 значений: tickets_freeplay, packages_turnkey, rental_only, mobile_services, hybrid)
   - `space_type_enum` (6 значений: outdoor, closed_arena, mall_venue, loft_studio, mobile, country_base)
   
2. **Каталоги** ✅
   - `activity_catalog`: 25 активностей (батуты, лазертаг, боулинг...)
   - `service_catalog`: 15 дополнительных услуг (аниматоры, кейтеринг, фотограф...)

3. **Новые колонки в profiles** ✅
   - `primary_venue_type`
   - `activities` (TEXT[])
   - `business_models` (business_model_enum[])
   - `space_type`
   - `additional_services` (TEXT[])
   - `search_vector` (tsvector с автотриггером)
   - `tags` (TEXT[])
   
4. **12 новых JSONB полей** ✅
   - `working_hours`, `metro_stations`, `parking_info` (критичные)
   - `age_restrictions`, `capacity_info`, `payment_methods`, `messenger_contacts` (важные)
   - `accessibility`, `amenities`, `prepayment_policy`, `area_info`, `structured_address` (желательные)

5. **10 тестовых профилей** ✅
   - МазаПарк, Лазертаг Альфа, FamiLand, Голубой чемодан, МузеУм и др.
   - С заполненными `primary_venue_type`, `activities`, `business_models`

---

### ✅ ЧТО УЖЕ СОЗДАНО (Код):

1. **Генератор блоков** ✅
   - `lib/profile-blocks/generator.ts`
   - `generateProfileBlocks(profile)` - умная генерация на основе данных

2. **Визард 5 шагов** ✅
   - `components/features/profile/wizard/classification-wizard.tsx` (главный)
   - `step-1-primary-type.tsx`, `step-2-activities.tsx`, `step-3-business-models.tsx`, `step-4-space-type.tsx`, `step-5-services.tsx`
   - Mobile-first дизайн, валидация, загрузка из каталогов

3. **Новые блоки для профиля** ✅
   - `ActivitiesBlock` (3 варианта: standard, compact, large)
   - `TicketPricingBlock`
   - `RentalPricingBlock`
   - `CateringMenuBlock` (с модальным окном)
   - `ProfileHeader`, `ProfileGallery`, `ProfileReviews`, `ContactsBlock`

4. **TypeScript типы** ✅
   - `lib/types/profile.ts` - все новые интерфейсы

---

## ❌ ЧТО НЕ СДЕЛАНО (КРИТИЧНО):

### 1. **НЕ ИНТЕГРИРОВАНО В РЕДАКТИРОВАНИЕ ПРОФИЛЯ**

**Файл:** `app/(dashboard)/profiles/[slug]/edit/client.tsx`  
**Проблема:** Использует СТАРУЮ систему классификации:
- **Строка 909-923**: `CategoryVisualSelector` - выбор только основной категории
- **Строка 927-948**: `VenueTypeGroupedSelector` - СТАРАЯ система типов площадок
- **НЕТ:** Визарда 5 шагов, выбора активностей, бизнес-моделей

**Что нужно:**
- Заменить `CategoryVisualSelector` + `VenueTypeGroupedSelector` на `ClassificationWizard`
- Или добавить кнопку "Изменить классификацию" которая открывает визард

---

### 2. **НЕ ИНТЕГРИРОВАНО В ФОРМУ СОЗДАНИЯ ПРОФИЛЯ**

**Файл:** `components/features/profile/create-profile-form.tsx`  
**Проблема:** То же самое - старая система:
- **Строка 868**: `step === 'category'` только выбирает одну категорию
- **Строка 909-923**: `CategoryVisualSelector`
- **Строка 927-948**: `VenueTypeGroupedSelector` для venue

**Что нужно:**
- Заменить "Шаг 1: Категория" + "Шаг 2: Тип площадки" на визард из 5 шагов
- Визард должен возвращать `ClassificationData` и сохранять в форму

---

### 3. **НЕ ИСПОЛЬЗУЕТСЯ НА ГЛАВНОЙ СТРАНИЦЕ**

**Файл:** `app/(public)/page.tsx`  
**Проблема:** Использует старые поля:
- **Строка 43**: `category: string` - старая плоская категория
- **НЕТ:** Фильтрации по `primary_venue_type`, `activities`, `business_models`

**Что нужно:**
- Добавить фильтры по новой классификации
- Группировка по `primary_venue_type`
- Теги из `activities` и `additional_services`

---

### 4. **НЕ ИСПОЛЬЗУЕТСЯ В КАРТОЧКАХ**

**Файл:** `components/features/profile/profile-card.tsx`  
**Проблема:** 
- **Строка 31**: `venueType?: string | null` - старый тип
- **Строка 10**: `getVenueTypeName` - старая функция маппинга
- **НЕТ:** Отображения активностей, бизнес-моделей как тегов

**Что нужно:**
- Отображать `primary_venue_type` вместо старого `venueType`
- Теги из `activities[]` как chips
- Бейджи для `business_models` (например, "🎟️ Билеты", "🎁 Пакеты")

---

### 5. **НЕ ИСПОЛЬЗУЕТСЯ В ОТОБРАЖЕНИИ ПРОФИЛЯ**

**Файл:** `app/(public)/profiles/[slug]/page.tsx`  
**Проблема:** Огромная существующая страница НЕ использует:
- ✅ `generateProfileBlocks()` - НЕ вызывается
- ✅ `ActivitiesBlock`, `TicketPricingBlock` и др. - НЕ рендерятся
- Показывает старые блоки напрямую

**Что нужно:**
- Вызвать `generateProfileBlocks(profile)` в начале
- Маппить блоки на компоненты
- Условно рендерить только то, что есть в `blocks`

---

### 6. **СТАРЫЕ КОНСТАНТЫ НЕ ОБНОВЛЕНЫ**

**Файл:** `lib/constants/venue-types.ts`  
**Проблема:** Содержит старую классификацию
- Старые типы площадок (event_loft, event_loft_hybrid...)
- Не соответствует новым ENUM в БД

**Что нужно:**
- Удалить или пометить как deprecated
- Создать новые константы под `primary_venue_type_enum`

---

### 7. **НЕ ОБНОВЛЕНА КОМПОНЕНТА ПОИСКА**

**Файл:** `app/(public)/search/page.tsx` (предположительно)  
**Проблема:** Использует старую категоризацию для поиска

**Что нужно:**
- Фильтры по `primary_venue_type`
- Фильтры по `activities`
- Использование `search_vector` для full-text search

---

## 📋 ПЛАН ДЕЙСТВИЙ (по приоритетам)

### 🔴 ПРИОРИТЕТ 1: Создание/Редактирование профилей

#### Задача 1.1: Интегрировать визард в редактирование
**Файлы:** `app/(dashboard)/profiles/[slug]/edit/client.tsx`

**План:**
1. Импортировать `ClassificationWizard`
2. Заменить секцию "Определите ваш профиль" (строки 883-975) на визард
3. Обработать `onComplete` визарда → сохранить в `profile`
4. Убрать старые `CategoryVisualSelector`, `VenueTypeGroupedSelector`

#### Задача 1.2: Интегрировать визард в создание
**Файлы:** `components/features/profile/create-profile-form.tsx`

**План:**
1. Заменить `step === 'category'` на `step === 'classification'`
2. Показывать `ClassificationWizard` вместо `CategorySelectionStep`
3. Сохранять `ClassificationData` в форму
4. Убрать старую секцию "Определите ваш профиль"

---

### 🟠 ПРИОРИТЕТ 2: Отображение профиля

#### Задача 2.1: Интегрировать генератор блоков
**Файлы:** `app/(public)/profiles/[slug]/page.tsx`

**План:**
1. Импортировать `generateProfileBlocks`, новые блоки
2. После получения профиля: `const blocks = generateProfileBlocks(profile as Profile)`
3. Заменить жесткий рендеринг секций на маппинг:
   ```tsx
   {blocks.find(b => b.type === 'activities') && (
     <ActivitiesBlock 
       profileId={profile.id}
       activities={profile.activities}
       variant="desktop"
       isOwner={isOwner}
     />
   )}
   ```
4. Условный рендеринг для всех 10 типов блоков

---

### 🟡 ПРИОРИТЕТ 3: Главная страница

#### Задача 3.1: Обновить фильтры и карточки
**Файлы:** `app/(public)/page.tsx`, `components/features/profile/profile-card.tsx`

**План:**
1. Добавить фильтры по `primary_venue_type`
2. Добавить фильтры по `activities[]`
3. Обновить `ProfileCard`:
   - Показывать `primary_venue_type` вместо старого
   - Теги из `activities` как chips
   - Бейджи для `business_models`

#### Задача 3.2: Обновить CategoryNav
**Файлы:** `components/features/home/category-nav.tsx`

**План:**
1. Группировать профили по `primary_venue_type` вместо старого
2. Обновить маппинг категорий

---

### 🟢 ПРИОРИТЕТ 4: Поиск и миграция

#### Задача 4.1: Обновить поиск
**Файлы:** `app/(public)/search/page.tsx`

**План:**
1. Использовать `search_vector` для full-text search
2. Фильтры по новой классификации

#### Задача 4.2: Миграция старых профилей
**Файлы:** SQL миграция

**План:**
1. Создать функцию маппинга старых типов → новая классификация
2. Запустить миграцию для существующих профилей

---

## 📊 СТАТИСТИКА

### Что СДЕЛАНО:
- ✅ 6 SQL миграций
- ✅ 26 файлов кода
- ✅ ~5,500 строк кода
- ✅ 10 тестовых профилей

### Что НЕ СДЕЛАНО:
- ❌ Интеграция визарда в редактирование
- ❌ Интеграция визарда в создание
- ❌ Интеграция генератора блоков в просмотр
- ❌ Обновление главной страницы
- ❌ Обновление карточек
- ❌ Обновление поиска
- ❌ Миграция старых профилей

---

## 🎯 ВЫВОД

**Создан фундамент (база данных + компоненты), но НЕ интегрирован в существующую систему.**

Система работает параллельно со старой, но не заменяет её. Нужно:
1. Заменить старую классификацию на новую в формах
2. Использовать генератор блоков для рендеринга
3. Обновить карточки и главную страницу
4. Мигрировать старые профили

**Следующий шаг:** Начать с Приоритета 1 (формы создания/редактирования).





