# ⚠️ УСТАРЕЛО - Глобальное изменение Responsive Breakpoints

> **ВНИМАНИЕ:** Эта документация устарела. Изменения были откачены из-за проблем с вертикальными мониторами (Full HD вертикально = 1080px).
> 
> **Актуальная документация:** [`RESPONSIVE_STRATEGY.md`](./RESPONSIVE_STRATEGY.md)
> 
> **Дата отката:** 06.12.2024

---

# ~~Глобальное изменение Responsive Breakpoints~~

## 📱 Что изменено

Весь сайт теперь использует **увеличенный порог переключения** на мобильную версию: **1280px (xl)**.

### До изменений:
- **< 768px (md)** - мобильная версия
- **768px - 1024px** - "средняя" версия  
- **≥ 1024px (lg)** - десктопная версия

### После изменений:
- **< 1280px (xl)** - **мобильная версия**
- **≥ 1280px (xl)** - **десктопная версия**

## 🔧 Что было сделано

### 1. Изменён базовый хук
**Файл:** `hooks/use-media-query.ts`

```typescript
export function useIsMobile(): boolean {
  return !useMediaQuery('(min-width: 1280px)') // Было: 768px
}
```

### 2. Заменены все Tailwind CSS классы
Автоматически заменено **64 файла**:
- Все `md:` → `xl:` (было 768px, стало 1280px)
- Все `lg:` → `xl:` (было 1024px, стало 1280px)

**Затронутые папки:**
- `app/` - 20 файлов
- `components/` - 44 файла

### 3. Обновлён globals.css
**Файл:** `app/globals.css`

Все media queries изменены с `1024px` на `1280px`:
- CSS переменные (--radius, --spacing, --height)
- Utility классы (aspect-card, grid-cards, touch-card-gap и др.)

## 📊 Статистика изменений

| Тип изменения | Количество |
|---------------|------------|
| TypeScript/TSX файлов | 64 |
| CSS media queries | 5 |
| JavaScript хуков | 1 |
| **Всего** | **70** |

## 🎯 Зачем это нужно?

### Проблема
На экранах 768px - 1280px (планшеты, узкие десктопы) интерфейс был "сжатым":
- Сложные компоненты (чат, формы) не помещались комфортно
- Требовалась промежуточная "планшетная" версия
- Сложность в поддержке 3 версий интерфейса

### Решение
Теперь есть только **2 версии**:
- **Мобильная** (< 1280px) - полноэкранные компоненты, bottom navigation
- **Десктопная** (≥ 1280px) - полноценный layout с сайдбарами и панелями

### Преимущества
✅ Проще поддерживать (2 версии вместо 3)  
✅ Комфортнее работать на планшетах (мобильная версия оптимизирована)  
✅ Больше места для сложных компонентов на десктопе  
✅ Единая точка контроля (один breakpoint)

## 🔍 Как проверить

1. Откройте сайт в браузере
2. Откройте DevTools (F12)
3. Включите режим адаптивности (Ctrl+Shift+M)
4. Измените ширину экрана:
   - **1279px и меньше** - должна быть мобильная версия
   - **1280px и больше** - должна быть десктопная версия

## ⚙️ Технические детали

### Tailwind Breakpoints
Хотя мы заменили все классы, стандартные breakpoints Tailwind остались:
- `sm:` = 640px
- `md:` = 768px
- `lg:` = 1024px
- `xl:` = 1280px ✅ **Используем везде**
- `2xl:` = 1536px

### Почему xl: а не lg:?
Мы используем `xl:` (1280px) потому что:
1. Это стандартный Tailwind breakpoint
2. Не нужно настраивать `tailwind.config.js`
3. Все классы из библиотек компонентов работают без изменений

### Исключения
Некоторые компоненты могут использовать промежуточные breakpoints `sm:` (640px) для мелких адаптаций (размер текста, отступы), но основная логика mobile/desktop всегда на `xl:` (1280px).

## 📝 Файлы для восстановления

Если нужно откатить изменения:

```bash
# 1. Откатить хук
git checkout hooks/use-media-query.ts

# 2. Откатить CSS
git checkout app/globals.css

# 3. Откатить компоненты (все измененные файлы)
git checkout app/ components/
```

## 🚀 Дальнейшие действия

После этих изменений рекомендуется:
1. ✅ Протестировать основные страницы на разных разрешениях
2. ✅ Проверить формы, модалки, навигацию
3. ✅ Убедиться, что чат работает корректно
4. ✅ Протестировать на реальных устройствах (телефон, планшет, десктоп)

---

**Дата изменения:** 06.12.2024  
**Причина:** Упрощение адаптивности для всего сайта  
**Статус:** ✅ Завершено

