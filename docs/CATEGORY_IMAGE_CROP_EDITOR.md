# Редактор Кропа Изображений Категорий

## Обзор

Новый редактор позволяет настраивать **независимые кропы** для десктопной (16:9) и мобильной (1:1) версий изображений категорий.

## Что изменилось

### До
- ❌ Один кроп для обоих режимов (десктоп и мобильный)
- ❌ Много лишних контролов (яркость, контрастность, поворот)
- ❌ Слишком большой минимальный zoom (1x)
- ❌ AI расширение не обновляло изображение в редакторе

### После
- ✅ **Два независимых кропа** - настройки десктопа не влияют на мобильный
- ✅ **Только нужные контролы** - zoom (0.5x - 3x) и перемещение мышкой
- ✅ **Уменьшенный минимальный zoom** - можно уменьшить до 0.5x
- ✅ **AI расширение обновляет изображение** в редакторе с обходом кеша

## Как использовать

### 1. Загрузка нового изображения

1. Откройте главную страницу как администратор
2. Нажмите на шестеренку на карточке категории
3. Выберите **"Загрузить изображение"**
4. Выберите файл (до 5MB, JPG/PNG/WebP)
5. Редактор откроется автоматически

### 2. Настройка Desktop кропа (16:9)

1. Убедитесь, что выбран режим **"Desktop (16:9)"**
2. Используйте слайдер **"Увеличение"** для zoom (0.5x - 3x)
3. Перетаскивайте изображение мышкой для позиционирования
4. Кроп применяется в реальном времени

### 3. Настройка Mobile кропа (1:1)

1. Переключитесь на режим **"Mobile (1:1)"**
2. Настройте zoom и позицию **независимо** от десктопной версии
3. Мобильный кроп использует `objectFit: contain` для предотвращения обрезки

### 4. AI Расширение изображения

Если изображение слишком маленькое или нужно добавить пространство:

1. Прокрутите вниз до секции **"AI расширение изображения"**
2. Выберите **степень расширения** (20-60%)
3. Выберите **направление**:
   - Вверх ↑
   - Вниз ↓
   - Влево ←
   - Вправо →
   - Все стороны (по умолчанию)
4. Нажмите **"Расширить"**

**Стоимость:** 10 кредитов за одно расширение

#### Что делать если AI не смог расширить

Gemini может отказаться расширять некоторые изображения по разным причинам:

**Ошибка авторских прав:**
- ❌ Изображение содержит логотипы/торговые марки
- ❌ Защищенный контент (персонажи, бренды)
- ✅ **Решение:** Загрузите другое изображение без защищенного контента

**Технические проблемы:**
- ❌ AI не может продолжить композицию
- ✅ **Решение:** Попробуйте:
  1. Уменьшить процент расширения (20-30%)
  2. Выбрать другое направление
  3. Загрузить другое изображение

### 5. Сохранение

После настройки обоих кропов нажмите **"Сохранить оба кропа"**

Система сохранит:
- URL изображения
- Параметры desktop кропа (x, y, zoom)
- Параметры mobile кропа (x, y, zoom)

### 6. Редактирование существующего кропа

1. Откройте шестеренку на карточке категории
2. Выберите **"Редактировать кроп"**
3. Редактор откроется с текущим изображением
4. Настройте оба кропа
5. Сохраните изменения

## Технические детали

### Структура данных

```typescript
interface CropParams {
  crop: { x: number; y: number }  // Позиция кропа
  zoom: number                     // Увеличение (0.5 - 3)
  croppedAreaPixels: any          // Пиксельная область (для будущего)
}
```

### API Endpoints

**Загрузка файла:**
```
POST /api/category-images/upload
Body: FormData { file, categoryId }
Response: { imageUrl, filePath }
```

**Сохранение кропа:**
```
POST /api/category-images
Body: { categoryId, imageUrl?, desktopCrop, mobileCrop }
Response: { success: true, data }
```

**AI расширение:**
```
POST /api/ai/expand-category-image
Body: { categoryId, imageUrl, direction, expandPercent }
Response: { imageUrl, creditsUsed, creditsRemaining }
```

### База данных

Таблица `category_images`:
- `category_id` (PK) - ID категории
- `image_url` - URL загруженного изображения
- `desktop_crop` - JSONB параметры desktop кропа
- `mobile_crop` - JSONB параметры mobile кропа
- `updated_at` - Время последнего обновления

## Горячие клавиши

- **Tab** - переключение между Desktop/Mobile режимами
- **Escape** - закрыть редактор
- **Enter** - сохранить изменения

## Оптимизация производительности

### Кеширование изображений

Изображения категорий загружаются с обходом кеша:
```typescript
fetch(`/api/category-images?t=${Date.now()}`, {
  cache: 'no-store'
})
```

После AI расширения добавляется timestamp:
```typescript
const newImageUrl = `${result.imageUrl}?t=${Date.now()}`
```

### Next.js Image Optimization

На главной странице категории используют оптимизированный компонент `Image`:
```tsx
<Image 
  src={imageUrl} 
  alt={name}
  fill
  className="object-cover"
  sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 25vw"
/>
```

**Преимущества:**
- ✅ Автоматическая конвертация в WebP/AVIF
- ✅ Адаптивный ресайз под разные экраны
- ✅ Lazy loading
- ✅ Blur placeholder

## Troubleshooting

### Изображение не обновляется после AI расширения

**Проблема:** После расширения старое изображение остается в редакторе

**Решение:** Проверьте браузерную консоль:
```
[CategoryCard] Starting AI expand: { categoryId, imageUrl, direction, expandPercent }
[CategoryCard] API response status: 200
[CategoryCard] API success: { imageUrl, creditsUsed }
[CategoryCard] Updated image in cropper: https://...?t=1234567890
```

Если видите `Updated image in cropper` - изображение должно обновиться

### 422 ошибка при AI расширении

**Проблема:** Gemini не смог расширить изображение

**Причины:**
1. Изображение содержит защищенный контент
2. Слишком большой процент расширения
3. Композиция слишком сложная для модели

**Решение:**
1. Попробуйте уменьшить процент до 20-30%
2. Выберите другое направление
3. Загрузите более простое изображение

### Кроп сбрасывается при переключении режимов

**Это ожидаемое поведение!** Desktop и Mobile кропы полностью независимы.

Каждый режим имеет свои параметры:
- Desktop: zoom, crop.x, crop.y
- Mobile: zoom, crop.x, crop.y

При переключении режима загружаются параметры для выбранного режима.

## Best Practices

### Для Desktop (16:9)

- ✅ Используйте zoom 1.0-1.5x для широких планов
- ✅ Центрируйте главный объект
- ✅ Оставляйте пространство по краям для текста (название категории)

### Для Mobile (1:1)

- ✅ Используйте zoom 0.8-1.2x для квадратного формата
- ✅ Вертикальное центрирование важнее горизонтального
- ✅ Убедитесь, что главный объект полностью виден

### AI Расширение

- ✅ Начинайте с 30-40% расширения
- ✅ Используйте "Все стороны" для симметричного расширения
- ✅ Проверяйте результат перед сохранением
- ❌ Не расширяйте на 60% сразу - это часто приводит к ошибкам

## Changelog

### v2.0 (December 12, 2024)

**Новое:**
- ✨ Независимые кропы для Desktop и Mobile
- ✨ Minumum zoom уменьшен до 0.5x
- ✨ AI расширение обновляет изображение в редакторе

**Удалено:**
- ❌ Яркость контрол
- ❌ Контрастность контрол
- ❌ Поворот контрол

**Исправлено:**
- 🐛 AI расширение не обновляло изображение
- 🐛 Кроп сбрасывался при переключении режимов
- 🐛 Слишком большой минимальный zoom









