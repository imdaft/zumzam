# ⚠️ КРИТИЧНО: Удалить .next и перезапустить!

## Проблема

Webpack все еще пытается забандлить `@xenova/transformers` для API route, хотя это серверный код.

## Решение

### 1. Остановить сервер
```bash
Ctrl + C
```

### 2. **ОБЯЗАТЕЛЬНО** удалить кеш Next.js
```bash
# В терминале PowerShell:
Remove-Item -Recurse -Force .next

# Или вручную:
# Удалите папку .next в корне проекта
```

### 3. Запустить сервер заново
```bash
npm run dev
```

---

## Что изменилось:

Добавлена правильная externals конфигурация в `next.config.ts`:

```typescript
if (isServer) {
  config.externals = config.externals || []
  
  const externalsArray = Array.isArray(config.externals) 
    ? config.externals 
    : [config.externals]
  
  externalsArray.push(
    '@xenova/transformers',
    'sharp',
    'onnxruntime-node'
  )
  
  config.externals = externalsArray
}
```

Теперь Next.js НЕ будет пытаться забандлить эти модули - они будут загружаться динамически через `require()` на сервере.

---

## ⚠️ БЕЗ удаления .next это НЕ сработает!

Webpack кеширует конфигурацию в `.next/cache/`, поэтому **обязательно** удали эту папку!

---

# 🚀 Действуй:

1. **Ctrl+C** - останови сервер
2. **Удали `.next`** - Remove-Item -Recurse -Force .next
3. **npm run dev** - запусти заново

После этого голосовой ввод **должен заработать**! 🎤✨















