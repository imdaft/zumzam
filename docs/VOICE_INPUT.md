# 🎤 Голосовой ввод - Whisper Транскрипция

## 📋 Описание

Система голосового ввода для AI чата использует **бесплатный open-source Whisper** через **@xenova/transformers** (transformers.js). Это позволяет пользователям отправлять голосовые сообщения, которые автоматически распознаются и отправляются в AI чат.

---

## 🏗️ Архитектура

### 1. Frontend (UI)

**Файл:** `components/shared/mobile-bottom-tabs.tsx`

**Hook:** `hooks/use-voice-recorder.ts`

- Кнопка микрофона в input области (вместо кнопки отправки, когда поле пустое)
- Индикатор записи с таймером (красная точка + время)
- Индикатор транскрипции (3 прыгающих точки)
- Кнопки "Стоп" (квадрат) и "Отмена" (X) во время записи
- Автоматическая отправка после успешной транскрипции

### 2. Backend (API)

**Файл:** `app/api/ai/transcribe/route.ts`

**POST `/api/ai/transcribe`**

**Request:**
```typescript
FormData {
  audio: File (audio/webm)
}
```

**Response:**
```json
{
  "text": "Помоги организовать день рождения для ребенка семи лет",
  "language": "ru",
  "duration": 3.5
}
```

### 3. Whisper Model

**Модель:** `Xenova/whisper-tiny` (quantized)

- **Размер:** ~75 MB (сжатая версия)
- **Язык:** Русский (`ru`)
- **Задача:** Транскрипция (`transcribe`)
- **Кеширование:** Модель загружается один раз при первом запросе
- **Runtime:** Node.js (не Edge)
- **Timeout:** 60 секунд максимум

---

## 🎙️ Как работает

1. **Пользователь нажимает кнопку микрофона** (🎤)
2. **Браузер запрашивает доступ к микрофону**
3. **Начинается запись** (`MediaRecorder API`)
   - Формат: `audio/webm` (Opus кодек)
   - Включены: echo cancellation, noise suppression, auto gain control
4. **Пользователь нажимает "Стоп"** (или "Отмена")
5. **Аудио blob отправляется на сервер** (`/api/ai/transcribe`)
6. **Whisper распознает речь** (локально на сервере)
7. **Текст возвращается клиенту**
8. **Автоматическая отправка в AI чат**

---

## 🔧 Технические детали

### MediaRecorder настройки

```typescript
const stream = await navigator.mediaDevices.getUserMedia({ 
  audio: {
    echoCancellation: true,    // Подавление эха
    noiseSuppression: true,    // Подавление шума
    autoGainControl: true,     // Автоматическая регулировка громкости
  } 
})

const mediaRecorder = new MediaRecorder(stream, {
  mimeType: 'audio/webm;codecs=opus' // Эффективный кодек
})
```

### Whisper Pipeline

```typescript
const transcriber = await pipeline(
  'automatic-speech-recognition',
  'Xenova/whisper-tiny',
  { quantized: true } // Сжатая модель
)

const result = await transcriber(audioBuffer, {
  language: 'ru',
  task: 'transcribe',
  return_timestamps: false,
})
```

### Кеширование модели

```typescript
let transcriber: any = null // Глобальный кеш

if (!transcriber) {
  transcriber = await pipeline(...) // Загружается один раз
}
```

---

## 🎨 UI States

| Состояние | Кнопка | Индикатор | Input |
|-----------|--------|-----------|-------|
| **Обычное** | 🎤 Микрофон | - | Активен |
| **Запись** | ⏹ Стоп + ✖ Отмена | 🔴 Таймер | Отключен |
| **Транскрипция** | - | 3 точки | Отключен |
| **Есть текст** | ➤ Отправка | - | Активен |

---

## 📊 Параметры (можно настроить)

**Файл:** `app/api/ai/transcribe/route.ts`

```typescript
// Модель Whisper
model: 'Xenova/whisper-tiny'  // Самая быстрая (tiny)
// Альтернативы:
// - 'Xenova/whisper-base'     // Больше точность
// - 'Xenova/whisper-small'    // Еще больше точность (медленнее)

// Настройки транскрипции
language: 'ru'                 // Русский язык
task: 'transcribe'             // Транскрипция (не перевод)
return_timestamps: false       // Без временных меток

// Таймауты
maxDuration: 60                // 60 секунд максимум
```

**Файл:** `hooks/use-voice-recorder.ts`

```typescript
// MediaRecorder
mimeType: 'audio/webm;codecs=opus' // Формат
echoCancellation: true             // Подавление эха
noiseSuppression: true             // Подавление шума
autoGainControl: true              // Авто-громкость
```

---

## 🚀 Использование

### В AI чате:

1. Откройте AI чат (оранжевая кнопка в нижнем меню)
2. Нажмите кнопку микрофона 🎤
3. Разрешите доступ к микрофону (если первый раз)
4. Говорите свой вопрос
5. Нажмите "Стоп" ⏹ (или "Отмена" ✖)
6. Подождите пока распознается речь (2-5 секунд)
7. Сообщение автоматически отправится в чат

---

## 🐛 Troubleshooting

### Ошибка: "Не удалось получить доступ к микрофону"
- Проверьте, что браузер имеет доступ к микрофону
- Убедитесь, что используете HTTPS (или localhost)
- Проверьте настройки микрофона в системе

### Ошибка: "Не удалось распознать речь"
- Говорите четче и громче
- Уменьшите фоновый шум
- Убедитесь, что микрофон работает

### Ошибка: "Ошибка транскрипции"
- Проверьте логи сервера
- Убедитесь, что модель Whisper загрузилась
- Проверьте, что аудио файл не поврежден

### Модель Whisper не загружается
При первом запросе модель загружается автоматически (~75 MB).
Это может занять 10-30 секунд в зависимости от интернета.

**Логи:**
```
[Whisper] Loading model...
[Whisper] Model loaded!
```

---

## 📝 Следующие шаги

1. ✅ Базовая запись голоса
2. ✅ Whisper транскрипция (русский)
3. ✅ UI с индикаторами
4. ✅ Автоотправка в чат
5. ⏳ Поддержка других языков
6. ⏳ Улучшенная модель (whisper-base)
7. ⏳ Кеширование на клиенте
8. ⏳ Визуализация аудио-волны

---

## 💡 Полезные команды

### Тестирование локально:
```bash
# Открыть в браузере
http://localhost:4000

# Проверить доступ к микрофону
navigator.mediaDevices.getUserMedia({ audio: true })
```

### Проверка модели:
```bash
# Логи сервера при первой транскрипции
[Whisper] Loading model...
[Whisper] Model loaded!
[Whisper] Received audio: recording.webm 45231 bytes
[Whisper] Transcribing...
[Whisper] Result: помоги организовать день рождения для ребенка семи лет
```

---

## 🎉 Готово!

Голосовой ввод с бесплатной open-source транскрипцией готов к использованию! 🚀🎤















