# Настройка переменных окружения в Vercel

## Шаг 1: Откройте настройки проекта

1. Войдите на https://vercel.com
2. Выберите проект **zumzam**
3. В верхнем меню нажмите **Settings** (Настройки)
4. В левом меню выберите **Environment Variables** (Переменные окружения)

## Шаг 2: Добавление переменных

Для каждой переменной нажмите кнопку **"Add New"** или **"Добавить"**

### Переменная 1: DATABASE_URL

**Name (Имя):**
```
DATABASE_URL
```

**Value (Значение):**
```
postgresql://zumzam_admin:ВАШ_ПАРОЛЬ@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
```

**Важно:** Замените `ВАШ_ПАРОЛЬ` на реальный пароль из Яндекс.Облако

**Где взять пароль:**
1. Яндекс.Облако → Managed Service for PostgreSQL
2. Откройте ваш кластер
3. Перейдите в **Пользователи** → `zumzam_admin`
4. Скопируйте пароль (или создайте новый, если забыли)

**Environment (Окружения):**
- ✅ **Production** (обязательно)
- ✅ **Preview** (рекомендуется)
- ✅ **Development** (опционально)

Нажмите **Save** (Сохранить)

---

### Переменная 2: GOOGLE_GEMINI_API_KEY

**Name (Имя):**
```
GOOGLE_GEMINI_API_KEY
```

**Value (Значение):**
```
ваш_gemini_api_key_здесь
```

**Где взять ключ:**
1. Перейдите на https://aistudio.google.com/app/apikey
2. Войдите в Google аккаунт
3. Нажмите **"Create API Key"** или **"Создать API ключ"**
4. Скопируйте ключ (начинается с `AIza...`)

**Environment (Окружения):**
- ✅ **Production** (обязательно)
- ✅ **Preview** (рекомендуется)
- ✅ **Development** (опционально)

Нажмите **Save** (Сохранить)

**Альтернативное имя:**
Если у вас ключ называется по-другому, можно также добавить:
```
GEMINI_API_KEY
```
(с тем же значением)

---

### Переменная 3: JWT_SECRET

**Name (Имя):**
```
JWT_SECRET
```

**Value (Значение):**
```
ваш_случайный_секретный_ключ_минимум_32_символа
```

**Как сгенерировать секретный ключ:**

**Windows PowerShell:**
```powershell
-join ((1..64) | ForEach-Object { Get-Random -Maximum 256 | ForEach-Object { '{0:X2}' -f $_ } })
```

**Linux/Mac:**
```bash
openssl rand -hex 32
```

**Или онлайн:**
- https://randomkeygen.com/ (выберите "CodeIgniter Encryption Keys")
- Скопируйте любой ключ длиной 32+ символов

**Важно:** 
- Минимум 32 символа
- Используйте случайный набор букв, цифр и символов
- Сохраните ключ в безопасном месте (он нужен для работы сайта)

**Environment (Окружения):**
- ✅ **Production** (обязательно)
- ✅ **Preview** (рекомендуется)
- ✅ **Development** (опционально)

Нажмите **Save** (Сохранить)

---

### Переменная 4: NEXT_PUBLIC_SITE_URL (опционально)

**Name (Имя):**
```
NEXT_PUBLIC_SITE_URL
```

**Value (Значение):**
```
https://ваш-домен.vercel.app
```

Или если подключили свой домен:
```
https://zumzam.ru
```

**Environment (Окружения):**
- ✅ **Production** (обязательно)
- ✅ **Preview** (рекомендуется)
- ⚪ **Development** (не обязательно)

Нажмите **Save** (Сохранить)

---

## Шаг 3: Проверка переменных

После добавления всех переменных вы должны увидеть список:

```
✅ DATABASE_URL          [Production, Preview, Development]
✅ GOOGLE_GEMINI_API_KEY [Production, Preview, Development]
✅ JWT_SECRET            [Production, Preview, Development]
✅ NEXT_PUBLIC_SITE_URL  [Production, Preview] (опционально)
```

## Шаг 4: Применение изменений

**Важно:** После добавления/изменения переменных окружения нужно перезапустить деплой:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Или создайте новый коммит:
   ```bash
   git commit --allow-empty -m "Update environment variables"
   git push origin main
   ```

## Частые ошибки

### Ошибка: "Environment variable not found"

**Причина:** Переменная не добавлена или добавлена не для того окружения

**Решение:**
1. Проверьте, что переменная добавлена
2. Убедитесь, что выбраны правильные окружения (Production, Preview)
3. Перезапустите деплой

### Ошибка: "Database connection failed"

**Причина:** Неправильный `DATABASE_URL` или пароль

**Решение:**
1. Проверьте пароль в `DATABASE_URL`
2. Убедитесь, что БД доступна из интернета (firewall в Яндекс.Облаке)
3. Проверьте формат: `postgresql://user:password@host:port/database?sslmode=require`

### Ошибка: "JWT secret is required"

**Причина:** `JWT_SECRET` не добавлен или слишком короткий

**Решение:**
1. Добавьте `JWT_SECRET` (минимум 32 символа)
2. Перезапустите деплой

## Формат DATABASE_URL

```
postgresql://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]?sslmode=require
```

**Пример:**
```
postgresql://zumzam_admin:мой_пароль_123@rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net:6432/zumzam?sslmode=require
```

**Компоненты:**
- `zumzam_admin` - имя пользователя
- `мой_пароль_123` - пароль (замените на реальный)
- `rc1b-ktk7vobktajbv2sd.mdb.yandexcloud.net` - хост
- `6432` - порт
- `zumzam` - имя базы данных
- `?sslmode=require` - обязательное SSL подключение

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте переменные окружения в Git
- Не делитесь паролями и ключами
- Используйте разные пароли для разных окружений (Production/Preview)
- Регулярно меняйте пароли

## Проверка после настройки

После добавления всех переменных:

1. Создайте новый деплой (Redeploy)
2. Проверьте логи сборки (Build Logs)
3. Убедитесь, что нет ошибок подключения к БД
4. Откройте сайт и проверьте работу

## Готово! 🎉

После настройки всех переменных окружения ваш сайт должен успешно задеплоиться и работать.

