# Исправление системы уведомлений и добавление настроек

## Дата: 6 декабря 2025

## Проблемы

### 1. Баг: Маркеры уведомлений не обновляются
**Описание:** В мобильной версии около значка колокольчика оставался маркер с количеством непрочитанных уведомлений, даже после того как все уведомления были прочитаны.

**Причина:** Когда пользователь отмечал уведомления как прочитанные на странице `/notifications`, событие не отправлялось другим компонентам (в частности, `MobileHeaderIcons`) для обновления счетчиков.

### 2. Отсутствие настроек уведомлений
Не было функционала для:
- Выбора каналов доставки (сайт, email, Telegram, push)
- Выбора типов уведомлений (заявки, сообщения, платежи и т.д.)

## Решения

### ✅ 1. Исправлен баг с маркерами

#### Изменения в `app/(dashboard)/notifications/page.tsx`
Добавлено отправление события при отметке уведомлений как прочитанных:

```typescript
const markAsRead = async (id: string) => {
  try {
    await fetch(`/api/notifications/${id}/read`, { method: 'POST' })
    setNotifications(notifications.map(n => 
      n.id === id ? { ...n, read_at: new Date().toISOString() } : n
    ))
    // Отправляем событие для обновления маркеров в других компонентах
    window.dispatchEvent(new CustomEvent('notification-read'))
  } catch (error) {
    console.error('Error marking as read:', error)
  }
}

const markAllAsRead = async () => {
  const unread = notifications.filter(n => !n.read_at)
  for (const n of unread) {
    await markAsRead(n.id)
  }
  // Отправляем событие для обновления маркеров
  window.dispatchEvent(new CustomEvent('notification-read'))
}
```

#### Изменения в `components/shared/mobile-header-icons.tsx`
Добавлен слушатель события для автоматического обновления счетчиков:

```typescript
useEffect(() => {
  if (!isAuthenticated || !user?.id) return
  
  const loadCounts = async () => {
    // ... загрузка счетчиков
  }

  loadCounts()
  
  // Обновляем каждые 30 секунд
  const interval = setInterval(loadCounts, 30000)
  
  // Слушаем событие обновления уведомлений
  const handleNotificationRead = () => {
    console.log('[MobileHeaderIcons] Notification read event received, reloading counts')
    loadCounts()
  }
  
  window.addEventListener('notification-read', handleNotificationRead)
  
  return () => {
    clearInterval(interval)
    window.removeEventListener('notification-read', handleNotificationRead)
  }
}, [isAuthenticated, user?.id])
```

**Результат:** Маркеры теперь мгновенно обновляются при прочтении уведомлений на любой странице.

### ✅ 2. Добавлены настройки уведомлений

#### Создана миграция БД: `supabase/migrations/20251206_notification_settings.sql`

**Таблица `notification_settings`:**

```sql
CREATE TABLE notification_settings (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  
  -- Каналы доставки
  enabled_site BOOLEAN DEFAULT true,        
  enabled_email BOOLEAN DEFAULT true,       
  enabled_telegram BOOLEAN DEFAULT false,   
  enabled_push BOOLEAN DEFAULT false,       
  
  -- Типы уведомлений
  notify_new_order BOOLEAN DEFAULT true,
  notify_order_accepted BOOLEAN DEFAULT true,
  notify_order_rejected BOOLEAN DEFAULT true,
  notify_order_completed BOOLEAN DEFAULT true,
  notify_order_cancelled BOOLEAN DEFAULT true,
  
  notify_new_response BOOLEAN DEFAULT true,
  notify_response_accepted BOOLEAN DEFAULT true,
  notify_response_rejected BOOLEAN DEFAULT true,
  
  notify_new_message BOOLEAN DEFAULT true,
  notify_new_review BOOLEAN DEFAULT true,
  notify_payment_success BOOLEAN DEFAULT true,
  notify_payment_failed BOOLEAN DEFAULT true,
  
  notify_subscription_expiring BOOLEAN DEFAULT true,
  notify_subscription_expired BOOLEAN DEFAULT true,
  notify_subscription_renewed BOOLEAN DEFAULT true,
  
  notify_profile_verified BOOLEAN DEFAULT true,
  notify_profile_rejected BOOLEAN DEFAULT true,
  
  notify_board_new_match BOOLEAN DEFAULT true,
  
  -- Метаданные
  telegram_chat_id TEXT,
  email_verified BOOLEAN DEFAULT false,
  telegram_verified BOOLEAN DEFAULT false,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  
  UNIQUE(user_id)
);
```

**Возможности:**
- RLS политики для безопасности
- Автоматическое создание настроек при регистрации
- Триггер для обновления `updated_at`

#### Создан API endpoint: `app/api/settings/notifications/route.ts`

**GET /api/settings/notifications** - получить настройки
**POST /api/settings/notifications** - обновить настройки

```typescript
// GET - возвращает настройки пользователя
// Если настроек нет - создает дефолтные

// POST - обновляет настройки (upsert)
// Автоматически обновляет updated_at
```

#### Создана страница настроек: `app/(dashboard)/settings/notifications/page.tsx`

**Функционал:**

1. **Каналы доставки:**
   - ✅ На сайте (всегда включено)
   - ✅ Email (с проверкой подтверждения)
   - 🔜 Telegram (скоро)
   - 🔜 Push-уведомления (скоро)

2. **Типы уведомлений:**
   - Заявки и заказы (для клиентов и исполнителей)
   - Сообщения и отзывы
   - Платежи и подписки
   - Профиль и верификация (для исполнителей)
   - Доска объявлений

3. **UX:**
   - Автосохранение при изменении
   - Визуальная обратная связь (успех/ошибка)
   - Индикатор загрузки
   - Адаптивный дизайн

#### Обновлена главная страница настроек: `app/(dashboard)/settings/page.tsx`

Добавлена карточка со ссылкой на настройки уведомлений:

```tsx
<Link href="/settings/notifications">
  <div className="flex items-center gap-3">
    <Bell className="h-5 w-5 text-orange-600" />
    <div>
      <p className="font-medium">Уведомления</p>
      <p className="text-xs text-gray-500">
        Настройте способы получения уведомлений
      </p>
    </div>
  </div>
</Link>
```

## Структура файлов

```
app/
├── (dashboard)/
│   ├── notifications/
│   │   └── page.tsx                 ✅ Исправлен баг с маркерами
│   └── settings/
│       ├── page.tsx                 ✅ Добавлена ссылка на настройки
│       └── notifications/
│           └── page.tsx             ✨ НОВОЕ: страница настроек
│
├── api/
│   └── settings/
│       └── notifications/
│           └── route.ts             ✨ НОВОЕ: API endpoint
│
components/
└── shared/
    └── mobile-header-icons.tsx      ✅ Добавлен слушатель события

supabase/
└── migrations/
    └── 20251206_notification_settings.sql  ✨ НОВОЕ: миграция БД
```

## Как это работает

### Синхронизация маркеров

1. Пользователь отмечает уведомление как прочитанное
2. Страница `/notifications` отправляет событие `notification-read`
3. Все компоненты с маркерами (MobileHeaderIcons, layout и др.) слушают это событие
4. При получении события компоненты перезагружают счетчики через API
5. Маркеры обновляются автоматически

### Настройки уведомлений

1. Пользователь открывает `/settings/notifications`
2. Загружаются текущие настройки (или создаются дефолтные)
3. При изменении переключателя:
   - Настройки сохраняются через API (автосохранение)
   - Показывается уведомление об успехе
   - Обновляется локальное состояние

## Будущие улучшения

### Интеграция с Email
- [ ] Подтверждение email для получения уведомлений
- [ ] Красивые email шаблоны
- [ ] Настройка частоты email (мгновенно/дайджест)

### Интеграция с Telegram
- [ ] Telegram бот для ZumZam
- [ ] Подключение через QR код
- [ ] Отправка уведомлений в Telegram

### Push уведомления
- [ ] Service Worker для PWA
- [ ] Запрос разрешений на push
- [ ] Отправка через Firebase или аналог

### Дополнительные настройки
- [ ] Тихий режим (не беспокоить)
- [ ] Расписание уведомлений
- [ ] Группировка уведомлений
- [ ] Приоритеты уведомлений

## Тестирование

### Проверка исправления бага

1. Откройте сайт в мобильной версии
2. Создайте тестовое уведомление
3. Убедитесь, что маркер появился
4. Откройте страницу уведомлений
5. Отметьте уведомление как прочитанное
6. **Ожидаемо:** Маркер исчезает мгновенно

### Проверка настроек

1. Откройте `/settings`
2. Кликните на "Уведомления"
3. Переключите любую настройку
4. **Ожидаемо:** Появляется сообщение "Настройки успешно сохранены"
5. Обновите страницу
6. **Ожидаемо:** Настройки сохранились

## Применение миграции

Для применения миграции выполните:

```bash
# Через Supabase CLI
supabase db push

# Или выполните SQL напрямую в Supabase Dashboard
```

## Автор
Исправления и улучшения: 6 декабря 2025

















