# 🔍 Почему прямой Supabase запрос не работает в wizard контексте?

## 📊 **КЛЮЧЕВОЕ РАЗЛИЧИЕ В КОНТЕКСТЕ**

### ✅ **ActivityFilters (работает):**
```tsx
// app/(public)/page.tsx - главная страница
export default function HomePage() {
  // Простой клиентский компонент
  return (
    <div>
      <ActivityFilters 
        onFilterChange={setSelectedActivityIds}
        selectedActivities={selectedActivityIds}
      />
    </div>
  )
}
```

**Характеристики:**
- Монтируется на **главной странице** при загрузке
- Простой компонент без сложных зависимостей
- Страница уже загружена, все провайдеры инициализированы
- Auth session готова к использованию

---

### ❌ **ActivitiesStep в wizard (не работает):**
```tsx
// components/features/profile/create-profile-form.tsx
export function CreateProfileForm() {
  const form = useForm<ProfileInput>({ ... }) // React Hook Form
  // ... множество состояний ...
  
  return (
    <div>
      {/* Условный рендеринг */}
      <ClassificationWizard
        initialData={{ ... }}
        onComplete={async (data) => { ... }}
      />
    </div>
  )
}
```

**Характеристики:**
- Монтируется **внутри сложной формы** с множеством состояний
- React Hook Form управляет формой
- Множество useEffect'ов, состояний, асинхронных операций
- Монтируется **условно** (может быть скрыт/показан)
- Wizard имеет собственные состояния и шаги

---

## 🔬 **ВОЗМОЖНЫЕ ПРИЧИНЫ**

### 1. **Auth Session блокировка (НАИБОЛЕЕ ВЕРОЯТНО)**

**Проблема:**
Supabase client может ждать инициализации auth session перед выполнением запроса.

```tsx
// lib/supabase/client.ts
const client = createBrowserClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    lock: lockNoOp, // Кастомный lock
  }
})
```

**Что происходит:**
1. В `activity-filters.tsx`: страница уже загружена → auth session готова → запрос выполняется ✅
2. В wizard: компонент монтируется динамически → auth session может быть не готова → client ждёт → timeout ❌

**Доказательства:**
- Промис создаётся ✅ (query builder создан)
- HTTP-запрос **НЕ отправляется** ❌ (нет в Network tab)
- Промис зависает навсегда

**Почему так происходит:**
- `createBrowserClient` может проверять/инициализировать session перед запросом
- В wizard контексте эта инициализация может блокироваться или зависать
- `lockNoOp` может не работать правильно в этом контексте

---

### 2. **React Hook Form конфликт**

**Проблема:**
Множество состояний и асинхронных операций в `CreateProfileForm` могут конфликтовать с Supabase client.

**Возможный сценарий:**
- React Hook Form имеет множество `watch`, `formState`, `trigger` операций
- Эти операции могут создавать множество ре-рендеров
- Supabase client может быть создан в момент, когда компонент в нестабильном состоянии
- Промис может быть "потерян" из-за ре-рендеров

---

### 3. **Контекст монтирования (React Strict Mode)**

**Проблема:**
Хотя Strict Mode не включён явно, Next.js в dev режиме может иметь подобное поведение.

**Возможный сценарий:**
- Wizard монтируется → создаётся Supabase client → запрос инициируется
- Компонент ре-рендерится (из-за родительских состояний)
- Старый промис "отменяется" или теряется
- Новый промис создаётся, но запрос не отправляется

---

### 4. **Navigator Lock блокировка**

**Проблема:**
`lockNoOp` может не работать правильно в wizard контексте.

```tsx
async function lockNoOp<R>(
  _name: string,
  _acquireTimeout: number,
  fn: () => Promise<R>
): Promise<R> {
  return await fn()
}
```

**Возможный сценарий:**
- В wizard может быть несколько попыток создать client одновременно
- `lockNoOp` может не правильно обрабатывать это
- Запрос может быть заблокирован внутренней логикой Supabase

---

### 5. **QueryProvider конфликт**

**Проблема:**
Хотя `activity-filters.tsx` тоже обёрнут в QueryProvider, в wizard может быть дополнительный конфликт.

**Возможный сценарий:**
- QueryProvider создаёт глобальный QueryClient
- Wizard компоненты могут иметь конфликтующие query keys или состояния
- Supabase промис может быть "перехвачен" React Query

**НО:** Это маловероятно, так как мы используем прямой `useEffect`, а не `useQuery`.

---

## 🎯 **ПОЧЕМУ API ENDPOINT РЕШАЕТ ПРОБЛЕМУ**

### **API Endpoint работает потому что:**

1. **Изолированный контекст**
   - Запрос выполняется на сервере, где нет React состояний
   - Нет конфликтов с React Hook Form или другими клиентскими библиотеками

2. **Auth session на сервере**
   - Server-side Supabase client инициализируется независимо
   - Нет проблем с client-side auth session блокировкой

3. **Прямой HTTP запрос**
   - `fetch('/api/activity-catalog')` - простой HTTP запрос
   - Не зависит от Supabase client инициализации на клиенте

4. **Стабильный контекст**
   - API route выполняется в стабильном серверном контексте
   - Нет ре-рендеров, нет конфликтов состояний

---

## 🔍 **КАК МОЖНО ПРОВЕРИТЬ ГИПОТЕЗЫ**

### **Тест 1: Auth Session**
```tsx
// В step-2-activities.tsx перед запросом
useEffect(() => {
  async function test() {
    const supabase = createClient()
    
    // Проверяем session
    const { data: { session }, error } = await supabase.auth.getSession()
    console.log('Session:', session, 'Error:', error)
    
    // Теперь делаем запрос
    const { data, error: queryError } = await supabase
      .from('activity_catalog')
      .select('*')
      .limit(1)
    
    console.log('Query result:', data, 'Error:', queryError)
  }
  
  test()
}, [])
```

### **Тест 2: Задержка монтирования**
```tsx
// В step-2-activities.tsx
useEffect(() => {
  // Ждём 1 секунду перед запросом
  const timer = setTimeout(async () => {
    const supabase = createClient()
    const { data } = await supabase.from('activity_catalog').select('*')
    console.log('Delayed query:', data)
  }, 1000)
  
  return () => clearTimeout(timer)
}, [])
```

### **Тест 3: Изоляция от React Hook Form**
```tsx
// Создать отдельный компонент вне формы
// И проверить, работает ли там Supabase запрос
```

---

## ✅ **ВЫВОД**

**Наиболее вероятная причина:** Auth session блокировка

Supabase client в wizard контексте пытается инициализировать/проверить auth session перед выполнением запроса, но эта инициализация блокируется или зависает из-за:
- Динамического монтирования компонента
- Конфликта с множеством состояний в родительской форме
- Проблем с `lockNoOp` в этом контексте

**Решение:** Использовать API endpoint, который:
- Работает на сервере (нет проблем с client-side session)
- Изолирован от React состояний
- Всегда работает стабильно

---

## 📝 **РЕКОМЕНДАЦИЯ**

**Для Production:**
- ✅ Использовать API endpoint (надёжно, работает везде)
- ✅ Кэшировать на сервере (быстрее для пользователей)
- ✅ Не тратить время на отладку client-side проблемы (которая может быть багом Supabase или конфликтом библиотек)

**Если всё же нужно использовать прямой запрос:**
- Попробовать добавить явную проверку session перед запросом
- Попробовать задержку перед запросом (100-200ms)
- Попробовать изолировать компонент от React Hook Form




