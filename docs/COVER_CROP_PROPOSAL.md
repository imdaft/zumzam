# Предложение: Редактирование обложки профиля

## Проблема

При переключении между шаблонами секции "О нас" меняется формат отображения обложки:
- **Classic**: горизонтальная обложка (широкая, низкая)
- **Modern**: вертикальная обложка слева (узкая, высокая)
- **Minimal**: горизонтальная обложка с наложением текста

Одна и та же картинка может выглядеть по-разному в зависимости от шаблона. Например:
- В Modern шаблоне важны правая и левая части картинки
- В Classic важны верх и низ
- В Minimal важен центр композиции

## Решение

### Вариант 1: Редактор кропа (рекомендуется) ⭐

**Преимущества:**
- Пользователь контролирует, какая часть картинки видна
- Не требует загрузки новой картинки
- Быстро и интуитивно

**Что нужно:**
1. Библиотека `react-easy-crop` (уже установлена!)
2. Хранение координат кропа в БД
3. UI для настройки кропа

**Реализация:**

```typescript
// В таблице profiles добавить:
cover_photo_crop: {
  classic: { x: 0, y: 0, zoom: 1 },
  modern: { x: 0, y: 0, zoom: 1 },
  minimal: { x: 0, y: 0, zoom: 1 }
}
```

**Интерфейс:**
- Клик на обложку → открывается редактор
- Перетаскивание + зум для выбора области
- Превью для всех шаблонов
- "Сохранить" → применяется только к текущему шаблону

**Время реализации:** 4-6 часов

---

### Вариант 2: Разные обложки для разных шаблонов

**Преимущества:**
- Полный контроль
- Можно использовать разные картинки

**Недостатки:**
- Нужно загружать 3 картинки
- Занимает больше места
- Дольше для пользователя

**Реализация:**

```typescript
// В таблице profiles:
cover_photos: {
  classic: 'url1.jpg',
  modern: 'url2.jpg',
  minimal: 'url3.jpg'
}
```

**Время реализации:** 6-8 часов

---

### Вариант 3: Автоматическая обработка (AI)

**Преимущества:**
- Не требует действий пользователя
- Умное кадрирование

**Недостатки:**
- Сложная реализация
- Дорого (API вызовы)
- Не всегда правильный результат

**Реализация:**
- Cloudinary / Imgix для умного кропа
- API для определения важных областей
- Автоматическое применение

**Время реализации:** 8-12 часов + настройка сервисов

---

## Рекомендация

### 🎯 Вариант 1 (Редактор кропа) - лучший выбор

**Почему:**
1. ✅ Быстрая реализация (у нас уже есть `react-easy-crop`)
2. ✅ Отличный UX
3. ✅ Экономит место на сервере
4. ✅ Интуитивно понятно

**План реализации:**

#### Шаг 1: БД (15 мин)
```sql
ALTER TABLE profiles
ADD COLUMN cover_photo_crop JSONB DEFAULT '{
  "classic": {"x": 0, "y": 0, "zoom": 1},
  "modern": {"x": 0, "y": 0, "zoom": 1},
  "minimal": {"x": 0, "y": 0, "zoom": 1}
}';
```

#### Шаг 2: Компонент редактора (2-3 часа)
```tsx
// components/features/profile/cover-crop-editor.tsx
- Modal с react-easy-crop
- Превью для текущего шаблона
- Сохранение координат
```

#### Шаг 3: Интеграция (1 час)
```tsx
// В AboutSection компонентах:
- Клик на обложку (если isOwner)
- Открытие редактора
- Применение сохраненного кропа
```

#### Шаг 4: API (30 мин)
```typescript
// PATCH /api/profiles/[id]/cover-crop
- Валидация координат
- Сохранение в БД
```

#### Шаг 5: Применение кропа при рендере (30 мин)
```tsx
// CSS object-position + zoom для применения кропа
<Image 
  style={{ 
    objectPosition: `${crop.x}% ${crop.y}%`,
    transform: `scale(${crop.zoom})`
  }}
/>
```

**Итого:** ~4-5 часов чистой работы

---

## Дополнительные улучшения

### Превью при выборе шаблона
Показывать как обложка будет выглядеть в каждом шаблоне:

```tsx
<TemplateSwitcher>
  {templates.map(t => (
    <div>
      <span>{t.name}</span>
      <MiniPreview template={t} cover={cover} crop={crops[t.id]} />
    </div>
  ))}
</TemplateSwitcher>
```

### Умные дефолты
При первом переключении на новый шаблон автоматически:
- **Classic**: центр по горизонтали, верх по вертикали
- **Modern**: центр по вертикали, лево по горизонтали
- **Minimal**: центр по всем осям

### Быстрое копирование
Кнопка "Скопировать настройки из Classic" в других шаблонах

---

## Приоритет

**Высокий** - это критично для хорошего UX, так как разные шаблоны требуют разного кадрирования.

Рекомендую реализовать в первую очередь базовый редактор кропа для текущего шаблона, а остальные улучшения добавить позже по необходимости.

---

## Альтернатива "на быструю руку"

Если нужно быстрое решение прямо сейчас (10 минут):

1. Добавить к обложке `object-position: center` 
2. Для Modern шаблона увеличить высоту блока
3. Добавить кнопку "Загрузить новую обложку"

Это не идеально, но даст пользователям хоть какой-то контроль.

```tsx
// В AboutSectionModern:
<div className="relative h-[280px] md:h-full md:min-h-[600px]">
  <Image 
    src={data.coverPhoto}
    className="object-cover object-center"
  />
</div>
```

Хочешь, чтобы я реализовал полноценный редактор кропа? 🎨















