# 🚀 База для профилей: Завершено!

## ✅ Выполненные задачи

### 1. Создан `lib/constants/profile-categories.ts`
**Что включено:**
- ✅ Метаданные для всех **13 категорий** профилей
- ✅ Определение подтипов для каждой категории
- ✅ Минимумы фото/видео
- ✅ Требования к документам (для некоторых категорий)
- ✅ Вспомогательные функции (getCategoryMetadata, hasSubtypes, и т.д.)

**Категории:**
1. venue (Площадка) — 6 подтипов
2. animator (Аниматор) — без подтипов
3. show (Шоу-программа) — 7 подтипов
4. agency (Агентство) — без подтипов
5. quest (Квест) — 2 подтипа
6. master_class (Мастер-класс) — 4 подтипа
7. photographer (Фото/Видео) — 3 варианта
8. **🆕 catering (Кейтеринг)** — 4 подтипа
9. **🆕 confectionery (Кондитерская)** — 3 подтипа
10. **🆕 decorator (Декоратор)** — 3 подтипа
11. **🆕 dj_musician (Диджей/Музыкант)** — 3 подтипа
12. **🆕 host (Ведущий)** — 2 подтипа
13. **🆕 transport (Транспорт)** — 4 подтипа

---

### 2. Обновлён `types/supabase.ts`
**Что изменено:**
- ✅ Добавлены 6 новых значений в enum `profile_category`
- ✅ Теперь TypeScript знает о всех 13 категориях
- ✅ Автокомплит в IDE будет работать для новых категорий

---

### 3. Создана миграция БД
**Файл:** `supabase/migrations/20251207000000_add_new_profile_categories.sql`

**Что делает:**
- ✅ Добавляет 6 новых значений в enum `profile_category` (безопасно, с `IF NOT EXISTS`)
- ✅ Обновляет комментарии к колонкам `category` и `details`
- ✅ Документирует структуру JSON для каждой категории

**⚠️ Миграция НЕ применена автоматически!**

---

### 4. Обновлён `category-selection-step.tsx`
**Что изменено:**
- ✅ Теперь использует `PROFILE_CATEGORIES` из констант
- ✅ Отображает все 13 категорий (включая новые)
- ✅ Добавлены иконки для новых категорий (Lucide Icons)
- ✅ Код стал более поддерживаемым (один источник правды)

---

## 📋 Следующие шаги

### Шаг 1: Применить миграцию БД

#### Локально (через Supabase CLI):
```bash
# Убедитесь, что Supabase CLI установлен и настроен
supabase db push

# Или вручную:
# 1. Откройте Supabase Dashboard → SQL Editor
# 2. Скопируйте содержимое supabase/migrations/20251207000000_add_new_profile_categories.sql
# 3. Выполните SQL
```

#### На продакшене:
```bash
# Через Supabase CLI (после тестирования на локальной БД)
supabase db push --linked

# Или через Dashboard:
# 1. Supabase Dashboard → SQL Editor
# 2. Скопируйте миграцию
# 3. Выполните
```

---

### Шаг 2: Проверить работу

#### Тест 1: Выбор категории
1. Откройте `/profiles/create`
2. Проверьте, что все 13 категорий отображаются
3. Выберите любую категорию → переход на следующий шаг

#### Тест 2: Создание профиля (пока работает только Площадка)
1. Выберите "Площадка"
2. Заполните все шаги
3. Создайте профиль
4. Проверьте, что профиль сохранился в БД

---

### Шаг 3: Разработка wizard для новых категорий

**Приоритет:**
1. ✅ Площадка (venue) — в процессе
2. 🔄 Аниматор (animator) — следующий
3. 🔄 Агентство (agency)
4. 🔄 Остальные категории

**Для каждой категории нужно:**
- Создать компоненты wizard-шагов
- Определить структуру `details` (JSON)
- Добавить валидацию (zod schemas)
- Протестировать создание и редактирование

---

## 🛠️ Технические детали

### Использование констант в коде

#### Получить метаданные категории:
```typescript
import { getCategoryMetadata, hasSubtypes } from '@/lib/constants/profile-categories'

const metadata = getCategoryMetadata('venue')
console.log(metadata.nameRu) // "Площадка"
console.log(metadata.minPhotos) // 5

if (hasSubtypes('venue')) {
  const subtypes = getCategorySubtypes('venue')
  // subtypes: [{ id: 'kids_center', name: 'Детский центр', ...}, ...]
}
```

#### Валидация минимумов:
```typescript
import { hasEnoughPhotos, hasEnoughVideos } from '@/lib/constants/profile-categories'

const canPublish = 
  hasEnoughPhotos('photographer', photoCount) &&
  hasEnoughVideos('photographer', videoCount)
```

#### Проверка документов:
```typescript
import { requiresDocuments, getDocumentTypes } from '@/lib/constants/profile-categories'

if (requiresDocuments('show')) {
  const docs = getDocumentTypes('show')
  // ["Сертификаты безопасности (для криошоу, животных)", ...]
}
```

---

## 📊 Структура `details` (JSON)

### Примеры для каждой категории:

#### Venue (Площадка):
```json
{
  "subtype": "kids_center",
  "capacity": 50,
  "area": 100,
  "amenities": ["игровая_зона", "санузел_детский", "кухня"],
  "rules": {
    "own_food": true,
    "own_alcohol": false
  }
}
```

#### Catering (Кейтеринг):
```json
{
  "subtype": "kids_menu",
  "menu": ["основные_блюда", "десерты", "напитки"],
  "min_order_people": 10,
  "price_per_person": 1000,
  "delivery": {
    "included": true,
    "radius_km": 10
  }
}
```

#### Decorator (Декоратор):
```json
{
  "subtype": "balloons",
  "services": ["арки", "фигуры", "цифры"],
  "min_order_rub": 5000,
  "includes_installation": true
}
```

---

## 🎯 Статус TODO

- [x] Обновить `types/supabase.ts`
- [x] Создать миграцию БД
- [x] Создать `lib/constants/profile-categories.ts`
- [x] Обновить `category-selection-step.tsx`
- [ ] Применить миграцию (вручную, требуется доступ к БД)
- [ ] Разбить `characteristics.tsx` на подкомпоненты
- [ ] Создать wizard-шаги для новых категорий
- [ ] Добавить валидацию (zod)

---

## 🚨 Важно

1. **Миграция обратима:** Использован `ADD VALUE IF NOT EXISTS`, можно применять многократно
2. **Нет ломающих изменений:** Существующие профили не затронуты
3. **TypeScript безопасность:** Все типы обновлены, нет `any`
4. **Единый источник правды:** `profile-categories.ts` — основа для всех компонентов

---

**Дата:** 7 декабря 2025  
**Статус:** ✅ База готова, можно строить wizard для новых категорий  
**Следующий шаг:** Применить миграцию и начать разработку компонентов

















