# 🏢 Глубокий анализ типов площадок для стандартизации

## 📊 Основные выводы после анализа рынка

### Классификация и различия

**ДЕТСКИЙ ЦЕНТР vs ПАРК РАЗВЛЕЧЕНИЙ:**

| Параметр | Детский центр | Парк развлечений |
|----------|---------------|------------------|
| **Размер** | Небольшое помещение (100-500 м²) | Крупный комплекс (500+ м²) |
| **Фокус** | Развитие и обучение | Развлечения и аттракционы |
| **Возраст** | 2-12 лет (преимущественно) | Все возрасты |
| **Формат** | Закрытое помещение | Может быть крытым/открытым |
| **Вместимость** | 20-100 детей | Сотни-тысячи посетителей |
| **Программы** | Дни рождения, мастер-классы | Аттракционы, шоу, массовые события |

**БАТУТНЫЙ ЦЕНТР** — отдельная категория:
- Спортивно-развлекательный комплекс
- Возраст: 5+ лет (дети и взрослые)
- Вместимость: 50-200 человек
- Оборудование: батуты, поролоновая яма, скалодром
- Относится к **спортивным сооружениям** или может быть подвидом парка развлечений

---

## 🎯 СТАНДАРТИЗИРОВАННАЯ СТРУКТУРА ПОЛЕЙ

### УНИВЕРСАЛЬНЫЕ ПОЛЯ (для всех типов площадок)

#### 1. Основные параметры
- `capacity_min` (число) - Минимальная вместимость
- `capacity_max` (число) - Максимальная вместимость
- `area_sqm` (число) - Площадь в м²
- `parking` (boolean) - Наличие парковки
- `parking_spots` (число, опционально) - Количество парковочных мест

#### 2. Инфраструктура
- `kitchen` (boolean) - Наличие кухни
- `kitchen_type` (enum: 'full' | 'mini' | 'none') - Тип кухни
- `toilets` (boolean) - Наличие туалетов
- `toilets_count` (число, опционально) - Количество туалетов
- `changing_room` (boolean) - Раздевалка
- `storage` (boolean) - Кладовая/хранилище

#### 3. Техническое оборудование
- `projector` (boolean) - Проектор
- `screen` (boolean) - Экран
- `sound_system` (boolean) - Звуковая система
- `microphone` (boolean) - Микрофон(ы)
- `lighting` (boolean) - Профессиональное освещение
- `wifi` (boolean) - Wi-Fi
- `electricity` (boolean) - Доступ к электричеству
- `water_supply` (boolean) - Водоснабжение (для outdoor)

#### 4. Зоны и мебель
- `zones` (массив строк) - Зоны (игровая, для еды, отдыха и т.д.)
- `tables` (число) - Количество столов
- `chairs` (число) - Количество стульев
- `adult_tables` (число) - Столов для взрослых (лофт)
- `adult_seats` (число) - Мест для взрослых за столом
- `kids_tables` (число) - Столов для детей (лофт)
- `kids_seats` (число) - Мест для детей за столом

---

## 📋 СПЕЦИФИЧНЫЕ ПОЛЯ ПО ТИПАМ

### 1. 🎈 ДЕТСКИЙ ЦЕНТР

#### Подтипы (нужно добавить в классификацию):
- `developing` - Развивающий центр (обучение, мастер-классы)
- `playroom` - Игровая комната (игры, развлечения)
- `sports` - Спортивный центр (активные игры, спорт)
- `mixed` - Смешанный тип

#### Специфичные поля:
```typescript
{
  // Возрастные группы
  age_groups: string[] // ['0-3', '3-7', '7-12']
  
  // Оборудование и игровые зоны
  equipment: {
    trampoline: boolean        // Батут
    ball_pool: boolean         // Сухой бассейн
    maze: boolean              // Лабиринт
    slides: boolean            // Горки
    climbing_wall: boolean     // Скалодром
    soft_play: boolean         // Мягкая игровая зона
    creative_zone: boolean     // Зона для творчества
  }
  
  // Специальные помещения
  animator_room: boolean       // Комната для аниматора
  parent_area: boolean         // Зона для родителей
  nap_room: boolean            // Комната для сна (для малышей)
  
  // Зоны
  play_zone: boolean           // Игровая зона
  dining_zone: boolean         // Зона для еды
  active_games_zone: boolean   // Зона активных игр
  
  // Питание
  own_food_allowed: boolean    // Можно принести свою еду
  catering_available: boolean  // Услуги кейтеринга
  
  // Безопасность
  security_system: boolean     // Система безопасности
  medical_kit: boolean         // Аптечка
  staff_trained: boolean       // Обученный персонал
}
```

---

### 2. 🏢 ЛОФТ / СТУДИЯ

#### Специфичные поля:
```typescript
{
  // Интерьер
  interior_style: string // 'loft' | 'minimalism' | 'classic' | 'modern' | 'vintage'
  interior_photos: string[] // Фото интерьера
  
  // Планировка
  open_space: boolean         // Открытое пространство
  rooms_count: number         // Количество комнат
  main_hall: boolean          // Основной зал
  separate_entrance: boolean  // Отдельный вход
  
  // Особенности
  panoramic_windows: boolean  // Панорамные окна
  natural_light: boolean      // Естественное освещение
  cyclorama: boolean          // Белая циклорама (для фото)
  high_ceilings: boolean      // Высокие потолки
  brick_walls: boolean        // Кирпичные стены
  
  // Мебель (детальная разбивка)
  tables: {
    adult_count: number       // Столов для взрослых
    adult_seats_per_table: number // Мест за взрослым столом
    kids_count: number        // Столов для детей
    kids_seats_per_table: number  // Мест за детским столом
    high_chairs: number       // Детских стульчиков
  }
  
  // Зоны
  zones: {
    main_hall: boolean
    changing_room: boolean
    storage: boolean
    balcony: boolean          // Балкон/терасса
  }
}
```

---

### 3. 🍽️ КАФЕ / РЕСТОРАН

#### Подтипы:
- `cafe` - Кафе (небольшое, непринужденное)
- `restaurant` - Ресторан (формальная обстановка)
- `family_restaurant` - Семейный ресторан (детское меню + зона)

#### Специфичные поля:
```typescript
{
  // Кухня и меню
  menu_type: string[] // ['kids', 'adults', 'drinks', 'cakes', 'banquet']
  kids_menu: boolean          // Детское меню
  adults_menu: boolean        // Взрослое меню
  cake_policy: string         // 'allowed' | 'not_allowed' | 'only_from_us'
  own_food_allowed: boolean   // Можно принести свою еду
  
  // Залы
  private_hall: boolean       // Отдельный зал
  hall_capacity: number       // Вместимость зала
  halls_count: number         // Количество залов
  vip_room: boolean           // VIP-зал
  
  // Детская инфраструктура
  kids_zone: boolean          // Детская зона
  kids_playground: boolean    // Игровая площадка
  high_chairs: number         // Детских стульчиков
  animator_available: boolean // Услуги аниматора
  
  // Банкетные услуги
  banquet_available: boolean  // Организация банкетов
  catering_available: boolean // Кейтеринг
  waiter_service: boolean     // Обслуживание официантами
  
  // Режим работы
  working_hours: string       // Часы работы
  weekend_available: boolean  // Работает в выходные
}
```

---

### 4. 🎠 ПАРК РАЗВЛЕЧЕНИЙ

#### Подтипы:
- `indoor` - Крытый парк (всесезонный)
- `outdoor` - Открытый парк (сезонный)
- `mixed` - Смешанный (крытые павильоны + открытые зоны)

#### Специфичные поля:
```typescript
{
  // Масштаб
  territory_ha: number        // Территория в гектарах (для outdoor)
  indoor_area_sqm: number     // Площадь крытых помещений
  
  // Аттракционы
  attractions: {
    kids: string[]            // Детские аттракционы
    family: string[]          // Семейные аттракционы
    extreme: string[]         // Экстремальные аттракционы
    interactive: string[]     // Интерактивные зоны
  }
  
  // Зоны
  zones: {
    kids_zone: boolean        // Детская зона
    family_zone: boolean      // Семейная зона
    extreme_zone: boolean     // Экстремальная зона
    food_court: boolean       // Фуд-корт
    shops: boolean            // Магазины
    parking: boolean          // Парковка
  }
  
  // Инфраструктура
  pavilions: boolean          // Крытые павильоны
  all_season: boolean         // Всесезонность
  heating: boolean            // Отопление (для крытых)
  air_conditioning: boolean   // Кондиционирование
  
  // Развлечения
  shows_available: boolean    // Шоу-программы
  entertainment_staff: boolean // Персонал для развлечений
  event_organization: boolean  // Организация мероприятий
}
```

---

### 5. 🌳 ОТКРЫТАЯ ПЛОЩАДКА (OUTDOOR)

#### Подтипы:
- `park` - Парк
- `forest` - Лесная территория
- `beach` - Пляжная зона
- `field` - Поле/луг
- `courtyard` - Двор

#### Специфичные поля:
```typescript
{
  // Территория
  territory_sqm: number       // Площадь территории
  territory_ha: number        // Площадь в гектарах
  
  // Защита от погоды
  tent: boolean               // Тент/навес
  tent_type: string           // 'full_coverage' | 'partial' | 'none'
  covered_area_sqm: number    // Площадь под навесом
  
  // Инфраструктура
  electricity: boolean        // Электричество
  water_supply: boolean       // Водоснабжение
  sewerage: boolean           // Канализация
  toilets: boolean            // Туалеты
  toilets_mobile: boolean     // Мобильные туалеты
  
  // Покрытие
  surface_type: string[]      // ['grass', 'tile', 'asphalt', 'sand', 'wood']
  
  // Дополнительно
  fire_place: boolean         // Мангал/костровище
  grill_available: boolean    // Гриль
  kids_playground: boolean    // Детская площадка
  sports_equipment: boolean   // Спортивное оборудование
  
  // Погодные условия
  weather_backup: string      // 'none' | 'tent' | 'indoor_backup'
  indoor_backup_available: boolean // Есть помещение-запас
  
  // Доступ
  road_access: boolean        // Подъездные пути
  parking: boolean            // Парковка
  public_transport: boolean   // Доступность общественного транспорта
}
```

---

### 6. 🎯 БАТУТНЫЙ ЦЕНТР (новый тип!)

#### Специфичные поля:
```typescript
{
  // Батутная зона
  trampolines_count: number   // Количество батутов
  trampoline_types: string[]  // ['standard', 'extreme', 'sports', 'kids']
  
  // Дополнительное оборудование
  foam_pit: boolean           // Поролоновая яма
  climbing_wall: boolean      // Скалодром
  ninja_warrior: boolean      // Полоса препятствий
  basketball: boolean         // Баскетбольные кольца на батуте
  dodgeball: boolean          // Зона для вышибал
  
  // Зоны
  zones: {
    kids: boolean             // Детская зона
    adult: boolean            // Зона для взрослых
    extreme: boolean          // Экстремальная зона
    party: boolean            // Зона для праздников
  }
  
  // Безопасность
  safety_equipment: boolean   // Защитное оборудование
  instructors_available: boolean // Инструкторы
  safety_rules: boolean       // Правила безопасности
  
  // Программы
  programs: {
    birthday: boolean         // Дни рождения
    training: boolean         // Тренировки
    team_building: boolean    // Тиमбилдинг
    fitness: boolean          // Фитнес-программы
  }
  
  // Возрастные ограничения
  age_min: number             // Минимальный возраст
  age_groups: string[]        // ['kids_5-10', 'teens_10-16', 'adults_16+']
}
```

---

### 7. 🎪 ДРУГОЕ (кастомный тип)

#### Поля для описания:
```typescript
{
  custom_type: string         // Описание типа площадки
  description: string         // Подробное описание
  special_features: string[]  // Особенности
  
  // Универсальные поля из общей структуры
  // + любые специфичные по необходимости
}
```

---

## ✅ РЕКОМЕНДАЦИИ ПО ИМПЛЕМЕНТАЦИИ

### 1. Пересмотр классификации подтипов

**Добавить подтипы для "Детский центр":**
- Развивающий центр
- Игровая комната
- Спортивный центр
- Смешанный тип

**Добавить новый тип "Батутный центр"** (не относится ни к детскому центру, ни к парку)

**Добавить подтипы для "Парк развлечений":**
- Крытый парк
- Открытый парк
- Смешанный

**Добавить подтипы для "Кафе/Ресторан":**
- Кафе
- Ресторан
- Семейный ресторан

### 2. Стандартизация полей

Все поля должны быть:
- ✅ Четко определены (enum, boolean, number, string)
- ✅ Не кастомными текстовыми полями
- ✅ С предустановленными значениями где возможно
- ✅ С опциональными расширенными описаниями (дополнительное поле)

### 3. Пример структуры данных

```typescript
interface VenueProfile {
  // Универсальные поля
  capacity_min: number
  capacity_max: number
  area_sqm: number
  parking: boolean
  
  // Тип-специфичные поля (conditional types)
  venue_type: 'kids_center' | 'loft' | 'cafe' | 'entertainment_center' | 'outdoor' | 'trampoline_center' | 'other'
  venue_subtype?: string // Для подтипов
  
  // Специфичные детали (зависят от venue_type)
  details: {
    // Общие
    kitchen_type?: 'full' | 'mini' | 'none'
    equipment?: Record<string, boolean>
    
    // Для детского центра
    age_groups?: string[]
    animator_room?: boolean
    
    // Для лофта
    interior_style?: string
    adult_tables?: number
    kids_tables?: number
    
    // Для кафе/ресторана
    menu_type?: string[]
    private_hall?: boolean
    
    // И т.д. по типам
  }
}
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Обновить типы в TypeScript
2. ✅ Создать формы для каждого подтипа
3. ✅ Обновить визуализацию с новыми типами
4. ✅ Добавить валидацию полей
5. ✅ Создать миграцию БД для новых полей

