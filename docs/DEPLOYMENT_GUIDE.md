# 🚀 Инструкция по развертыванию расширенной структуры площадок

## 📋 Предварительные требования

- ✅ Доступ к Supabase Dashboard или Supabase CLI
- ✅ Права на выполнение SQL миграций
- ✅ Node.js и npm установлены
- ✅ Проект локально склонирован

---

## 🔧 Шаги развертывания

### Шаг 1: Применение SQL миграции

#### Вариант A: Через Supabase Dashboard (рекомендуется)

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите свой проект ZumZam
3. Перейдите в **SQL Editor**
4. Создайте новый запрос
5. Скопируйте содержимое файла `supabase/migrations/20241208_add_venue_types.sql`
6. Вставьте в редактор
7. Нажмите **Run** (или **Ctrl+Enter**)
8. Дождитесь сообщения об успешном выполнении

#### Вариант B: Через Supabase CLI

```bash
# Установите Supabase CLI (если еще не установлен)
npm install -g supabase

# Войдите в Supabase
supabase login

# Подключитесь к проекту
supabase link --project-ref YOUR_PROJECT_REF

# Примените миграции
supabase db push
```

#### Вариант C: Через psql (для продвинутых)

```bash
# Подключитесь к БД
psql "postgresql://postgres:[YOUR_PASSWORD]@db.[YOUR_PROJECT_REF].supabase.co:5432/postgres"

# Выполните миграцию
\i supabase/migrations/20241208_add_venue_types.sql
```

### Шаг 2: Проверка применения миграции

После выполнения миграции проверьте:

```sql
-- Проверка новых типов
SELECT unnest(enum_range(NULL::venue_subtype));

-- Должно вернуть 33 значения

-- Проверка статистики
SELECT * FROM venue_types_stats;

-- Проверка функции
SELECT get_venue_type_label('trampoline_park');
-- Должно вернуть: "Батутный центр"
```

### Шаг 3: Обновление локальных зависимостей

```bash
# Убедитесь, что находитесь в корне проекта
cd D:\CODES\ZumZam

# Обновите зависимости (если нужно)
npm install

# Проверьте сборку
npm run build
```

### Шаг 4: Тестирование в dev режиме

```bash
# Запустите dev сервер
npm run dev

# Откройте браузер
# http://localhost:3000/create-profile
```

### Шаг 5: Тестирование функционала

1. **Создание профиля:**
   - Перейдите на `/create-profile`
   - Выберите категорию "Площадка"
   - Убедитесь, что видны 33 подтипа, разбитые на 9 групп
   - Попробуйте поиск

2. **Заполнение характеристик:**
   - Выберите любой новый тип (например, "Батутный центр")
   - Заполните базовые характеристики
   - Проверьте сохранение

3. **Проверка в БД:**
   ```sql
   -- Проверьте последний созданный профиль
   SELECT 
     display_name,
     category,
     details->>'subtype' as subtype,
     details
   FROM profiles
   WHERE category = 'venue'
   ORDER BY created_at DESC
   LIMIT 1;
   ```

---

## 🧪 Тестовые сценарии

### Тест 1: Создание батутного центра

```typescript
// Ожидаемые данные в details:
{
  subtype: 'trampoline_park',
  capacity_max: 50,
  trampoline_zones_count: 15,
  foam_pit: true,
  climbing_wall: true,
  ninja_course: true,
  coaches_available: true,
  parking: true,
  wifi: true
}
```

### Тест 2: Поиск "батутный центр"

1. Создайте профиль батутного центра
2. Подождите ~30 сек (генерация embeddings)
3. Выполните поиск: "батутный центр с поролоновой ямой"
4. Профиль должен появиться в результатах

### Тест 3: Фильтрация по типу

```typescript
// В поисковой форме
filters = {
  category: 'venue',
  venue_subtype: 'trampoline_park',
  city: 'Москва'
}
```

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: Ошибка при добавлении enum values

**Симптом:** `ERROR: enum label "trampoline_park" already exists`

**Решение:**
```sql
-- Проверьте существующие значения
SELECT unnest(enum_range(NULL::venue_subtype));

-- Если значение уже есть, пропустите ADD VALUE для него
```

### Проблема 2: Компонент не отображается

**Симптом:** Пустой экран на выборе подтипа

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что файл `subtype-selection-step.tsx` импортирован в wizard
3. Проверьте, что `categoryHasSubtypes('venue')` возвращает `true`

```bash
# Перезапустите dev сервер
npm run dev
```

### Проблема 3: Типы TypeScript не подхватываются

**Симптом:** Ошибки TypeScript в IDE

**Решение:**
```bash
# Перегенерируйте типы из Supabase
npx supabase gen types typescript --project-id YOUR_PROJECT_ID > types/supabase.ts

# Перезапустите TypeScript сервер в VS Code/Cursor
# Ctrl+Shift+P > "TypeScript: Restart TS Server"
```

### Проблема 4: Embeddings не обновляются

**Симптом:** Поиск не находит профили по новым характеристикам

**Решение:**
```sql
-- Обновите embeddings для существующих профилей
UPDATE profiles
SET embedding = NULL
WHERE category = 'venue';

-- Embeddings будут перегенерированы автоматически при следующем обновлении профиля
```

---

## 📊 Мониторинг после развертывания

### Метрики для отслеживания:

1. **Распределение по типам:**
```sql
SELECT 
  details->>'subtype' as venue_type,
  COUNT(*) as count
FROM profiles
WHERE category = 'venue'
GROUP BY details->>'subtype'
ORDER BY count DESC;
```

2. **Заполненность характеристик:**
```sql
SELECT 
  details->>'subtype' as venue_type,
  AVG(CASE WHEN details->>'capacity_max' IS NOT NULL THEN 1 ELSE 0 END) as has_capacity,
  AVG(CASE WHEN details->>'area_sqm' IS NOT NULL THEN 1 ELSE 0 END) as has_area,
  AVG(CASE WHEN details->>'parking' = 'true' THEN 1 ELSE 0 END) as has_parking
FROM profiles
WHERE category = 'venue'
GROUP BY details->>'subtype';
```

3. **Качество поиска:**
```sql
-- Логируйте поисковые запросы и кликабельность результатов
-- Анализируйте CTR (Click-Through Rate)
```

---

## 🔄 Откат изменений (если нужно)

### Откат SQL миграции:

```sql
-- ВНИМАНИЕ: Это удалит новые типы и данные!
-- Используйте только в крайнем случае

-- 1. Обновите профили с новыми типами
UPDATE profiles 
SET details = jsonb_set(
  details::jsonb, 
  '{subtype}', 
  '"other"'
)
WHERE category = 'venue' 
  AND details->>'subtype' NOT IN ('kids_center', 'loft', 'cafe', 'entertainment_center', 'outdoor', 'other');

-- 2. Удалите view и функции
DROP VIEW IF EXISTS venue_types_stats;
DROP FUNCTION IF EXISTS get_venue_type_label;

-- 3. Удалите индекс
DROP INDEX IF EXISTS idx_profiles_venue_subtype;

-- Enum values нельзя удалить в PostgreSQL без пересоздания типа
```

### Откат кода:

```bash
# Вернитесь к предыдущему коммиту
git checkout HEAD~1 -- components/features/profile/
git checkout HEAD~1 -- lib/constants/
git checkout HEAD~1 -- lib/ai/
git checkout HEAD~1 -- types/

# Или используйте конкретный коммит
git revert <commit-hash>
```

---

## ✅ Чек-лист развертывания

- [ ] SQL миграция применена успешно
- [ ] 33 типа площадок доступны в БД
- [ ] Функция `get_venue_type_label()` работает
- [ ] View `venue_types_stats` создан
- [ ] Локальный dev сервер запускается без ошибок
- [ ] Компонент выбора подтипа отображается
- [ ] Поиск работает корректно
- [ ] Форма характеристик открывается для всех типов
- [ ] Данные сохраняются в БД
- [ ] Embeddings генерируются с новыми полями
- [ ] TypeScript типы корректны
- [ ] Линтер не выдает ошибок

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи Supabase Dashboard → Logs
2. Проверьте консоль браузера (F12)
3. Проверьте терминал dev сервера
4. Откройте issue в репозитории

---

## 🎉 Готово!

После успешного развертывания:
- ✅ Доступно 33 типа площадок
- ✅ 200+ полей для характеристик
- ✅ Улучшенный векторный поиск
- ✅ Богатые embeddings
- ✅ Стандартизированная структура

**Следующий шаг:** Начните создавать профили для новых типов площадок!

