# 🤖 AI Помощник - Быстрый старт

## ✅ Что работает

1. **Векторный поиск** - AI находит профили по смыслу запроса
2. **Умный чат** - контекстные ответы с учетом истории
3. **Рекомендации** - AI предлагает конкретные профили со ссылками
4. **Мобильный UI** - кнопка в нижнем меню, модалка в стиле Яндекса
5. **Markdown форматирование** - жирный, курсив, списки, ссылки
6. **Быстрые вопросы** - 3 варианта следующих вопросов после каждого ответа
7. **Короткие ответы** - до 120 слов, структурированные и по делу

## 🚀 Как протестировать

### 1. Откройте сайт в мобильном режиме
```
http://localhost:4000
```

### 2. Нажмите на оранжевую кнопку AI в центре нижнего меню

### 3. Попробуйте примеры вопросов:
- 🎂 "Помоги организовать день рождения для ребенка 7 лет"
- 🎪 "Какие шоу-программы есть для детей 5-6 лет?"
- 🎨 "Где провести мастер-класс для детей?"

### 4. Или задайте свой вопрос:
- "Ищу студию для праздника в Петроградском районе"
- "Нужен аниматор на детский день рождения, бюджет 5000"
- "Хочу квест для детей 10 лет"

## 🔍 Что происходит под капотом

1. **Ваш запрос** → Gemini создает embedding (вектор 768D)
2. **Векторный поиск** → PostgreSQL ищет похожие профили через `pgvector`
3. **Контекст** → Найденные профили форматируются для AI
4. **Ответ** → Gemini генерирует персонализированную рекомендацию

## 📊 Параметры (можно настроить)

**Файл:** `app/api/ai/chat/route.ts`

```typescript
// Векторный поиск
match_threshold: 0.5  // Минимальная похожесть (0-1)
match_count: 5        // Сколько профилей искать

// AI генерация
temperature: 0.7      // Креативность (0-1)
maxOutputTokens: 512  // Длина ответа (уменьшено для краткости)
```

## 🎨 Форматирование ответов

AI использует Markdown, который рендерится в красивый HTML:

- **Жирный** для названий профилей → `<strong>`
- *Курсив* для деталей (цены, возраст) → `<em>`
- Списки (- или 1.) → `<ul>` / `<ol>`
- Ссылки [Название](/profiles/slug) → кликабельные `<Link>`

## 💬 Быстрые вопросы

После каждого ответа AI предлагает 3 варианта следующих вопросов:
- Кнопки появляются под последним сообщением AI
- Клик по кнопке → автоматическая отправка вопроса
- Примеры генерируются на основе контекста разговора

## 🐛 Если что-то не работает

### Ошибка: "AI request failed"
Проверьте `GEMINI_API_KEY` в `.env.local`:
```bash
GEMINI_API_KEY=AIzaSy...
```

### Ошибка: "Bucket not found"
Проверьте Supabase Storage bucket `profile-images`:
```sql
SELECT * FROM storage.buckets WHERE id = 'profile-images';
```

### Нет результатов поиска
Проверьте, есть ли embeddings у профилей:
```sql
SELECT COUNT(*) FROM profiles WHERE embedding IS NOT NULL;
```

Если нет, нужно сгенерировать embeddings для всех профилей.

## 📝 Следующие шаги

1. ✅ Базовый чат работает
2. ✅ Векторный поиск интегрирован
3. ✅ Markdown форматирование
4. ✅ Быстрые вопросы
5. ⏳ Добавить сохранение истории чата в БД
6. ⏳ Генерация embeddings для всех профилей
7. ⏳ Интеграция с корзиной (добавление из чата)
8. ⏳ Персонализация на основе избранного

## 💡 Полезные команды

### Проверить векторный поиск вручную:
```sql
SELECT * FROM search_profiles_by_vector(
  (SELECT embedding FROM profiles WHERE slug = 'kidspoint' LIMIT 1),
  0.5,
  5
);
```

### Посмотреть логи AI:
- Открыть DevTools (F12)
- Вкладка Console
- Смотреть `[AI Chat]` логи

## 🎉 Готово!

AI Помощник готов к использованию. Протестируйте и дайте фидбек! 🚀

