# 📋 Changelog - Версия Профили

## 🎉 Последние изменения (10.12.2025)

### 🎨 ИСПРАВЛЕНО: Галерея только когда нужна!

**Проблема:** Галерея профилей показывалась в каждом сообщении AI, даже когда не нужна.

**Примеры лишней галереи:**
```
❓ "Обобщи отзывы об этом месте"
❌ AI: (текст + галерея KidsPoint) - зачем?

❓ "Что входит в пакет Премиум?"
❌ AI: (текст + галерея) - избыточно
```

**Решение:** Умная логика - галерея только при поиске/подборке!

**Когда галерея ПОКАЗЫВАЕТСЯ:**
```typescript
const isSearchQuery = 
  /покажи|найди|порекоменду|подбер|предложи|есть ли|
   где найти|для.*лет|бюджет|праздник|студи|аниматор|
   квест|подборк|вариант/i.test(message)

if (isSearchQuery && enrichedProfiles.length > 0) {
  // Показываем галерею
}
```

✅ "Покажи студии для 10-летнего" → **галерея нужна**
✅ "Найди аниматоров с выездом" → **галерея нужна**
✅ "Порекомендуй что-то бюджетное" → **галерея нужна**
✅ "Есть ли квесты?" → **галерея нужна**

**Когда галерея НЕ показывается:**
```typescript
// Если пользователь на странице профиля
if (currentPageProfile) {
  const aboutCurrentProfile = 
    /расскажи|обобщи|отзыв|что входит|какие услуг|
     подробнее|про это место/i.test(message)
  
  if (aboutCurrentProfile) {
    // Галерея НЕ нужна
  }
}
```

❌ "Обобщи отзывы" (на странице профиля) → **галерея не нужна**
❌ "Расскажи об этом месте" → **галерея не нужна**
❌ "Что входит в пакет?" → **галерея не нужна**
❌ "Какие услуги здесь?" → **галерея не нужна**

**Результат:**
✅ Чистый UI - галерея только по делу
✅ Быстрее загрузка - меньше данных
✅ Логичнее - галерея = выбор из вариантов

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (умная логика галереи)

### 📍 AI теперь понимает где вы находитесь!

**Проблема:** Если открыть AI помощник на странице профиля (например, KidsPoint) и спросить "Расскажи об этом месте", AI не понимал о чем речь.

**Решение:** AI теперь видит текущую страницу и понимает контекст!

**🐛 ИСПРАВЛЕНО (10.12.2025):** 
- Next.js 15 сделал `params` асинхронными, из-за чего API не работал. Добавил `await params`.
- Добавлены подробные логи для отладки загрузки профиля в браузере и на сервере.

**Как работать:**

1. **Определение страницы**:
   ```typescript
   const pathname = usePathname()
   const profileMatch = pathname?.match(/\/profiles\/([^\/]+)/)
   if (profileMatch) {
     // Загружаем профиль по slug
     const profile = await fetch(`/api/profiles/by-slug/${slug}`)
     setCurrentProfile(profile)
   }
   ```

2. **Передача контекста в AI**:
   ```typescript
   await fetch('/api/ai/chat', {
     body: JSON.stringify({
       message: question,
       currentProfileId: currentProfile?.id // Передаем ID
     })
   })
   ```

3. **AI видит контекст**:
   ```
   📍 ВАЖНО: Пользователь сейчас находится на странице 
   профиля "KidsPoint"!
   
   Когда пользователь спрашивает:
   - "Расскажи об этом месте" → рассказывай о KidsPoint
   - "Обобщи отзывы" → обобщай отзывы о KidsPoint
   - "Что входит в пакет?" → описывай пакеты KidsPoint
   ```

**Примеры работы:**

На странице **KidsPoint**:
- ❓ "Расскажи об этом месте" 
- ✅ AI: "KidsPoint - это детская студия рисования..."

- ❓ "Обобщи отзывы"
- ✅ AI: "Родители отмечают что в KidsPoint..."

- ❓ "Какие пакеты здесь есть?"
- ✅ AI: "В KidsPoint есть 3 пакета: Эконом, Стандарт, Премиум..."

На **главной странице**:
- ❓ "Покажи студии для 10-летнего"
- ✅ AI: "Вот отличные варианты..." (обычный поиск)

**Технические детали:**
- Работает только если авторизован (иначе нет профиля)
- Определяется по URL `/profiles/[slug]`
- Контекст автоматически обновляется при переходе
- Работает во всех браузерах и вкладках одновременно

**Файлы:**
- `components/features/ai/chat-widget.tsx` ✅ (определение страницы)
- `app/api/profiles/by-slug/[slug]/route.ts` ✅ (новый endpoint)
- `app/api/ai/chat/route.ts` ✅ (контекст в промпте)

### 💾 История чата теперь сохраняется!

**Проблема:** При перезагрузке страницы или переходе в другой браузер история чата терялась.

**Решение:** Добавил сохранение всей переписки в базу данных!

**Что работает:**
✅ История привязана к аккаунту (не к сессии)
✅ Переписка сохраняется автоматически при каждом сообщении
✅ Загружается при открытии чата (последние 50 сообщений)
✅ Работает между браузерами и устройствами
✅ Кнопка "Новый чат" очищает историю
✅ AI использует последние 20 сообщений для контекста

**Технические детали:**
- Таблица `ai_chat_messages` (user_id, role, content, suggestions, gallery)
- API endpoints:
  - `GET /api/ai/chat/history` - загрузить историю
  - `POST /api/ai/chat/history` - сохранить сообщение
  - `DELETE /api/ai/chat/history` - очистить историю (новый чат)
- RLS политики для безопасности
- Индексы для быстрой загрузки

**Файлы:**
- `supabase/migrations/create_ai_chat_history.sql` ✅ (новая таблица)
- `app/api/ai/chat/history/route.ts` ✅ (новый API)
- `components/features/ai/chat-widget.tsx` ✅ (интеграция)

### 📝 Улучшенное форматирование ответов AI!

**Проблема:** AI писал сплошным текстом, трудно читать рекомендации.

**Решение:** Обновил промпт - теперь AI использует структурированные списки!

**Было (сплошной текст):**
```
Квест-программа "Поиск сокровищ" от Радуга Лофт - это отличный 
вариант для активных детей. Стоимость 8000₽, длительность 90 минут...
```

**Стало (структурированный список):**
```
Вот отличные варианты для вас:

1. **Радуга Лофт** - Квест-программа "Поиск сокровищ" (8,000₽)
   - Что входит: квест, реквизит, призы
   - Возраст: 8-12 лет
   - *"Дети в восторге! Очень интересно"*

2. **Super Party СПб** - Праздник под ключ "Стандарт" (18,000₽)
   - Что входит: аниматоры, игры, дискотека
   - Возраст: 6-12 лет
```

**Преимущества:**
✅ Легко читать и сравнивать
✅ Четкая структура (название - услуга - цена)
✅ Все детали по пунктам
✅ Цитаты из отзывов выделены курсивом

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (обновлен промпт)

### 🎯 ИСПРАВЛЕНО: Фильтрация доп. услуг в галерее!

**Проблема:** В галерею попадали дополнительные услуги типа "Дополнительное время", "Тематический декор", которые имеют смысл только вместе с основными.

**Решение:** Добавил умную фильтрацию с приоритетами!

**Приоритеты выбора услуги для галереи:**

1. **ПРИОРИТЕТ 1: Пакетные тарифы** (`package_group_id`)
   - Эконом, Стандарт, Премиум и т.д.
   - Всегда показываем пакеты, если есть

2. **ПРИОРИТЕТ 2: Праздники под ключ**
   - `tags` содержит "под ключ"
   - Или в названии есть "под ключ"

3. **ПРИОРИТЕТ 3: Основные услуги**
   - **Фильтруем:**
     - ❌ НЕ доп. услуги (`tags` не содержит "доп", "дополнительная")
     - ❌ НЕ пакеты (`package_group_id` пустое)
     - ❌ Название не содержит "дополнительн"
   - Берем самую дорогую основную услугу

**Что теперь показывается:**
✅ Пакетные предложения (Эконом, Стандарт, Премиум)
✅ Праздники под ключ
✅ Основные услуги (квесты, аниматоры, шоу)
❌ Дополнительные услуги (декор, доп. время, фото)

**Технические изменения:**
- Добавлен `tags` и `package_group_id` в загрузку услуг
- Обновлена логика `enrichedProfiles` (используем `packageTiers`)
- Умная фильтрация в формировании галереи

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (фильтрация + приоритеты)

### 🖱️ Drag-to-scroll для галереи (десктоп)!

**Проблема:** В десктопной версии неудобно листать галерею скроллбаром.

**Решение:** Добавил drag-to-scroll - зажми мышку и перетаскивай галерею влево-вправо!

**Как работает:**
1. Наведи на галерею → курсор меняется на `grab` (рука)
2. Зажми левую кнопку мыши → курсор `grabbing`
3. Двигай мышкой влево-вправо → галерея плавно скроллится
4. Отпусти мышку → скролл останавливается

**🐛 ИСПРАВЛЕНО: Случайные клики после drag**
- **Проблема**: При отпускании мыши после перетаскивания срабатывал клик → открывался профиль
- **Решение**: Добавил флаг `hasMoved` - если сдвинули больше 5px, считается drag, клик не срабатывает
- **Логика**:
  ```typescript
  // При движении
  if (Math.abs(walk) > 5) {
    setHasMoved(true) // Это drag, не клик
  }
  
  // При клике
  if (hasMoved) {
    e.preventDefault() // Блокируем переход
  }
  ```

**Технические детали:**
- `onMouseDown` → запоминаем позицию
- `onMouseMove` → двигаем скролл (с множителем x2 для скорости)
- `onMouseUp/onMouseLeave` → останавливаем
- Если движение < 5px → это клик, открываем профиль ✅
- Если движение > 5px → это drag, клик блокируется ❌
- `setTimeout(100ms)` для сброса `hasMoved` после отпускания
- Скрыт скроллбар для чистого UI

**Компонент:**
- `components/features/ai/draggable-gallery.tsx` ✅ (обновлен)
- `components/features/ai/chat-widget.tsx` ✅ (интеграция)

**UX улучшения:**
✅ Удобно листать длинные галереи
✅ Интуитивно понятно (курсор `grab`)
✅ НЕ мешает кликам по карточкам (если не двигал мышку)
✅ Плавная анимация

### 🖼️ ИСПРАВЛЕНО: Изображения в галерее!

**Проблема:** Галерея показывалась, но изображения не загружались (пустые плейсхолдеры).

**Причина:** RPC функция `search_profiles_by_vector` не возвращала поле `cover_photo` из базы данных.

**Решение:**
1. Обновил RPC функцию, добавил `cover_photo` в возвращаемые поля:
   ```sql
   CREATE OR REPLACE FUNCTION search_profiles_by_vector(...)
   RETURNS TABLE (
     ...,
     cover_photo text,  -- Добавлено!
     ...
   )
   ```

2. Добавил `cover_photo` в fallback поиск (ILIKE):
   ```typescript
   .select('id, display_name, ..., cover_photo')
   ```

**Теперь работает:**
✅ API возвращает `cover_photo` в галерее
✅ Изображения отображаются в карточках
✅ Работает в мобильной и десктопной версии

**Файлы:**
- `supabase/migrations/add_cover_photo_to_vector_search_v2.sql` ✅ (новая миграция)
- `app/api/ai/chat/route.ts` ✅ (fallback с `cover_photo`)

### 🏗️ Архитектура галереи переработана!

**Старая проблема:**
- AI генерировал JSON руками: `[GALLERY][{"id":"..."}][/GALLERY]`
- Клиент парсил это через `JSON.parse()` → ошибки, обрывы
- Разные подходы в мобильной и десктопной версиях
- AI мог ошибиться в синтаксисе JSON

**Новая архитектура (по твоей идее!):**

1. **API формирует галерею (`/api/ai/chat`)**:
   ```typescript
   // После векторного поиска
   const gallery = enrichedProfiles.slice(0, 5).map(p => ({
     id: bestService?.id,
     profileId: p.id,
     profileName: p.display_name,
     profileSlug: p.slug,
     serviceTitle: bestService?.title,
     image: p.cover_photo || '/placeholder.jpg',
     price: bestService?.price,
     rating: p.rating
   }))
   
   return { response, suggestions, gallery }
   ```

2. **AI только пишет текст**:
   - Убрал из промпта инструкции про `[GALLERY]`
   - AI не генерирует JSON
   - Просто описывает профили текстом

3. **Клиент получает готовую галерею**:
   ```typescript
   const data = await response.json()
   setMessages([...prev, {
     role: 'assistant',
     content: data.response,
     gallery: data.gallery // Готовый массив!
   }])
   ```

4. **Единый рендеринг (мобильная + десктоп)**:
   ```tsx
   {msg.gallery && msg.gallery.length > 0 && (
     <div className="flex gap-3 overflow-x-auto">
       {msg.gallery.map(item => (
         <Link href={`/profiles/${item.profileSlug}`}>
           {/* Карточка */}
         </Link>
       ))}
     </div>
   )}
   ```

**Преимущества:**
✅ AI не может ошибиться в JSON синтаксисе
✅ Галерея всегда корректная
✅ Единый код для мобильной и десктопа
✅ API контролирует какие профили показывать
✅ Легко добавлять поля (цену, рейтинг, фото)

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (формирование `gallery`)
- `components/shared/mobile-bottom-tabs.tsx` ✅ (убран парсинг)
- `components/features/ai/chat-widget.tsx` ✅ (десктоп)

### 🚀 AI теперь выдает полные ответы + галерея работает!

**Проблема:** AI ответы обрывались на середине, а галерея показывалась как текст `[GALLERY]...` вместо карточек.

**Причины:**
1. `maxOutputTokens: 600` - слишком мало для длинных ответов с галереей
2. Десктопный чат парсил галерею из текста, хотя API возвращает её отдельно

**Решение:**
✅ Увеличил `maxOutputTokens` с 600 до **2048**
✅ Добавил поле `gallery` в интерфейс `Message`
✅ Сохраняем `data.gallery` из API в сообщение
✅ Рендерим галерею из поля `msg.gallery`, а не из текста

**Что работает теперь:**
- AI выдает полные развернутые ответы
- Галерея профилей показывается красивыми карточками
- Карточки кликабельны → ведут в профиль
- Работает в мобильной и десктопной версии

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (`maxOutputTokens: 2048`)
- `components/features/ai/chat-widget.tsx` ✅ (галерея из API)

### 💻 Десктопный AI чат теперь работает!

**Проблема:** В левом нижнем углу на десктопе была оранжевая кнопка AI, но она была ссылкой на `/ai-search` и не работала.

**Решение:** Заменил на полноценный чат как в мобильной версии!

✅ **Что работает:**
- Оранжевая кнопка в левом нижнем углу (десктоп)
- Открывается окно чата 396×600px
- Те же функции что в мобильной версии:
  - ✨ AI рекомендации с векторным поиском
  - 🎨 Горизонтальная галерея профилей
  - 🎤 Голосовой ввод (только на HTTPS)
  - 🛒 Function Calling для корзины
  - 💬 Контекстные предложения
- Клик по карточке → переход в профиль → чат закрывается

**Файлы:**
- `components/features/ai/chat-widget.tsx` ✅ (полностью переписан)
- `app/(public)/layout.tsx` ✅ (добавлен ChatWidget)

### 🎤 Кнопка микрофона скрыта на HTTP

**Проблема:** На Android в браузере через HTTP (`localhost:4000`) микрофон не работал, выдавал ошибки.

**Причина:** Браузеры блокируют доступ к микрофону на незащищенных HTTP соединениях.

**Решение:** 
- Кнопка микрофона показывается **только на HTTPS** (`window.isSecureContext`)
- На локальном HTTP кнопка скрыта, ошибок нет
- На продакшене с HTTPS всё работает

**Файлы:**
- `components/shared/mobile-bottom-tabs.tsx` ✅
- `components/features/ai/chat-widget.tsx` ✅

### 🎨 НОВИНКА: Горизонтальная галерея профилей в AI чате!

**Теперь AI показывает карточки профилей прямо в чате!**

#### Как работает:
1. **Пользователь спрашивает**: "Хочу аниматора в костюме Эльзы"
2. **AI находит варианты** и показывает:
   - Текстовое описание с ценами и деталями
   - **Горизонтальную галерею карточек** с фото, рейтингом, ценой
3. **Листаешь влево-вправо**, смотришь кто лучше выглядит
4. **Кликаешь по карточке** → переход в профиль

#### Дизайн галереи:
- 📱 Горизонтальный скролл (свайп влево-вправо)
- 🖼️ Карточки 192px с фото профиля
- ⭐ Рейтинг в углу
- 💰 Цена и название услуги
- 🎯 Клик → переход в профиль

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (добавлен формат [GALLERY])
- `components/shared/mobile-bottom-tabs.tsx` ✅ (парсинг и отрисовка галереи)

**Примеры использования:**
```
👤 "Покажи аниматоров с Эльзой"
🤖 "Вот несколько отличных вариантов:
     
     [Горизонтальная галерея с 3-5 карточками]
     
     Какой больше понравился?"
```

### 🛒 КРИТИЧЕСКОЕ: Корзина теперь обновляется после операций AI!

#### Проблема
AI говорил "Очистил корзину! 🗑️", но корзина оставалась полной.
Пользователь видел старые товары, несмотря на операции AI (add_to_cart, remove_from_cart, clear_cart).

#### Причина
После вызова функций корзины на сервере, клиентская часть НЕ обновляла свое состояние.
Корзина менялась в БД, но UI показывал устаревшие данные.

#### Решение
**Добавлено автоматическое обновление корзины после каждого ответа AI:**

```typescript
// После получения ответа от AI
if (isAuthenticated) {
  await fetchCart() // Обновляем корзину из БД
  console.log('[AI Chat] Cart refreshed after AI operation')
}
```

**Теперь:**
- ✅ AI очищает корзину → UI сразу обновляется
- ✅ AI добавляет товар → товар сразу появляется
- ✅ AI удаляет товар → товар сразу исчезает

**Файлы:**
- `components/shared/mobile-bottom-tabs.tsx` ✅
  - Добавлен импорт `useCartStore`
  - Добавлено `fetchCart()` после `handleSendMessage`
  - Добавлено `fetchCart()` после `handleQuickQuestion`

### ❤️ КРИТИЧЕСКОЕ: AI теперь живой человек, а не робот!

#### Проблема
AI показывал технические коды пользователям:
```
add_to_cart(serviceId="dfe9f7e7-3760-4a3e-b71f-40c5396efd6f", notes="Премиум")
```

Это выглядело как ошибка, пугало пользователей и разрушало доверие.

#### Решение
**ПОЛНАЯ ПЕРЕРАБОТКА СТИЛЯ ОБЩЕНИЯ:**

**Было (техническое):**
```
[ВЫЗЫВАЮ add_to_cart(serviceId="dfe9f7e7-...", notes="Премиум")]
✅ Добавил! Пакет Премиум в корзине (30,000₽)
```

**Стало (человечное):**
```
Готово! ✨ Добавила пакет Премиум от Kids Point (30,000₽). 
Это 3 часа праздника для 20 детей с двумя аниматорами, 
фотографом и кучей творчества! Что еще добавим?
```

#### Новый стиль общения:
- ❤️ Теплый, заботливый, эмпатичный
- 🎯 Задает уточняющие вопросы о ребенке
- 🚫 НИКОГДА не показывает технические коды, UUID, ID
- ✨ Использует эмоциональную окраску
- 👂 Слушает и понимает потребности родителя

#### Примеры вопросов от AI:
- "Сколько лет вашему малышу?"
- "Какие у него/нее интересы?"
- "Есть ли особенности? Может, стеснительный или очень активный?"
- "Сколько будет гостей?"
- "Какой у вас бюджет? Не стесняйтесь!"

**Файлы:**
- `app/api/ai/chat/route.ts` ✅ (полная переработка промпта)

### 🔄 КРИТИЧЕСКАЯ РЕСТРУКТУРИЗАЦИЯ: Отдельные ID для каждого тарифа пакета

#### Проблема
Пакетные предложения (Эконом, Стандарт, Премиум) хранились как ОДИН сервис с массивом тарифов в JSONB.
AI не мог напрямую добавить конкретный тариф, т.к. у него не было отдельного UUID.

#### Решение
**ПОЛНАЯ РЕСТРУКТУРИЗАЦИЯ ДАННЫХ:**

**Было:**
```
1 запись:
id: dfe9f7e7-...
title: "Пакетные предложения"
details: {
  tier_packages: [
    {name: "Эконом", price: 20000, ...},
    {name: "Стандарт", price: 26000, ...},
    {name: "Премиум", price: 30000, ...}
  ]
}
```

**Стало:**
```
3 отдельные записи:
id: uuid1, title: "Пакетные предложения - Эконом", package_group_id: group1, tier_name: "Эконом"
id: uuid2, title: "Пакетные предложения - Стандарт", package_group_id: group1, tier_name: "Стандарт"
id: uuid3, title: "Пакетные предложения - Премиум", package_group_id: group1, tier_name: "Премиум"
```

#### Миграция выполнена:
- ✅ Добавлены поля: `package_group_id`, `tier_name`, `tier_order`
- ✅ Мигрированы все существующие пакеты (3 пакета → 9 записей)
- ✅ Старые пакеты помечены как `[MIGRATED]` и деактивированы
- ✅ Перегенерированы embeddings для всех профилей (18 updated)

#### Преимущества новой архитектуры:
- ✅ AI может напрямую добавлять тарифы по UUID
- ✅ Упрощена логика корзины (не нужен `custom_price`)
- ✅ Проще отчетность и аналитика
- ✅ Единообразная структура данных

**Миграция:**
- `supabase/migrations/20251210100000_restructure_package_tiers.sql` ✅
- `scripts/migrate-package-tiers.ts` ✅ (выполнен)

### ⚠️ Архитектурная проблема: Пакетные тарифы

#### Текущая ситуация
**Пакетные предложения** хранятся как ОДНА услуга с массивом тарифов в JSONB `details.tier_packages`:
```json
{
  "id": "dfe9f7e7-...",
  "title": "Пакетные предложения",
  "details": {
    "tier_packages": [
      { "name": "Эконом", "price": 20000, ... },
      { "name": "Стандарт", "price": 26000, ... },
      { "name": "Премиум", "price": 30000, ... }
    ]
  }
}
```

#### Проблема для AI Function Calling
AI не может напрямую добавить конкретный тариф в корзину, т.к. каждый тариф НЕ имеет своего UUID.

#### Временное решение (текущее)
- AI использует ID пакета + передает тариф в `notes: "Премиум"`
- В `cart_items.custom_price` сохраняется цена выбранного тарифа

#### Долгосрочное решение (TODO)
Создать отдельные записи для каждого тарифа с общим `package_group_id`:
```sql
-- Вместо 1 записи с JSONB:
services (id: uuid1, title: "Пакетные предложения", details: {...})

-- Сделать 3 записи:
services (id: uuid1, title: "День рождения", package_group_id: group1, tier: "Эконом")
services (id: uuid2, title: "День рождения", package_group_id: group1, tier: "Стандарт")  
services (id: uuid3, title: "День рождения", package_group_id: group1, tier: "Премиум")
```

### 🔧 Исправление: Парсинг "Праздников под ключ"

#### Проблема
В `generate-profile-embedding.ts` искался несуществующий `service_type === 'turnkey'`.

#### Решение
- ✅ Исправлен фильтр: проверка `title` и `tags` на наличие "под ключ"
- ✅ Добавлена проверка `!s.is_package` чтобы не путать с пакетами
- ✅ Добавлена поддержка `details.includes` помимо `details.included`

**Файлы:**
- `lib/ai/generate-profile-embedding.ts` ✅

### 🎯 Улучшение: ПОЛНЫЙ парсинг профиля для векторного поиска

#### Что теперь включено в embeddings:

**Основная информация:**
- ✅ Название, категория, город
- ✅ Описание (bio + description)
- ✅ Теги

**Характеристики профиля (NEW!):**
- ✅ Форматы работы (выездной/студия/оба)
- ✅ Удобства (парковка, Wi-Fi, кондиционер...)
- ✅ Оборудование (проектор, звук, свет...)
- ✅ Особенности (инклюзивность, безопасность...)

**Контакты:**
- ✅ Телефон, email, сайт
- ✅ Социальные сети

**Локации и площадки:**
- ✅ Адрес, телефон, метро
- ✅ Площадь и вместимость
- ✅ Помещения и их характеристики
- ✅ Форматы работы локации
- ✅ Удобства и оборудование локации
- ✅ Специфические особенности
- ✅ Правила (еда, торт, украшения, курение, животные...)
- ✅ Условия аренды (минимум, цена/час, депозит, отмена)

**Услуги:**
- ✅ ID, название, описание, цена, возраст, длительность
- ✅ Детали программы

**Пакетные предложения:**
- ✅ ID пакета
- ✅ Все тарифы (Эконом, Стандарт, Премиум)
- ✅ Что включено в каждый тариф
- ✅ Цены, длительность, количество участников

**Праздники под ключ:**
- ✅ Название, описание, цена
- ✅ Что включено

**FAQ:**
- ✅ Вопросы и ответы

**Отзывы:**
- ✅ Внутренние отзывы платформы
- ✅ Отзывы с Яндекс.Карт

**Файлы:**
- `lib/ai/generate-profile-embedding.ts` ✅

### 🔧 Исправление: AI передает правильные ID услуг

#### Проблема
AI пытался добавлять услуги в корзину, используя названия вместо UUID:
```
Function call: add_to_cart { serviceId: 'Фотограф', notes: 'KidsPoint' }
```

#### Решение
- ✅ Обновлен system prompt с четкими инструкциями использовать UUID
- ✅ Добавлены ID услуг и пакетов в контекст профилей: `[ID: uuid]`
- ✅ Добавлено предупреждение: "НЕ используй названия или текст вместо ID!"

**Теперь:**
```
Function call: add_to_cart { 
  serviceId: 'a1b2c3d4-e5f6-...', 
  notes: 'Премиум' 
}
```

**Файлы:**
- `app/api/ai/chat/route.ts` ✅

### 🛒 AI Function Calling - Работа с корзиной

#### Новые возможности
AI-ассистент теперь может напрямую работать с корзиной пользователя:
- ✅ **Добавлять услуги в корзину** по голосовой/текстовой команде
- ✅ **Удалять услуги из корзины**
- ✅ **Показывать содержимое корзины** с общей суммой
- ✅ **Очищать корзину** полностью

#### Примеры команд
```
👤 "Добавь KidsPoint в корзину"
🤖 ✅ Добавлено в корзину: "День рождения" от KidsPoint

👤 "Добавь пакет Премиум"
🤖 ✅ Добавлено: Пакет "Премиум" (30,000₽)

👤 "Что у меня в корзине?"
🤖 🛒 В корзине 2 услуги на сумму 50,000₽

👤 "Удали KidsPoint"
🤖 🗑️ Удалено из корзины: "День рождения"
```

#### Технические детали
- **Gemini Function Calling** (нативная поддержка)
- **4 функции**: `add_to_cart`, `remove_from_cart`, `show_cart`, `clear_cart`
- **API endpoint**: `/api/cart/ai/route.ts`
- **Поддержка тарифов**: автоматически передает название (Эконом/Стандарт/Премиум)

**Файлы:**
- `app/api/cart/ai/route.ts` ✅ (новый)
- `app/api/ai/chat/route.ts` ✅ (обновлен)

### 🖼️ Исправление: Картинки профилей в AI-чате

#### Проблема
AI отправлял картинки профилей в Markdown, но они не отображались - только текст-ссылка.

#### Решение
- ✅ Добавлен обработчик `img` в `ReactMarkdown`
- ✅ Обновлен формат: картинка и ссылка на отдельных строках
- ✅ Стили: `rounded-2xl`, `shadow-md`, адаптивная ширина

**Было:**
```
[KidsPoint](/profiles/kidspoint)
```

**Стало:**
```
[Красивая картинка обложки]
[Открыть KidsPoint](/profiles/kidspoint)
```

**Файлы:**
- `components/shared/mobile-bottom-tabs.tsx` ✅

### 🔧 Исправление: Детали пакетов в AI-чате

#### Проблема
AI не показывал что входит в пакеты (Эконом, Стандарт, Премиум).
Показывались только названия тарифов и цены, но без списка включенных услуг.

#### Решение
- ✅ Обновлен контекст в `/api/ai/chat`: теперь передаются детали каждого тарифа
- ✅ Обновлен промпт: AI обязан показывать что включено
- ✅ Добавлен пример правильного ответа в промпт

**Было:**
```
KidsPoint: Эконом (20,000₽), Стандарт (26,000₽), Премиум (30,000₽)
```

**Стало:**
```
KidsPoint предлагает пакет "День рождения":
- Эконом (20,000₽): 2 часа, до 10 детей
  Включено: аниматор, материалы для рисования
- Премиум (30,000₽): 3 часа, до 20 детей
  Включено: 2 аниматора, материалы, фотограф
```

### 🛡️ Защита AI-ассистента

#### 1. Строгие ограничения темы
- ❌ Запрещено: Команды, офф-топик, изменение БД
- ✅ Разрешено: Только праздники на ZumZam

#### 2. Rate Limiting (защита от спама)
- Лимит: 20 сообщений в час
- Ошибка 429 при превышении
- Таблица: `ai_chat_rate_limits`

#### 3. Картинки профилей
- AI отправляет кликабельные обложки
- Формат: `[![Название](URL)](/profiles/slug)`

**Миграции:**
- `20251210020000_add_ai_chat_rate_limits.sql` ✅

### 🎙️ Настройки провайдера для распознавания речи (STT)

#### Новые возможности
- ✅ **Выбор провайдера STT** в админ-панели
- ✅ **Whisper (локальный)**: Бесплатный, работает без интернета
- ✅ **Gemini 2.0 Flash**: Облачный, быстрый и точный

#### Что добавлено

**1. Таблица настроек STT**
```sql
CREATE TABLE stt_settings (
  id UUID PRIMARY KEY,
  name TEXT UNIQUE,
  provider TEXT, -- 'whisper' или 'gemini'
  is_active BOOLEAN,
  settings JSONB
)
```

**2. API для управления**
- `GET /api/admin/stt-settings` - Получить все настройки
- `PATCH /api/admin/stt-settings` - Переключить провайдер

**3. Обновлен STT endpoint**
- `/api/ai/transcribe` теперь поддерживает оба провайдера
- Автоматически выбирается активный провайдер
- Fallback на Whisper если настройки не найдены

**4. Админ-панель**
- Новая страница: `/admin/stt-settings`
- Переключение между провайдерами в 1 клик
- Показывает детали каждого провайдера

#### Сравнение провайдеров

| Параметр | Whisper (локальный) | Gemini 2.0 Flash |
|----------|---------------------|------------------|
| **Скорость** | 10-15 сек | 2-3 сек |
| **Точность** | 85-90% | 95-98% |
| **Стоимость** | Бесплатно | ~$0.00001/сек |
| **Интернет** | Не требуется | Требуется |
| **API ключ** | Не требуется | Требуется |
| **Загрузка модели** | Первый раз медленно | Нет |

#### Как использовать

**Для администратора:**
1. Открой `/admin/ai-settings`
2. Прокрути до секции "Распознавание речи (STT)"
3. Выбери нужный провайдер
4. Нажми "Использовать"
5. Готово! Теперь голосовые сообщения распознаются через выбранный провайдер

**Для пользователя:**
Ничего не меняется! Просто говори в микрофон в AI-чате, и текст распознается автоматически.

#### Технические детали

**Whisper:**
- Модель: `Xenova/whisper-small` (75 MB)
- Язык: Русский
- Формат: WebM → WAV → Float32Array
- Процессинг: ffmpeg + transformers.js

**Gemini:**
- Модель: `gemini-2.0-flash-exp`
- Язык: Автоопределение (с подсказкой "русский")
- Формат: WebM base64
- API: Google Generative AI

#### Миграции
- `20251210010000_add_stt_settings.sql` ✅

**Файлы:**
- `app/api/admin/stt-settings/route.ts` (новый)
- `app/admin/ai-settings/page.tsx` (обновлен - добавлена секция STT)
- `app/api/ai/transcribe/route.ts` (обновлен)

### 🔧 Исправление: Отзывы с Яндекс.Карт в AI-чате

#### Проблема
- AI-чат не загружал отзывы с Яндекс.Карт в реальном времени
- Embedding содержал отзывы, но контекст для чата - нет
- AI говорил "нет отзывов", хотя они есть на Яндекс.Картах

#### Решение
- ✅ Добавлена загрузка отзывов с Яндекс.Карт в `/api/ai/chat`
- ✅ Отзывы загружаются через `profile_locations` → `yandex_reviews_cache`
- ✅ AI видит отдельно внутренние отзывы и отзывы с Яндекс.Карт
- ✅ В контексте указан источник: "Отзывы на платформе" и "Отзывы на Яндекс.Картах"

#### Результат
Теперь AI знает отзывы из **2 источников**:
1. Внутренние отзывы (на платформе)
2. Отзывы с Яндекс.Карт

**Пример ответа:**
```
У KidsPoint отличные отзывы! 

На платформе:
⭐ 5/5: "Дочке очень понравилось..."

На Яндекс.Картах:
⭐ 5/5 (Ирина): "Замечательная студия..."
⭐ 4/5 (Алексей): "Дети в восторге..."
```

### 🔍 Всеобъемлющий AI-поиск по ВСЕМ данным профиля

#### 📋 Что теперь включено в поиск
AI знает **ВСЁ** о каждом профиле и может искать/отвечать по **ЛЮБОМУ** параметру:

**Основная информация:**
- ✅ Название, категория, город, описание, теги

**Контакты:**
- ✅ Телефон, email, сайт, социальные сети

**Локации и площадки:**
- ✅ Адреса с телефонами
- ✅ Метро (станция + время пешком)
- ✅ Площадь помещений
- ✅ Вместимость (мин-макс человек)
- ✅ Список комнат с площадью

**Форматы и удобства:**
- ✅ Форматы работы (выездной, стационарный, онлайн)
- ✅ Удобства (парковка, гардероб, кухня)
- ✅ Оборудование (проектор, звук, декорации)
- ✅ Специфические характеристики

**Правила и условия:**
- ✅ Можно ли приносить еду/напитки/торт
- ✅ Можно ли украшать
- ✅ Курение, животные
- ✅ Минимум аренды, депозит, цена/час
- ✅ Политика отмены

**Услуги и программы:**
- ✅ Все детали каждой услуги
- ✅ Цены, длительность, возраст
- ✅ Что включено/не включено
- ✅ Программа и дополнительные опции

**Пакетные предложения:**
- ✅ Все тарифы (Эконом, Стандарт, Премиум)
- ✅ Детали каждого тарифа
- ✅ Что включено в тариф

**Праздники под ключ:**
- ✅ Полное описание
- ✅ Цены и что входит

**FAQ:**
- ✅ Все вопросы и ответы

**Отзывы:**
- ✅ Внутренние отзывы
- ✅ Отзывы с Яндекс.Карт (рейтинг, текст, автор)
- 🔜 Отзывы с 2GIS

#### 🎯 Примеры поиска
Теперь можно спросить:
- *"Студия около метро Звенигородская с парковкой"*
- *"Можно приносить свой торт?"*
- *"Что включено в программу рисования?"*
- *"Какая разница между Эконом и Премиум?"*
- *"Что говорят про чистоту в отзывах?"*
- *"До какого возраста принимают?"*

#### 📊 Статистика
- **Средний размер embedding**: 10,000-20,000 символов
- **Автообновление**: при любом изменении профиля/услуг/отзывов
- **Точность поиска**: по всем 13+ категориям данных

**Документация**: `docs/AI_COMPREHENSIVE_SEARCH.md`

### ✨ Контекстные вопросы-подсказки в AI-чате

#### 🎯 Персонализированные suggestions
- **Учитывают историю чата**: возраст ребенка, упомянутые студии, бюджет
- **Примеры**:
  - Было: "Расскажи еще" ❌
  - Стало: "Какие отзывы о KidsPoint для детей 5 лет?" ✅
- **Естественный диалог**: как разговор с живым консультантом
- **Один клик**: не нужно набирать вопрос вручную

#### 🧠 Умный анализ контекста
AI извлекает из истории:
- Возраст ребенка (если упоминался)
- Названия студий и профилей
- Интересы (рисование, квесты, аниматоры)
- Бюджет и ценовые ожидания
- Локацию и район города

#### 📝 Правила генерации
- Конкретные имена: "Какие отзывы о **KidsPoint**?"
- Учет возраста: "Подходит ли для ребенка **5 лет**?"
- Развитие темы: обсуждали цену → спроси про пакеты
- Без повторов: не дублируй уже заданные вопросы

**Документация**: `docs/AI_CONTEXTUAL_SUGGESTIONS.md`

### ✨ AI с персонализированными рекомендациями на основе отзывов

#### 💬 Отзывы в контексте AI
- **Реальный опыт клиентов** теперь используется для рекомендаций
- AI цитирует отзывы: *"Родители отмечают: 'Дети были в восторге...'"*
- Загружается до 5 последних отзывов для каждого профиля
- Только одобренные и видимые отзывы (`moderated=true`, `visible=true`)

#### 🎯 Улучшенные рекомендации
- **Контекст из услуг**: возраст, цены, длительность
- **Контекст из отзывов**: что говорят реальные родители
- **Персонализация**: AI учитывает опыт других клиентов
- **Примеры**: 
  - "По отзывам, аниматоры очень внимательные"
  - "Родители хвалят программу: 'Дети были в восторге'"

#### ⚙️ Технические улучшения
- Увеличен `maxOutputTokens` до 600 (для цитат)
- Повышена `temperature` до 0.8 (более творческие ответы)
- Загрузка до 5 услуг вместо 3
- Оптимизированный промпт с примерами использования отзывов

### ✨ Автоматическая система векторных embeddings

#### 🤖 Полная автоматизация AI поиска
- **Универсальная функция генерации**: `generateProfileEmbedding(profileId)`
  - Загружает ВСЮ информацию: услуги, пакеты, отзывы
  - Генерирует полный embedding через Gemini text-embedding-004
  - Автоматически сохраняет в `profiles.embedding`
  
- **Автоматические триггеры**:
  - ✅ При создании услуги → регенерация embedding
  - ✅ При обновлении услуги → регенерация embedding
  - ✅ При удалении услуги → регенерация embedding
  - ✅ При добавлении отзыва → регенерация embedding

- **Webhook endpoint**: `/api/webhooks/profile-updated`
  - Асинхронная обработка регенерации
  - Не блокирует основные запросы
  - Фоновая обработка через `triggerEmbeddingRegeneration()`

- **Стоимость** (очень дешево):
  - Embedding профиля: ~$0.00002 (0.002₽)
  - 1000 профилей: $0.02 (2₽)
  - Запрос пользователя: ~$0.0001 (0.01₽)
  - 10,000 запросов/день = $1 (100₽/день)

- **Документация**: `docs/AI_VECTOR_EMBEDDINGS.md`

### 🐛 Исправления

#### 🔍 Векторный поиск - исправлена структура данных
- **Проблема**: Профили не возвращали полную информацию (услуги, пакеты, отзывы)
- **Решение**: 
  - Embeddings теперь генерируются с полным контекстом
  - RPC функция `search_profiles_by_vector` исправлена (убран несуществующий `price_from`)
  - AI чат загружает только краткую инфо для отображения (детали уже в embedding)
  - Фолбэк на текстовый поиск `ILIKE` если векторный не дал результатов

#### 🎤 Whisper транскрипция - улучшено качество
- **Модель**: Обновлена с `whisper-tiny` на `whisper-small` (non-quantized)
- **Результат**: Значительно лучшее распознавание русской речи
- **Компромисс**: Немного медленнее (3-8 сек вместо 2-5 сек)

#### 🎤 Whisper транскрипция - исправлен формат данных (попытка #3)
- **Проблема #3**: `ffmpeg-static` path resolution в ESM
- **Решение #3**: `(await import('ffmpeg-static')).default` вместо `require()`
- **Проблема #4**: Объект передавался вместо Float32Array
- **Решение #4**: Передача raw `audioFloat32` напрямую в `transcriber()`

#### 🎤 Whisper транскрипция - исправлен формат данных (попытка #2)
- **Проблема #1**: `WhisperFeatureExtractor expects Float32Array, but got Buffer`
- **Решение #1**: Использование `read_audio()` (не сработало в Node.js)
- **Проблема #2**: `AudioContext is not available in your environment`
- **Решение #2**: 
  - Передача пути к файлу напрямую в `transcriber()`
  - Transformers.js самостоятельно декодирует WebM/Opus в Node.js окружении
  - Сохранение WebM во временный файл (`os.tmpdir()`)
  - Автоматическая очистка файлов после обработки
- **Конфигурация**: Обновлен `next.config.ts` для `serverExternalPackages` (Next.js 15)

---

## 🎉 Изменения (09.12.2025)

### ✨ Новые функции

#### 🎤 Голосовой ввод с Whisper транскрипцией
- **Open-source Whisper** через `@xenova/transformers` (бесплатно, локально)
- **Модель**: `Xenova/whisper-tiny` (quantized, ~75 MB)
- **Запись голоса**:
  - Кнопка микрофона в AI чате
  - `MediaRecorder API` с `audio/webm` (Opus кодек)
  - Echo cancellation, noise suppression, auto gain control
  - Индикатор записи с таймером (красная точка + секунды)
  - Кнопки "Стоп" (⏹) и "Отмена" (✖)
- **Транскрипция**:
  - Автоматическое распознавание русской речи
  - Индикатор обработки (3 прыгающих точки)
  - Время транскрипции: 2-5 секунд
  - Кеширование модели (загружается один раз)
- **UX**:
  - Кнопка микрофона появляется когда поле ввода пустое
  - Автоматическая отправка текста после транскрипции
  - Обработка ошибок (нет доступа к микрофону, не распознано)
- **API**: `POST /api/ai/transcribe` с FormData
- **Runtime**: Node.js (60 сек timeout)

#### 🤖 AI Помощник с векторным поиском
- **Интеграция с Gemini 2.0 Flash** для интеллектуального поиска и рекомендаций
- **Векторный поиск через pgvector**:
  - Embedding модель: `text-embedding-004` (768D)
  - Функция PostgreSQL `search_profiles_by_vector` для косинусного поиска
  - Автоматическое сравнение запроса пользователя с профилями в базе
  - Топ-5 самых релевантных результатов с порогом похожести 0.5
- **Умный чат-бот**:
  - История разговора (user/assistant)
  - Контекстная генерация ответов на базе найденных профилей
  - Кнопка "Новый чат" для начала новой сессии
  - Примеры вопросов для быстрого старта
  - Автоскролл к последним сообщениям
- **Красивое форматирование ответов**:
  - **Markdown → HTML**: жирный, курсив, списки, ссылки
  - Компактные ответы (до 120 слов)
  - Структура: контекст + список профилей + уточняющий вопрос
  - Кликабельные ссылки на профили (с закрытием чата)
- **Быстрые вопросы**:
  - 3 варианта следующих вопросов после каждого ответа AI
  - Кнопки под последним сообщением
  - Автоматическая отправка при клике
  - Генерация на основе контекста разговора
- **Интеграция в UI**:
  - Оранжевая кнопка AI в нижнем меню (мобильная версия)
  - Модальное окно в стиле Яндекса
  - Плавные анимации отправки/получения сообщений
  - Индикатор загрузки (3 прыгающих точки)
- **Системный промпт**:
  - AI понимает категории услуг (студии, аниматоры, шоу, квесты и т.д.)
  - Рекомендует конкретные профили со ссылками
  - Использует эмодзи для дружелюбности 🎉🎂🎈
  - Краткие и по делу ответы (2-3 абзаца, до 120 слов)

#### 🖼️ AI Расширение изображений обложки (Outpainting)
- **Интеграция с Gemini 2.5 Flash Image** (Nano Banana) для расширения изображений обложки профиля
- **5 направлений расширения**:
  - Сверху (top) - +30% пространства
  - Снизу (bottom) - +30% пространства
  - Слева (left) - +40% пространства (важно для Modern шаблона)
  - Справа (right) - +40% пространства
  - Со всех сторон (all) - +30% с каждой стороны
- **Платная функция**: 10 кредитов за одно расширение
- **Умные промпты**: AI автоматически продолжает стиль, освещение и композицию оригинального изображения
- **Автоматическое сохранение**: Результаты загружаются в Supabase Storage и привязываются к профилю

#### ✂️ Редактор кропа обложки
- **Интерактивный редактор** на базе `react-easy-crop`
- **Функции**:
  - Zoom (1x - 3x)
  - Drag & Pan изображения
  - Разные соотношения сторон для разных шаблонов:
    - Classic: 16:9 (горизонтальная)
    - Modern: 3:4 (вертикальная для левого блока)
    - Minimal: 16:9 (горизонтальная)
- **Кнопка AI расширения** прямо в редакторе для каждого направления
- **Превью в реальном времени** - видно как будет выглядеть результат до сохранения
- **Автосохранение** настроек кропа для каждого шаблона отдельно

#### 💳 Система кредитов
- **Новая таблица `credit_transactions`** для логирования всех операций с кредитами
- **Поле `credits` в таблице `users`** для хранения баланса
- **Типы транзакций**:
  - `purchase` - покупка кредитов
  - `spend` - расход (AI расширение)
  - `refund` - возврат
  - `bonus` - бонусы
- **Проверка баланса**: Система не позволит использовать AI без достаточного количества кредитов
- **История транзакций**: Полный лог всех операций с метаданными

#### 🎨 Интеграция с шаблонами профиля
- **Кнопка "Обложка"** в верхнем правом углу секции "О нас" (только для владельца)
- **Применение кропа** к отображению изображения во всех 3 шаблонах:
  - Classic
  - Modern
  - Minimal
- **Smooth transitions** при переключении между шаблонами и применении кропа

### 🛠️ Технические улучшения

#### Database
- **Миграция `20251209010000_add_cover_crop_and_ai.sql`**:
  - Добавлено поле `cover_photo_crop` (JSONB) для хранения координат кропа для каждого шаблона
  - Добавлено поле `cover_photo_ai_expanded` (JSONB) для хранения URL AI-расширенных версий
- **Миграция `20251209020000_add_credits_system.sql`**:
  - Добавлено поле `credits` в таблицу `users`
  - Создана таблица `credit_transactions` для логирования

#### API Endpoints
- **POST `/api/profiles/[id]/cover-crop`** - Сохранение настроек кропа
- **POST `/api/ai/expand-image`** - AI расширение изображения с использованием Gemini 2.5 Flash Image

#### Components
- **`CoverCropEditor`** (`components/features/profile/cover-crop-editor.tsx`) - Интерактивный редактор кропа
- **`AboutSectionClient`** - Обновлен для поддержки редактора кропа и применения настроек
- **`AboutSection`** - Добавлена поддержка применения кропа через CSS transform

#### Types
- **`CropData`** (`lib/types/crop.ts`) - Типы для координат кропа
- **`AllTemplatesCoverCropData`** - Типы для кропа всех шаблонов
- **`AiExpandedImages`** - Типы для AI-расширенных изображений

### 📚 Документация
- **`docs/AI_IMAGE_EXPANSION.md`** - Полная документация по AI расширению изображений:
  - Обзор возможностей
  - Структура базы данных
  - API endpoints с примерами
  - Описание компонентов
  - Процесс работы
  - Примеры промптов для AI
  - Безопасность и требования
  - Стоимость и расчеты
  - Будущие улучшения

### 🔧 Dependencies
- **Установлен `@google/generative-ai@^0.21.0`** для интеграции с Gemini API
- **Уже установлен `react-easy-crop@^5.5.6`** для редактора кропа

### 💰 Монетизация
- **Стоимость AI расширения**: 10 кредитов
- **Реальная стоимость через Gemini API**: ~$0.0387 за изображение
- **Пример расчета прибыли**:
  - Пользователь покупает 1000 кредитов за $5
  - Может сделать 100 AI расширений
  - Ваша прибыль: $5 - (100 × $0.0387) = $1.13 (22.6% маржа)

### 🔒 Безопасность
- **Авторизация**: Только владелец профиля может редактировать кроп и использовать AI
- **Валидация**: Все параметры проверяются на сервере
- **Списание кредитов**: Происходит только при успешной генерации
- **Rollback**: При ошибке AI кредиты не списываются

---

## 🎉 Изменения за последние сутки (32-33 часа назад)

### ✨ Новые функции

#### 🚀 Упрощение создания профиля
- **Упрощенное создание профиля** - кнопка "Создать" теперь ведет на API `/api/profiles/quick-create` с автоматическим редиректом на страницу редактирования
- **Объединение форм Create и Edit** - создана единая форма `ProfileManageClient` для создания и редактирования профилей
- **Синхронизация wizard и edit формы** - полная синхронизация всех полей между формами

#### 🎨 Улучшенный UX выбора
- **Визуальный селектор категорий** (`CategoryVisualSelector`) - красивые карточки с иконками вместо dropdown
  - 🎭 Аниматор
  - 🏰 Площадка
  - 🎪 Шоу-программа
  - 🏢 Агентство
  - 🔍 Квест
  - 🎨 Мастер-класс
  - 📸 Фотограф
- **Группированный селектор типов площадок** (`VenueTypeGroupedSelector`) - 33 типа площадок разбиты на 8 визуальных групп:
  - 🏢 Традиционные (5 типов)
  - 🏃 Спортивно-активные (5 типов)
  - 💧 Водные развлечения (1 тип)
  - 🎓 Образовательно-культурные (4 типа)
  - 🎨 Творческие мастерские (5 типов)
  - 🌳 Животные и природа (4 типа)
  - ✨ Специализированные (4 типа)
  - 🏡 Загородный отдых (2 типа)

#### 🌳 Динамическое дерево характеристик
- **Динамическое отображение VenueDetailsForm** - форма теперь автоматически меняется в зависимости от выбранной категории
- **Специфичные поля для 30 типов площадок** (91% покрытие):

  **Традиционные (5/5) ✅:**
  - Детский центр → возрастные группы
  - Лофт/Студия → высота потолков, естественное освещение
  - Кафе/Ресторан → тип кухни, детское меню
  - Парк развлечений → количество аттракционов, фудкорт
  - Открытая площадка → навес, запасной вариант при непогоде

  **Спортивные (5/5) ✅:**
  - Батутный центр → количество батутов, поролоновая яма
  - Картинг-центр → количество картов, длина трассы
  - Лазертаг/Пейнтбол → тип арены, макс. игроков
  - Скалодром/Веревочный парк → высота стены, трассы, автостраховка, прокат
  - Боулинг/Бильярд → дорожки, столы

  **Водные (1/1) ✅:**
  - Аквапарк/Бассейн → глубина, горки, детский бассейн, подогрев

  **Образовательные (4/4) ✅:**
  - Музей → тематика, экскурсии, интерактивные экспонаты
  - Театр/Кукольный театр → количество мест, тип театра
  - Планетарий → диаметр купола, система проекции, интерактивные шоу
  - Библиотека → места в читальном зале, детская секция

  **Творческие (5/5) ✅:**
  - Художественная студия → мольберты, материалы
  - Гончарная мастерская → гончарные круги, обжиг
  - Кулинарная студия → рабочие места, проф. оборудование
  - Столярная мастерская → верстаки, электроинструменты
  - Швейная мастерская → швейные машины, оверлоки

  **Природа (4/4) ✅:**
  - Зоопарк → виды животных, контактный зоопарк, кормление
  - Конный клуб → лошади, пони, инструктор
  - Аквариум/Океанариум → объем, виды рыб, шоу с дельфинами, контактный бассейн
  - Ферма/Экоферма → виды животных, кормление, сбор урожая, эко-продукты

  **Специализированные (4/4) ✅:**
  - VR-арена → станции, мультиплеер
  - Квест-комната → количество квестов, сложность
  - Кинотеатр → количество залов, 2D/3D/IMAX, частные показы
  - Торговый центр/Склад → зона погрузки, складское помещение

  **Загородные (2/2) ✅:**
  - База отдыха/Загородный клуб → домики, территория, барбекю, водоем
  - Глэмпинг → домики, уровень комфорта, электричество, отопление

### 🔧 Улучшения

- **Рефакторинг ProfileManageClient** - упрощена структура, убран режим создания (теперь используется единая форма)
- **Все 8 групп типов площадок полностью покрыты** специфичными полями

### 🐛 Исправления багов

- **Исправлено деление на ноль в прогресс барах** (`app/(admin)/admin/pages-status/page.tsx`)
  - Добавлена проверка `stats.total > 0` перед делением
  - Исправлено 4 места с `NaN` в CSS width
- **Добавлено обязательное поле city** при создании профиля
- **Исправлено название поля** `is_verified` → `verified`

### 📚 Документация

- Добавлена финальная документация по упрощенной системе профилей
- Добавлена документация по прогрессу специфичных полей (20 → 30 типов)
- Добавлена документация по объединению форм и откату изменений
- Обновлена финальная документация - 30 из 33 типов готово (91%), все 8 групп полностью покрыты

### 📁 Изменения в структуре файлов

**Новые компоненты:**
- `components/features/profile/category-visual-selector.tsx` - визуальный выбор категории
- `components/features/profile/venue-type-grouped-selector.tsx` - группированный выбор типа площадки
- `app/api/profiles/quick-create/route.ts` - API для быстрого создания профиля

**Обновленные компоненты:**
- `components/features/profile/details-forms.tsx` - добавлены специфичные поля для 30 типов (+1685 строк)
- `components/features/profile/create-profile-form.tsx` - интегрирован VenueDetailsForm
- `app/(dashboard)/profiles/page.tsx` - кнопка "Создать" → quick-create
- `app/(dashboard)/profiles/[slug]/edit/client.tsx` - обновлен ProfileManageClient

**Backup файлы (старая система):**
- `components/features/profile/create-profile-wizard.tsx.backup`
- `components/features/profile/wizard-progress.tsx.backup`
- `components/features/profile/draft-continue-dialog.tsx.backup`

---

## 📊 Статистика изменений

- **Добавлено строк кода**: ~2464 строк
- **Новых компонентов**: 3
- **Обновлено компонентов**: 4
- **Исправлено багов**: 3
- **Добавлено типов площадок со специфичными полями**: 30 из 33 (91%)
- **Полностью покрытых групп**: 8 из 8 (100%)

---

## 🎯 Что осталось (низкий приоритет)

### Специфичные поля для оставшихся 3 типов:
- Антикафе - почасовая оплата, настольные игры
- Коворкинг - рабочие места, переговорные
- Спортивный клуб - виды спорта, тренажеры

*Эти типы редко используются для детских праздников*

---

## ✅ Преимущества новой системы

- ✅ **Максимально упрощено** - 1 клик создает профиль
- ✅ **Нет дублирования** - один код для всего
- ✅ **Полная синхронизация** - create = edit
- ✅ **Улучшенный UX** - визуальные селекторы
- ✅ **Динамическое дерево** - категория → тип → специфичные поля
- ✅ **30 типов площадок** с уникальными характеристиками (91%)
- ✅ **Все 8 групп** полностью покрыты ⭐
- ✅ **Проще поддержка** - изменения в одном месте

---

**Версия**: Профили v2.0  
**Дата**: Последние 32-33 часа  
**Статус**: ✅ Готово к тестированию

